# 5.11 调研事实

调研日期：2026-05-11  
工作区：`c:\Users\kukisama\Downloads\Project\docintel-md`

> 结论先说：当前工作区不是一个单一项目，而是“题库生产流水线 + 本地考试客户端 + AI/Copilot 技能包 + 大量中间产物”的组合仓库。它们功能上互补，源码层面没有明显构建冲突；真正需要治理的是数据源重复、旧新导入器并存、脚本硬编码路径、题库生产流程依赖 VS Code/Copilot skill 的问题。

## 1. 当前工作区有哪些项目，是否冲突，各自提供什么功能

### 1.1 项目/模块清单

| 模块 | 位置 | 技术栈 | 主要功能 | 当前定位 |
|---|---|---|---|---|
| `docintel-md` 根 Rust CLI | `Cargo.toml`、`src/main.rs` | Rust + `ureq` + `lopdf` | 调 Azure Document Intelligence；PDF 拆分；OCR Markdown/JSON 输出；按 `Question #` 切题；机械 JSON 抽取 | OCR/结构化前处理工具 |
| SC-100 视觉题库 skill | `.github/skills/sc100-vision-md/` | Copilot skill + Python 脚本 | 自动续批、渲染 PDF 页、抽取辅助文本、视觉读题、生成标准 Markdown、导入 SQLite、补 HOTSPOT/DRAG DROP 交互表 | SC-100 PDF → Markdown/SQLite 主流程 |
| 原创模拟题生成 skill | `.github/skills/exam-question-gen/` | Copilot skill + Python 导入器 | 按考试代号原创生成模拟题 Markdown；支持单选、多选、HOTSPOT、DRAG DROP、yes/no；导入同一套 TauriExam SQLite schema | AI 生成题库流程 |
| 根目录批处理脚本 | `continue-sc100.ps1`、`continue-exam-gen.ps1`、`batch-add-interactions.ps1`、`dabao.ps1` | PowerShell + Copilot CLI + Python 脚本 | 批量驱动 Copilot skill；补交互结构；同步题库；构建/启动 TauriExam | 流程胶水 |
| TauriExam 本地考试客户端 | `TauriExam/` | Tauri 2 + Rust + React + TypeScript + SQLite | 本地刷题、考试、历史、错题/收藏/复习、PDF 页渲染、AI 分析、题目翻译、复杂题交互自动判分 | 当前用户端产品雏形 |
| 旧导入器/实验脚本 | `scripts/` | Python | 旧版 `output/vision-md`、`output/exam-gen` 到 SQLite 导入 | 兼容/历史用途，已不应作为主流程 |
| 数据与中间产物 | `output/`、`question-banks/`、`TauriExam/question-banks/` | Markdown、PNG、SQLite、PDF | OCR 结果、渲染页图、视觉整理 Markdown、最终题库 SQLite/PDF、考试记录 | 数据资产与缓存 |
| 设计/规划文档 | 根目录多个 `.md` | Markdown | 题库流程、拖拽题、架构、验证报告等设计记录 | 项目知识库 |

### 1.2 是否冲突

#### 源码/构建层面：没有明显硬冲突

- 根项目 `Cargo.toml` 包名是 `docintel-md`，职责是命令行 OCR/切题工具。
- `TauriExam/src-tauri/Cargo.toml` 包名是 `tauri-exam`，是 Tauri 后端，和根 Rust CLI 是两个独立 Cargo 项目。
- `TauriExam/package.json` 只服务 Tauri 前端，根目录没有 npm 项目；Node 依赖不会与根 Rust CLI 冲突。
- `.github/skills/**` 是 Copilot skill 资产，不参与常规 Rust/Node 构建。
- `target/` 被 `.gitignore` 忽略，属于构建产物；Tauri 自己也有 `src-tauri/target` 构建区。

#### 数据/流程层面：存在“重复源”和“旧新路径并存”的冲突风险

1. **SQLite 生成位置有两套历史路径**
   - 根 `README.md` 仍描述旧流程：`scripts/import_vision_md_to_sqlite.py` 默认输出 `output/vision-db/sc-100.sqlite`。
   - skill 新流程默认输出：`question-banks/<EXAM>.sqlite`。
   - TauriExam 当前发现题库时优先使用 `%LOCALAPPDATA%\TauriExam\question-banks`，并会从旧位置迁移/复制。
   - 结论：`output/vision-db/sc-100.sqlite` 更像旧中间产物，`question-banks/SC-100.sqlite` 更像当前主题库，但文档没有完全统一。

2. **题库文件存在重复副本**
   - `question-banks/SC-100.sqlite`：2,170,880 bytes，修改时间 2026-05-10 19:07:54。
   - `TauriExam/question-banks/SC-100.sqlite`：2,170,880 bytes，修改时间 2026-05-10 19:07:54。
   - `question-banks/SC-100.pdf` 与 `TauriExam/question-banks/SC-100.pdf` 大小也一致：32,844,399 bytes。
   - 结论：目前这两个位置内容一致，但这是同步结果，不应长期作为两个“真实源”。后续一旦只更新其中一个，就会出现“应用看到的题库”和“脚本更新的题库”不一致。

3. **脚本硬编码本机 Python 路径**
   - `continue-sc100.ps1`、`batch-add-interactions.ps1`、skill 内 `continue-sc100-vision.ps1` 都默认使用 `C:/Users/kukisama/AppData/Local/Programs/Python/Python312/python.exe`。
   - 这能在当前机器工作，但不适合作为独立产品分发。

4. **根目录旧导入脚本与 skill 内新版导入器并存**
   - `scripts/import_vision_md_to_sqlite.py` schema 较旧，缺少新版统一 `interaction_options` / `interaction_targets` 的完整能力。
   - `.github/skills/sc100-vision-md/scripts/import_question_md_to_sqlite.py` 与 `.github/skills/exam-question-gen/scripts/import_question_md_to_sqlite.py` 是当前更完整的导入器。
   - 结论：旧脚本应标记 deprecated，避免将新格式 Markdown 导入到旧 schema 后造成 TauriExam 降级为人工自评。

### 1.3 各模块之间的真实关系

```text
PDF / AI 原创题
   │
   ├─ docintel-md：OCR / PDF 拆分 / 机械切题 / JSON 初抽取（偏前处理）
   │
   ├─ sc100-vision-md skill：视觉读 PDF 页 → 标准 Markdown → SQLite
   │
   ├─ exam-question-gen skill：原创 Markdown → SQLite
   │
   ├─ batch-add-interactions.ps1：检测 SQLite 缺交互结构 → 调 Copilot 补 Markdown → 增量导入 → 同步副本
   │
   └─ TauriExam：读取 SQLite/PDF → 本地刷题/考试/翻译/AI 分析/错题复习
```

根 Rust CLI 和 TauriExam 并不是相互调用的运行时关系。它们通过文件系统产物间接连接：Markdown、SQLite、PDF、PNG。

## 2. 文件/目录存放方式是否需要改进

### 2.1 当前目录职责判断

| 路径 | 当前实际内容 | 判断 |
|---|---|---|
| `src/` | 根 Rust CLI 源码 | 合理 |
| `TauriExam/` | Tauri 客户端完整子项目 | 合理，但可考虑未来独立仓库或 workspace 成员化 |
| `.github/skills/` | 两个 Copilot skill 及其脚本/模板 | 合理，符合 VS Code/Copilot 约定 |
| `output/document-intelligence/` | Azure Document Intelligence 输出 | 中间产物，合理；已被 `.gitignore` 忽略 |
| `output/pdf-chunks/` | PDF 分块 | 中间产物，合理；可定期清理 |
| `output/question-pipeline/` | 机械切题和 JSON 初抽取 | 中间产物，合理；但不应混入最终题库 |
| `output/vision-pages/` | PDF 页 PNG / 文本辅助文件 | 缓存/中间产物，体积大；合理但建议加清理策略 |
| `output/vision-md/` | 人工/AI 视觉整理后的标准 Markdown | 这是“高价值源数据”，不只是普通输出；建议提升为一等数据目录 |
| `output/vision-db/` | 旧流程生成的 SQLite | 建议标记 deprecated 或迁移到 `question-banks/` |
| `output/exam-gen/` | AI 原创题 Markdown | 这是“高价值源数据”；建议与 vision-md 一样纳入 source-of-truth 管理 |
| `question-banks/` | 当前最终 SQLite/PDF | 当前最接近“发布题库目录”，合理 |
| `TauriExam/question-banks/` | Tauri 本地开发副本 | 不合理作为长期真实源；最多保留为开发样例/迁移兼容 |
| `scripts/` | 旧导入脚本 | 建议 deprecated 或迁入 `legacy/` |
| 根目录大量设计 `.md` | 设计/验证/脑暴文档 | 可以保留，但建议按 `docs/` 归档 |

### 2.2 需要改进的点

#### A. 统一“源数据、构建产物、发布产物、运行时数据”四类目录

建议目标结构：

```text
docs/
  research/
  design/
  validation/

apps/
  tauri-exam/                 # 当前 TauriExam，可后续迁移

tools/
  docintel-md/                # 根 Rust CLI 若未来 workspace 化
  exam-pipeline/              # 未来独立流水线 CLI，推荐 Rust/C#

skills/
  sc100-vision-md/
  exam-question-gen/

data-source/
  vision-md/                  # 人工/AI 精修 Markdown，建议作为可审计源
  exam-gen-md/                # AI 原创 Markdown，建议作为可审计源
  raw/                        # 原始 PDF，可选，不一定入 Git

artifacts/
  question-banks/             # 由 data-source 构建出的 SQLite/PDF 发布包
  reports/                    # 校验报告

cache/
  document-intelligence/
  pdf-chunks/
  vision-pages/
  question-pipeline/
```

不一定马上大搬家，但应该先在文档和脚本里明确：

- **Markdown 源数据**：`output/vision-md`、`output/exam-gen`。
- **最终发布题库**：`question-banks/*.sqlite` + 同名 `.pdf`。
- **应用运行时题库**：`%LOCALAPPDATA%\TauriExam\question-banks`。
- **TauriExam/question-banks**：只作为开发样例或兼容迁移目录，不能作为主源。

#### B. 移除或降低重复题库副本的重要性

当前 `batch-add-interactions.ps1` 会同步到：

```text
%LOCALAPPDATA%\TauriExam\question-banks\SC-100.sqlite
TauriExam\question-banks\SC-100.sqlite
```

更好的方式：

1. `question-banks/` 作为构建产物主目录。
2. TauriExam 第一次启动时从 `question-banks/` 或安装包资源导入到 `%LOCALAPPDATA%`。
3. 后续运行只读 `%LOCALAPPDATA%\TauriExam\question-banks`。
4. 不再维护 `TauriExam/question-banks` 副本，除非它是明确的示例数据。

#### C. 根 README 需要更新

根 `README.md` 还把 `scripts/import_vision_md_to_sqlite.py` 和 `output/vision-db/sc-100.sqlite` 描述为主导入路径。实际新链路已迁到 skill 内导入器，并输出 `question-banks/<EXAM>.sqlite`。建议 README 改成：

```text
主流程：.github/skills/*/scripts/import_question_md_to_sqlite.py → question-banks/<EXAM>.sqlite
旧流程：scripts/import_vision_md_to_sqlite.py → output/vision-db/sc-100.sqlite（legacy，只读参考）
```

#### D. 脚本参数化，去掉本机绝对路径

当前脚本为了效率可以保留默认值，但建议逐步变为：

- `$Python` 改为自动发现：优先环境变量 `PYTHON`，其次 `py -3`，再次 `python`。
- `$Db` 默认保留 `question-banks/<EXAM>.sqlite`。
- `$InputRoot` 支持参数传入。
- Copilot 模型和 effort 保留参数。
- 所有路径基于 `$PSScriptRoot` 转绝对路径，避免从其他工作目录执行时失败。

#### E. 明确 `.gitignore` 策略

当前 `.gitignore` 忽略：`target/`、`.env`、`output/`、`*.pdf`、`__pycache__/` 等。注意：

- `question-banks/*.sqlite` 没有被忽略，意味着 SQLite 可能会进入版本管理。
- `question-banks/*.pdf` 会被 `*.pdf` 忽略。
- 如果题库 SQLite 是“发布资产”，可以继续不忽略，但最好使用 release artifact 管理。
- 如果题库涉及版权/内部内容，不应进入 Git；应改为 `question-banks/*.sqlite` 忽略，通过对象存储/制品库分发。

## 3. TauriExam + skill + ps1 是否完成题库处理全链路

### 3.1 SC-100 PDF 题库链路

当前已具备的事实链路：

```text
SC-100 PDF
  │
  ├─ detect_next_batch.py
  │   └─ 根据 output/vision-md 自动判断下一批页码/题号
  │
  ├─ render_pdf_pages.py
  │   └─ PDF 页 → output/vision-pages/**/page-xxx.png
  │
  ├─ extract_page_text.py
  │   └─ PDF 辅助文本，帮助抄录但不作为最终判断
  │
  ├─ Copilot + sc100-vision-md skill
  │   └─ 视觉读题 → output/vision-md/**/sc-100-questions-*.md
  │
  ├─ import_question_md_to_sqlite.py
  │   └─ Markdown → question-banks/SC-100.sqlite
  │
  ├─ batch-add-interactions.ps1
  │   ├─ detect_missing_interactions.py 查询 DB 缺失交互结构
  │   ├─ 调 Copilot 原地补 Interaction Options/Targets
  │   ├─ 增量导入单个 MD 文件
  │   ├─ SQL 验证 option_count/target_count
  │   └─ 同步 SQLite 到 AppData / TauriExam 开发副本
  │
  └─ TauriExam
      ├─ 发现题库 SQLite + 同名 PDF
      ├─ 读取 questions/options/answer_areas
      ├─ 读取 interaction_options/interaction_targets 或旧 drag/hotspot 表
      ├─ PDFium 按需渲染 PDF 页
      ├─ 考试/历史/错题/收藏写入 app.sqlite
      └─ 翻译写入 <bank>.translations.sqlite
```

### 3.2 AI 原创题库链路

```text
用户指定考试代号，如 AI-900
  │
  ├─ continue-exam-gen.ps1
  │   └─ 调 Copilot + exam-question-gen skill 续写 Markdown
  │
  ├─ output/exam-gen/<EXAM>/questions-*.md
  │
  ├─ .github/skills/exam-question-gen/scripts/import_question_md_to_sqlite.py
  │   └─ Markdown → question-banks/<EXAM>.sqlite
  │
  └─ TauriExam 读取并刷题
```

### 3.3 是否“完成了整个题库处理全链路”

答案要分层看：

#### 从“当前个人工作区可用性”看：基本完成

已完成：

- PDF 视觉整理。
- 原创模拟题生成。
- Markdown 标准结构。
- Markdown 导入 SQLite。
- HOTSPOT / DRAG DROP / ordered-list 的统一交互表。
- TauriExam 对统一交互表的自动渲染与判分。
- 本地考试记录、错题、收藏、复习。
- 同名 PDF 按需渲染。
- AI 分析与题目翻译缓存。
- 打包 release exe，`dabao.ps1` 会构建并复制 `pdfium.dll`。

#### 从“独立产品/可复制流水线”看：还没完成

主要缺口：

1. **生产链路依赖 VS Code/Copilot skill 语义环境**
   - skill 能在 VS Code/Copilot 中很好地指导 AI，但不是一个独立可执行 pipeline。
   - `continue-sc100.ps1` 和 `batch-add-interactions.ps1` 通过 `copilot --model ... -p ... --allow-all` 驱动，仍然依赖 Copilot CLI、模型权限和当前工作区 skill 发现。

2. **导入器仍是 Python 脚本**
   - 当前能用，但若目标是小体积、独立分发、少依赖，应迁到 Rust 或 C#。
   - 这也符合未来把 TauriExam 或服务端做成可部署产品的方向。

3. **TauriExam 只消费题库，不生产题库**
   - TauriExam 能读取 SQLite/PDF、渲染、考试、翻译。
   - 它没有内置“导入 Markdown → 生成题库 SQLite”的 UI。
   - 它也没有内置“从 PDF 生成 Markdown”的完整 AI 工作流。

4. **缺少题库包版本/manifest**
   - 当前靠文件名 `SC-100.sqlite`、`SC-100.pdf` 关联。
   - 没有 `bank.json` 描述版本、来源、更新时间、schema 版本、题数、hash、权限标签。

5. **质量门禁分散在脚本里**
   - `batch-add-interactions.ps1` 有交互缺失检测和 DB 校验。
   - TauriExam 有 `check_bank_health`。
   - 但没有统一的 `validate-bank` 命令输出完整报告。

### 3.4 当前连接方式是否已经整合在一起

已经整合的部分：

- 两个 skill 的导入器都输出 TauriExam 能读的同一套 SQLite schema。
- TauriExam 后端明确优先读取 `interaction_options` / `interaction_targets`，兼容旧 `drag_options` / `drag_slots` / `hotspot_options` / `hotspot_rows`。
- `batch-add-interactions.ps1` 能完成“发现缺失 → AI 补表 → 导入 → DB 验证 → 同步”。
- `dabao.ps1` 能生成可运行 release exe 并处理 PDFium。

未真正整合的部分：

- skill 是“AI 行为规范”，不是流水线服务。
- PowerShell 是调度胶水，和 TauriExam 没有统一产品入口。
- 数据目录存在多个主张：`output/vision-db`、`question-banks`、`TauriExam/question-banks`、`%LOCALAPPDATA%`。
- 题库生产和题库消费仍是两个世界：生产在 VS Code/脚本中，消费在 TauriExam 中。

### 3.5 是否可能不依赖 VS Code 工作区而独立工作

可能，而且建议这么做。分两种独立程度：

#### 方案 A：短期“半独立”

目标：不依赖 VS Code UI，但仍可依赖 Copilot CLI 或某个 AI API。

做法：

1. 保留 Markdown 合约和 SQLite schema。
2. 把 skill 内 Python 脚本迁出为独立 `exam-pipeline` CLI。
3. `exam-pipeline` 提供命令：
   - `detect-next-batch`
   - `render-pages`
   - `import-md`
   - `detect-missing-interactions`
   - `validate-bank`
   - `sync-bank`
4. PowerShell 只保留极薄包装，或完全取消。
5. Copilot prompt 从 skill 文档固化为 CLI 模板文件，运行时调用 Copilot CLI 或 OpenAI-compatible API。

优点：迁移成本低。  
缺点：AI 视觉读题仍依赖外部模型能力，不能完全离线。

#### 方案 B：中期“产品化独立”

目标：一个后台服务负责题库生产，一个客户端/网页负责刷题。

做法：

- 后台服务接收 PDF 或 Markdown。
- 服务端渲染 PDF 页、调用模型视觉读取、生成结构化题目、入库。
- 用户不再直接接触 `output/vision-md` 和脚本。
- TauriExam 或 Web 前端只通过 API 获取题库。

优点：适合多人、多设备、权限管理、云端题库。  
缺点：需要设计账号、权限、存储、任务队列、成本控制。

#### 方案 C：长期“双模式”，但默认应是“受保护在线模式”

- 作者/内部模式：TauriExam + 本地 SQLite/PDF，适合你自己生产、验证、快速迭代题库。
- 普通用户模式：AAD 登录 + API + 云端题库，用户端只拿当前题、当前交互、当前页图片，不拿完整 SQLite/PDF。
- 受信任离线模式：只给特定内部用户、特定设备、特定授权下载题库包；普通商业用户默认不开放。

我建议未来走“双模式”，但要明确默认产品策略：**SQLite 是作者/后台/内部构建格式，不是普通用户的分发格式**。云端模式要把 SQLite 从“客户端本地数据库”变成“服务端导入/导出/备份格式”。

## 4. exam 项目未来 CS/BS 架构与云端题库存储设计

### 4.1 目标需求重新表述

你希望未来的 exam 项目变成：

1. 用户登录一个 Microsoft Entra ID（AAD）账户。
2. 系统基于用户身份、组、角色、授权策略判断他能看到哪些题库。
3. 题库本体、题库 SQL、翻译 SQL 不放在用户本机作为唯一源，而在云端服务器存储。
4. 可以用容器方案，也可以用更合适的方案。
5. 支持未来题库更新、权限控制、翻译、考试记录、错题、AI 分析等能力。
6. 作为作者，PDF 和 SQLite 是核心资产；普通用户不应拿到完整 PDF、完整 SQLite 或完整翻译库，只能通过授权前端/API 按需访问。

### 4.2 CS 还是 BS：建议主线采用 B/S，Tauri 作为在线受控客户端

#### B/S 架构优势

- AAD 登录天然适合 Web：OIDC/OAuth2、MSAL、条件访问、企业组权限都成熟。
- 题库权限由服务端控制，不依赖分发本地 SQLite。
- 更新题库不需要用户升级客户端。
- 翻译、AI 分析、审计、统计都能集中处理。
- 更适合未来多用户、多租户和题库授权。

#### C/S 架构优势

- Tauri 已经有完整本地体验：快、离线、PDF 渲染、考试记录。
- 本地 SQLite 适合单人高性能刷题。
- 对大题库、PDF 原文页，本地缓存体验好。

#### 推荐结论

主产品设计为：

```text
B/S 为主：Web App + API + 云端数据库/对象存储
C/S 为辅：TauriExam 作为可登录的在线受控客户端；离线包仅限作者/内部受信任场景
```

Tauri 未来不应该直接持有“所有题库真实源”，普通用户版本应该：

- 登录 AAD。
- 拉取用户可访问题库列表。
- 通过 API 按题号/考试会话请求题目，不下载完整题库。
- 通过 API 请求当前题涉及的 PDF 页渲染图，不下载原始 PDF。
- 本地只缓存极少量短 TTL 数据，例如当前题、下一题预取、用户作答草稿。
- 缓存标记 `no-store` 或加密存放，并在退出/过期/切换账号时清理。
- 在线时同步考试记录、错题、收藏、翻译缓存。

只有作者本人、管理员、内部审题人员，才可以使用“完整 SQLite/PDF 本地模式”。

### 4.3 推荐服务端总体架构

本节原先按 Azure 托管服务展开。根据后续讨论，当前更合适的主线应调整为：**纯容器自托管优先，Azure SQL / Blob / Container Apps 作为以后云托管升级方案**。

优先架构不再要求把题库拆进 Azure SQL，而是先复用当前 SQLite schema，并用容器挂载目录保存私有资产。

```text
Microsoft Entra ID
   │
   │ OIDC / OAuth2 token
   ▼
Frontend
  ├─ Web App：React/Next.js/Blazor/ASP.NET Razor 均可
  └─ TauriExam：可选，同一套 API
   │
   ▼
API Backend
  ├─ AuthZ：用户、组、角色、题库授权
  ├─ Bank Catalog：题库目录与版本
  ├─ Question API：分页/搜索/考试/判分
  ├─ Translation API：翻译读取/生成/缓存
  ├─ Practice API：考试历史、错题、收藏、掌握度
  ├─ Import Pipeline：Markdown/SQLite/PDF 导入与校验
  └─ Admin API：题库发布、权限分配、版本回滚
   │
    ├──────────────────────┬──────────────────────────┬────────────────────┐
    ▼                      ▼                          ▼                    ▼
  SQLite 题库包/用户库       宿主机 Volume / 可选 MinIO   Worker/Job           Secret 配置
  复用当前 schema            私有 PDF/JPG/SQLite/缓存     导入/翻译/转图        OIDC/AI/翻译密钥
```

### 4.4 部署形态建议

  #### 首选：纯容器 + 宿主机 Volume + SQLite

适合当前阶段，原因：

  - 不强依赖 Azure SQL、Blob 或其它云服务。
  - 可以直接用 `docker compose` 部署。
  - 最大化复用当前 `question-banks/*.sqlite` schema。
  - PDF、JPG、SQLite 都是普通文件，适合挂载 `/data`。
  - 作者核心资产仍留在服务端容器 Volume，不给普通用户下载。
  - Rust API/worker 可以做成小体积单二进制镜像。

推荐组件：

  | 组件 | 推荐形态 | 原因 |
|---|---|---|
  | Web 前端 | `exam-web` 容器或静态文件由反代托管 | 复用当前 React/Vite 前端 |
  | API | `exam-api` Rust 容器 | 鉴权、ACL、题目 API、判分、PDF 页图响应 |
  | 后台任务 | `exam-worker` Rust 容器 | Markdown 导入、PDF 转 JPG、翻译、缓存预热 |
  | 数据库 | `/data/app/app.sqlite` + `/data/banks/*/bank.sqlite` | 先复用 SQLite，降低迁移成本 |
  | 大文件 | `/data/banks/*/source.pdf`、`/data/banks/*/pages/base/*.jpg` | 宿主机持久化，普通用户只能经 API 访问 |
  | 对象存储 | 可选 MinIO | 多实例或远程存储时再上 |
  | 反向代理 | Caddy/Nginx/Traefik | TLS、静态资源、API 路由 |
  | 登录 | Microsoft Entra ID OIDC | 仍可保留 AAD 登录和组权限 |
  | Secret | `.env` / Docker secret / 后续 Key Vault | 第一版用环境变量，生产再升级 |

  #### 备选：Azure Container Apps + Azure SQL + Blob Storage

  如果后续要云托管、多实例、自动备份、对象存储生命周期管理，可以升级为：

  - Container Apps 托管 `exam-api` / `exam-worker`。
  - Azure SQL 或 Postgres 管用户、ACL、审计、学习记录。
  - Blob Storage 管私有 PDF、页图、SQLite 制品。
  - Key Vault 管 AI Key、Translator Key、OIDC client secret。

  这个方案不再作为第一阶段首选，而是作为规模化/云托管升级路径。

#### 备选：Azure App Service + Azure SQL + Blob

如果后端用 ASP.NET Core 且不需要复杂后台任务，App Service 更简单。

#### 不建议一开始上 AKS

AKS 对这个项目过重。除非后续有大量模型推理、复杂队列和多服务治理，否则 Container Apps 足够。

### 4.5 云端题库存储形式设计

这里是关键：你提到“题库的具体 sql 以及翻译 sql，实际是在云端一个服务器存储”，并且现在补充了“普通用户不能拿到完整缓存”。按当前纯容器方向，我建议第一阶段不要把 SQLite 强行拆成云数据库，而是采用“服务端私有 SQLite + API 最小化访问”：

1. **题库主文件**：`/data/banks/<bank>/bank.sqlite`，复用当前 TauriExam schema，服务端只读打开。
2. **翻译文件**：`/data/banks/<bank>/translations.sqlite` 或合并进 `bank.sqlite` 的 `question_translations` 表。
3. **用户/权限/审计库**：`/data/app/app.sqlite`，保存用户、ACL、考试记录、错题、收藏、审计。
4. **PDF/页图资产**：`/data/banks/<bank>/source.pdf`、`/data/banks/<bank>/pages/base/*.jpg`，只由服务端读取。
5. **普通用户访问方式**：用户通过前端/API 逐题访问数据，PDF 只返回服务端渲染后的当前页图片或裁剪图，不返回原始 PDF 文件。

这样兼顾：

- Web/API 查询性能和权限控制。
- 作者仍可使用 SQLite 做本地生产、验证、备份。
- 普通用户无法一次性复制完整题库数据库和 PDF。
- 可版本化、可回滚、可导出。

如果后续改成 Azure SQL/Postgres，可以把当前 SQLite schema 视为导入源和离线制品，而不是第一天就拆表迁移。

### 4.6 Azure SQL 逻辑 schema 建议

#### 题库目录与版本

```sql
tenants
- id
- entra_tenant_id
- name
- created_at

exam_banks
- id
- tenant_id
- exam_code              -- SC-100 / AI-900
- slug                   -- sc-100
- title
- description
- status                 -- draft / published / archived
- visibility             -- private / org / public
- owner_object_id
- created_at
- updated_at

exam_bank_versions
- id
- bank_id
- version                -- 2026.05.11.1 或 semver
- schema_version
- source_kind            -- vision_pdf / ai_generated / imported_sqlite / manual
- source_commit_hash     -- 可选
- sqlite_blob_url        -- 导出的 SQLite 包
- pdf_blob_url           -- 同名 PDF，可空
- manifest_blob_url
- question_count
- content_hash
- published_by
- published_at
- is_current
```

#### 题目主数据

```sql
questions
- id
- bank_version_id
- stable_question_key     -- sc-100-q0001，跨版本稳定
- sequence_number
- source_question_number
- topic
- question_type           -- single_choice / multiple_choice / hotspot / drag_drop / yes_no_series
- interaction_kind        -- dropdown_hotspot / drag_drop / ordered_list / image_hotspot / null
- difficulty
- status                  -- parsed / needs_review / version_sensitive / archived
- source_pages
- page_from
- page_to
- question_text
- question_md
- source_answer
- recommended_answer
- chinese_judgement
- reasoning
- notes
- md_file
- pdf_file
- created_at
- updated_at
```

#### 标准选项与答案区

```sql
question_options
- id
- question_id
- option_key              -- A/B/C/D
- option_text
- sort_order

answer_areas
- id
- question_id
- prompt
- source_selection
- recommended_selection
- sort_order
```

#### 统一复杂交互模型

继续沿用当前已经验证过的统一模型：

```sql
interaction_options
- id                      -- opt-a / opt-b；题内唯一
- question_id
- option_text
- option_group
- is_distractor
- sort_order
- primary key(question_id, id)

interaction_targets
- id                      -- target-1 / slot-1
- question_id
- position
- target_label
- option_group
- correct_option
- primary key(question_id, id)
```

说明：

- 不建议云端继续扩散 `drag_options`、`hotspot_options` 等多套表。
- 云端主 schema 只保留 `interaction_options` / `interaction_targets`。
- 导出到旧 TauriExam SQLite 时，可以兼容性生成旧表。

#### 翻译存储

当前 TauriExam 本地翻译库是 `<bank>.translations.sqlite`，核心表是 `translation_segments`，按 `field_name + segment_index + source_hash + language` 缓存。云端可以保留这个思想：

```sql
translation_packages
- id
- bank_version_id
- language                -- zh-CN / ja-JP / etc.
- provider                -- microsoft-translator / ai / human
- model
- status                  -- partial / complete / failed
- translated_question_count
- created_at
- updated_at

translation_segments
- id
- package_id
- question_id
- field_name              -- question_text / option:A / interaction_option:opt-a / reasoning
- segment_index
- source_hash
- translated_text
- provider
- model
- version
- created_at
- updated_at
```

为什么按 segment 存，而不是一题一个大 JSON：

- 源字段没变时可复用缓存。
- 题干变了不影响选项翻译。
- 支持局部重翻。
- 和当前 TauriExam 代码一致，迁移成本低。

#### 用户学习数据

```sql
exam_sessions
- id
- user_object_id
- tenant_id
- bank_id
- bank_version_id
- title
- mode
- started_at
- finished_at
- duration_seconds
- total_questions
- correct_count
- wrong_count

exam_answers
- id
- session_id
- user_object_id
- question_id
- sequence_number
- user_answer             -- JSON 或普通 A,B
- correct_answer
- recommended_answer
- is_correct
- duration_seconds
- created_at

question_flags
- id
- user_object_id
- bank_id
- question_id
- flag_type               -- favorite / wrong / needs_review / mastered
- note
- created_at
- updated_at

question_progress
- user_object_id
- question_id
- attempt_count
- wrong_count
- latest_is_correct
- mastery_score
- latest_answered_at
```

#### 权限与授权

```sql
bank_acl
- id
- bank_id
- principal_type          -- user / group / app_role
- principal_object_id     -- Entra user/group object id 或 app role id
- role                    -- reader / learner / editor / owner
- valid_from
- valid_to
- created_by
- created_at

bank_version_acl          -- 可选，若不同版本权限不同
- id
- bank_version_id
- principal_type
- principal_object_id
- role
```

权限建议：

- 默认按 `exam_banks` 授权。
- 若某些用户只可看某个版本，再引入 `bank_version_acl`。
- AAD token 进入 API 后，服务端读取用户 `oid`、`tid`、`groups` 或 app roles，匹配 ACL。
- 大量 group 时不要依赖 token 全量 groups，应通过 Microsoft Graph 或本地 group cache 解析。

### 4.7 Blob Storage 文件布局

```text
exam-banks/
  <tenant-id>/
    <bank-slug>/
      versions/
        2026.05.11.1/
          bank.sqlite                  # 内部/作者包，不给普通用户直接下载
          bank.translations.zh-CN.sqlite # 内部/作者包，不给普通用户直接下载
          source.pdf                   # 私有原始 PDF，不给普通用户直接下载
          manifest.json
          import-report.json
          pages/
            page-001.png               # 可选预渲染；普通用户也应经 API 鉴权后按需取
          source-md/
            sc-100-questions-001-010.md
      latest.json
```

`manifest.json` 建议内容：

```json
{
  "schemaVersion": 1,
  "bankId": "sc-100",
  "version": "2026.05.11.1",
  "examCode": "SC-100",
  "questionCount": 793,
  "contentHash": "sha256:...",
  "sqlite": {
    "path": "bank.sqlite",
    "sha256": "..."
  },
  "pdf": {
    "path": "source.pdf",
    "sha256": "..."
  },
  "translations": [
    {
      "language": "zh-CN",
      "path": "bank.translations.zh-CN.sqlite",
      "coverage": 1.0
    }
  ],
  "createdAt": "2026-05-11T00:00:00Z"
}
```

这样服务端/作者工具可以：

- 对每个题库版本做 hash 校验、回滚和审计。
- 给内部审题或作者本机生成完整离线包。
- 普通用户只获取“可访问题库列表”和当前题所需资源，不获取 `bank.sqlite`、`source.pdf`、完整翻译库。

### 4.8 API 设计建议

#### 用户与题库目录

```http
GET /api/me
GET /api/banks
GET /api/banks/{bankId}
GET /api/banks/{bankId}/versions
GET /api/banks/{bankId}/client-manifest?version=current
```

说明：`client-manifest` 只能返回题库名、版本、题数、用户权限、前端功能开关等安全元数据，**不能返回** `bank.sqlite`、`source.pdf`、完整翻译包的 Blob URL、SAS 或可推导路径。完整 `manifest.json` 只给管理员 API、后台任务和作者工具使用。

#### 在线刷题

```http
GET /api/banks/{bankId}/questions?type=&topic=&status=&q=&page=&pageSize=
GET /api/banks/{bankId}/questions/{questionId}
GET /api/banks/{bankId}/questions/{questionId}/source-pages
POST /api/exam-sessions
POST /api/exam-sessions/{sessionId}/answers
GET /api/review/wrong?bankId=
POST /api/questions/{questionId}/flags
```

#### 翻译与 AI

```http
GET /api/banks/{bankId}/questions/{questionId}/translations?language=zh-CN
POST /api/banks/{bankId}/translate
POST /api/questions/{questionId}/ai/analyze
```

#### 管理员导入/发布

```http
POST /api/admin/banks
POST /api/admin/banks/{bankId}/imports
GET /api/admin/imports/{jobId}
GET /api/admin/banks/{bankId}/manifest?version=current
POST /api/admin/banks/{bankId}/versions/{versionId}/publish
POST /api/admin/banks/{bankId}/acl
```

### 4.9 导入流水线云端化

云端导入应拆成异步 job：

```text
上传 PDF / Markdown / SQLite
  │
  ▼
创建 import_job
  │
  ▼
Container Apps Job / Worker
  ├─ 校验输入
  ├─ PDF 渲染/页面抽取
  ├─ 调 AI 模型结构化读取（如需要）
  ├─ 生成标准 Markdown
  ├─ 导入 Azure SQL staging 表
  ├─ 运行 validate-bank
  ├─ 生成内部 SQLite 包（作者/后台/受信任用户使用；普通用户不下发）
  ├─ 写 Blob manifest
  └─ 等管理员发布
```

建议加入 staging/publish 双阶段：

- `draft`：导入后可预览、修复、重新翻译。
- `published`：发布后普通用户可见。
- `archived`：旧版本保留但默认不可见。

### 4.10 安全与权限细节

1. **登录**：前端用 MSAL，后端验证 JWT。
2. **租户隔离**：每条题库和学习记录都带 `tenant_id`。
3. **最小授权**：API 每次查询题库都走 `bank_acl`。
4. **Blob 访问**：不要公开容器；普通用户不拿原始 PDF/SQLite 的 SAS。若必须发 SAS，只能发给服务端渲染后的单页图片/裁剪图，并且短 TTL、绑定用户和题目上下文。
5. **翻译/AI Key**：放 Key Vault，后端 Managed Identity 读取。
6. **审计**：管理员发布题库、修改 ACL、批量导入都写 audit log。
7. **本地缓存保护**：普通用户版 Tauri 不下载完整 SQLite/PDF；只缓存当前题/下一题/当前页图，TTL 到期清理。内部版若允许下载完整包，应独立开关、独立授权、独立水印。
8. **题库水印/来源**：manifest 记录发布者、版本、hash，便于追责。

### 4.11 云端和本地 SQLite 的关系

建议保留 SQLite，但改变定位：普通用户不拿完整 SQLite，SQLite 主要服务作者、后台和受信任内部场景。

| 类型 | 当前定位 | 未来定位 |
|---|---|---|
| `question-banks/<EXAM>.sqlite` | 本地应用直接读取的题库源 | 作者/后台/内部构建与验证包；普通用户不直接下载 |
| `<EXAM>.translations.sqlite` | 本地翻译缓存 | 作者/后台翻译包；普通用户通过 API 按题读取翻译片段 |
| `app.sqlite` | 本地考试历史 | 普通用户可保留本地学习缓存，但内容只含用户记录，不含完整题库 |
| Azure SQL | 当前没有 | 云端主数据源、权限源、审计源 |

这样做的好处：

- B/S 用户直接查 Azure SQL/API。
- 普通 C/S 用户在线逐题请求，不拿全量库。
- 管理员/作者仍可用 SQLite 做快速备份、验证和内部迁移。
- 不会把大 PDF 和大量页图塞进关系数据库。

### 4.12 作者核心数据保护：不下发完整 PDF/SQLite 的在线授权模式

这个约束可以做到，而且应该作为商业化/共享模式的默认设计。核心思想是：**题库数据库、原始 PDF、完整翻译库永远留在服务端；客户端只拿“当前操作所需的最小数据”。**

#### 4.12.1 服务端持有核心资产

| 资产 | 存放位置 | 普通用户是否可直接拿到 | 访问方式 |
|---|---|---:|---|
| 原始 PDF | 服务端私有 Volume；后续可升级对象存储 | 否 | 服务端读取并渲染当前页/裁剪区 |
| 完整 SQLite 题库 | 服务端私有 Volume / 内部制品库 | 否 | 仅作者、后台任务、内部审题工具使用 |
| 完整翻译 SQLite | 服务端私有 Volume / 内部制品库 | 否 | 仅作者、后台任务、内部审题工具使用 |
| 题目结构化数据 | 服务端只读题库 SQLite；后续可升级 SQL/Postgres | 否，不能批量导出 | API 按题/按考试会话分页返回 |
| 当前题 PDF 页图 | 服务端动态渲染或私有预渲染图 | 是，但只是一页或裁剪图 | API 鉴权后返回，短 TTL、水印、禁止缓存 |
| 用户学习记录 | 服务端 `app.sqlite` + 用户本地轻缓存；后续可升级 SQL/Postgres | 用户只能看自己的 | API 读写 |

#### 4.12.2 普通用户请求模型

```text
用户登录 AAD
  │
  ▼
前端请求 /api/banks
  │  只返回题库名、题数、描述、用户权限，不返回 DB/PDF 地址
  ▼
开始练习 /api/exam-sessions
  │  服务端生成本次题目序列，只返回第一题 ID
  ▼
GET /api/exam-sessions/{id}/current-question
  │  返回当前题题干、选项、交互模型、当前语言翻译片段
  ▼
GET /api/questions/{id}/source-pages?page=123
  │  服务端从私有 PDF 渲染 page 123，加水印后返回图片
  ▼
POST /api/exam-sessions/{id}/answers
  │  服务端判分/记录/返回下一题
```

这里不要提供：

- `GET /download/SC-100.sqlite`
- `GET /download/SC-100.pdf`
- `GET /download/all-questions.json`
- 无限制的 `GET /api/questions?pageSize=999999`

这里的安全边界不是“用户猜不到文件地址”，而是**根本不存在普通用户可访问的文件直连地址**：

- `/data/banks/**` 不能被 Web server 当静态目录暴露。
- `source.pdf`、`bank.sqlite`、`translations.sqlite` 只能由后端进程通过本地文件系统或私有对象存储 SDK 读取。
- 反向代理只暴露 `/api/**` 和前端静态资源，不暴露 `/data/**`、`/banks/**`、`/files/**` 这类直连路由。
- 即使用户知道服务器内部路径，例如 `/data/banks/SC-100/source.pdf`，这也只是容器/宿主机文件路径，不是 HTTP URL。
- 如果以后用对象存储，bucket/container 必须是 private；不能公开读，不能给普通用户发完整 PDF/SQLite 的 SAS/签名 URL。
- 如果为了性能使用 Nginx `X-Accel-Redirect` 或类似内部转发，也必须由 API 先完成登录、题库授权、题目范围校验，再转发单页图/裁剪图；不能转发完整 PDF/SQLite。

#### 4.12.3 PDF 的保护方式

原始 PDF 永远在服务端私有存储中，第一版可以是宿主机 Volume，后续可升级到对象存储；普通用户只看服务端输出的图片：

1. API 接收 `questionId`，服务端验证用户是否有该题权限。
2. 服务端确认该题的 `page_from/page_to`，不允许用户随便枚举任意 PDF 页。
3. 服务端用 PDFium/容器任务渲染指定页。
4. 图片加动态水印：用户显示名、object id 末尾、时间、题库版本。
5. 返回 `Cache-Control: no-store` 或短 TTL。
6. 可选：只渲染题目所在区域，而不是整页。
7. 可选：图片 URL 使用一次性 token，几分钟过期。

注意：只要用户能在屏幕上看到内容，就无法从物理上阻止截图、拍照或手工抄录。这个设计的目标不是“绝对防复制”，而是防止用户一次性拿到完整 PDF/SQLite，降低批量泄露和二次分发风险。

#### 4.12.4 SQLite 的保护方式

普通用户不下载 SQLite。所有题目查询都在服务端完成：

- 第一版 `questions`、`options`、`interaction_options`、`interaction_targets` 仍在服务端私有 `bank.sqlite` 中，API 只读查询。
- 后续多实例/高并发时，可以迁移到 SQL/Postgres，但 API 返回策略不变。
- API 按当前题或小分页返回。
- 服务端限制分页大小，例如最大 20/50。
- 禁止返回 `question_md` 全量原文，除非是作者/编辑权限。
- 普通用户只拿渲染 UI 需要的字段。
- 答案可以按模式控制：练习模式提交后返回，浏览模式有权限才返回。

如果未来 Tauri 客户端需要更流畅体验，只允许：

- 预取当前题之后 1-3 题。
- 缓存当前会话题目，不缓存整库。
- 本地缓存加密，密钥来自当前登录会话，退出后失效。
- 不提供“导出题库”“打开数据库目录”给普通用户版。

#### 4.12.5 防批量抓取策略

| 风险 | 缓解措施 |
|---|---|
| 用户脚本枚举题号 | 每题请求必须绑定授权题库、考试会话、用户 token；限制速率 |
| 批量翻页抓全库 | 分页上限、行为风控、每日访问题数阈值、异常告警 |
| 复制 PDF 页图 | 动态水印、短 TTL、按题授权、可选裁剪区域 |
| 泄露账号共享 | Entra 条件访问、设备合规、并发会话限制、登录风险检测 |
| 内部人员导出 | 管理员操作审计、导出审批、导出包水印、最小权限 |
| 抓 API 返回 JSON | 字段最小化、不要返回完整 Markdown、答案按需返回 |

#### 4.12.6 前端形态选择

在“保护作者核心数据”的约束下，推荐优先级变为：

1. **Web 前端优先**：最容易控制缓存、鉴权、访问路径；用户只通过浏览器/API 使用。
2. **Tauri 在线版第二**：可以做更好的桌面体验，但必须去掉完整题库目录、完整 SQLite 下载、完整 PDF 下载能力。
3. **Tauri 离线版仅内部使用**：保留给作者本人、审题人员、无网络演示等场景。

也就是说，未来可以有两个 Tauri build profile：

```text
TauriExam Author/Internal
- 允许打开本地 question-banks
- 允许读取完整 SQLite/PDF
- 用于制题、验证、内测

TauriExam Cloud/User
- 必须 AAD 登录
- 只调用 API
- 不显示题库目录
- 不下载完整 SQLite/PDF
- 本地只保存用户学习记录和短 TTL 缓存
```

#### 4.12.7 对架构结论的影响

加入作者数据保护约束后，最终推荐从“完整题库分发”进一步细化为：

```text
作者/后台：服务端私有 Volume 中的 SQLite/PDF/Markdown 全量资产；后续可升级对象存储/云数据库
普通用户：AAD 登录 + API 逐题访问 + 服务端 PDF 页图渲染
内部用户：可选受信任离线包，但必须单独授权、审计、水印
```

这比“下载题库包离线刷”更符合你的商业/作者保护目标。

### 4.13 后端技术选型建议

结合当前偏好与项目现状，建议优先：

#### 方案 1：ASP.NET Core / C# 后端

优点：

- AAD/Entra、MSAL、Microsoft.Identity.Web、Azure SDK 一等支持。
- EF Core + Azure SQL 成熟。
- 做 B/S 管理后台和 API 快。
- 容器化部署简单。

适合：快速做云端登录、权限、API、管理后台。

#### 方案 2：Rust 后端

优点：

- 与现有 `docintel-md`、Tauri 后端语言一致。
- 性能好、单二进制、小体积。
- 适合做 `exam-pipeline` CLI 和高性能导入器。

缺点：

- AAD/OIDC、Graph、Azure SQL 开发体验不如 C# 顺滑。

#### 推荐组合

```text
C# ASP.NET Core：云端 API / 登录 / 权限 / 管理后台
Rust：题库导入 CLI / PDF 处理 / SQLite 包构建 / Tauri 后端
```

这不是语言洁癖，而是按优势分工：C# 管企业身份和云 API，Rust 管本地工具和高性能处理。

## 5. 建议的下一步

### 5.1 立即可做：整理现有仓库

1. 明确 `question-banks/` 是当前最终题库主目录。
2. 将 `TauriExam/question-banks/` 标记为开发副本或删除同步依赖。
3. 根 README 更新为新版 skill 导入器路径。
4. `scripts/` 旧导入器加 `legacy` 标记或移动到 `scripts/legacy/`。
5. 给每个题库加 `manifest.json`，至少记录：exam、version、sqlite hash、pdf hash、题数、schema version。
6. 写一个统一 `validate-bank` 脚本/命令，检查：
   - `questions` 数量；
   - 选择题答案是否匹配 options；
   - 复杂题是否有 interaction options/targets；
   - `correct_option` 是否引用存在的 option；
   - PDF 文件是否存在；
   - 页码是否越界。

### 5.2 短期产品化：独立流水线 CLI

建议新建 `exam-pipeline`，优先用 Rust 或 C#，命令形态：

```text
exam-pipeline import-md --input output/vision-md --exam SC-100 --db question-banks/SC-100.sqlite --reset
exam-pipeline validate-bank --db question-banks/SC-100.sqlite --pdf question-banks/SC-100.pdf
exam-pipeline package-bank --db question-banks/SC-100.sqlite --pdf question-banks/SC-100.pdf --out artifacts/SC-100
exam-pipeline sync-appdata --bank artifacts/SC-100/manifest.json
```

这样先把 Python 导入器和散落校验逻辑收束起来。

### 5.3 中期：TauriExam 支持远程题库目录

TauriExam 增加：

- AAD 登录。
- 远程 API base URL 设置。
- 获取可访问题库列表。
- 普通用户版按题调用 API，不下载完整题库包到 `%LOCALAPPDATA%\TauriExam\question-banks`。
- 作者/内部版可保留本地 `question-banks`，但要与普通用户版明确区分。
- 本地/远程题库混合显示。
- 在线同步考试历史。

### 5.4 长期：B/S 云端题库平台

构建：

- Web 前端。
- API 后端。
- 第一版服务端私有 SQLite 题库包与 `app.sqlite`。
- 宿主机 Volume 保存 PDF、页图缓存、SQLite 制品。
- Rust worker 导入/翻译/打包/预渲染。
- Entra ID 权限和 ACL。
- 管理员发布流程。

## 6. 最终判断

1. 当前工作区有多个项目/模块，但不是互相冲突，而是围绕题库生产和考试客户端组成的一条工具链。
2. 当前最大问题不是代码冲突，而是目录职责和数据源边界不够清晰：旧 DB 路径、新 DB 路径、Tauri 开发副本、AppData 运行副本并存。
3. TauriExam + skill + ps1 在当前个人工作区里已经能跑通“生成/整理题库 → SQLite → 本地刷题”的闭环；但它还不是脱离 VS Code/Copilot skill 的独立产品流水线。
4. 最优演进方向是：保留 SQLite 作为作者/后台/服务端第一阶段主格式；普通用户默认不下发完整 SQLite/PDF；部署优先采用纯容器 + 宿主机 Volume + Rust API，后续再按规模升级到对象存储或云数据库；前端以 B/S 为主，Tauri 作为可登录在线受控客户端。
5. 不建议把完整 SQLite/PDF 直接给普通用户；第一阶段可让服务端私有持有 SQLite/PDF/翻译库，并通过 API 最小化返回当前题、当前交互、当前页图。
6. 如果目标是保护作者核心数据，普通用户模式必须是在线授权逐题访问；任何完整离线包都应只给作者或受信任内部用户，并配套审计、水印和单独授权。

## 7. 追加讨论：按当前偏好修正后的更贴近落地方案

### 7.1 `%LOCALAPPDATA%` 是写死的吗？是否会从旧位置自动复制？

当前 TauriExam 代码里的事实是：

1. `app_data_dir()` 优先使用 Windows 环境变量 `%LOCALAPPDATA%`，拼成：

   ```text
   %LOCALAPPDATA%\TauriExam
   ```

2. `question_banks_dir()` 固定是：

   ```text
   %LOCALAPPDATA%\TauriExam\question-banks
   ```

3. `ensure_app_dirs()` 会创建目录，并调用 `migrate_question_bank_files()`。

4. `migrate_question_bank_files(target)` 的行为是：
   - 如果目标目录 `%LOCALAPPDATA%\TauriExam\question-banks` 里已经有任意支持的 SQLite 文件，就**不再迁移**。
   - 如果目标目录为空，才从旧位置找第一个有题库文件的目录并复制进去。

5. 旧位置搜索顺序大致是：
   - exe 旁边的 `question-banks`
   - 当前工作目录的 `question-banks`
   - 工作区 `TauriExam/question-banks`
   - 工作区根 `question-banks`

所以结论是：

- `%LOCALAPPDATA%\TauriExam\question-banks` 是当前普通运行时优先读取位置。
- 它有一次性迁移/补种子逻辑，能支持“老板/用户第一次打开程序时，从安装包旁边或工作区旧位置复制题库”。
- 但它不是持续同步：一旦 `%LOCALAPPDATA%` 里已经有 SQLite，后续根目录 `question-banks` 更新，不会被 Tauri 自动覆盖。
- 当前 `batch-add-interactions.ps1` 的 `Sync-DbCopies` 才是手动同步更新到 `%LOCALAPPDATA%` 和 `TauriExam/question-banks` 的逻辑。

### 7.2 `question-banks` 谁更权威？根目录还是 TauriExam 子目录？

按当前工作流，应该明确：

```text
根目录 question-banks/              作者/构建阶段权威数据
TauriExam/question-banks/            开发副本/历史兼容/种子数据
%LOCALAPPDATA%/TauriExam/question-banks/  用户运行时副本
```

更准确的判断：

- **AI 和导入程序的权威输出**：根目录 `question-banks/<EXAM>.sqlite`。
- **TauriExam 实际 UI 优先读取**：`%LOCALAPPDATA%\TauriExam\question-banks`。
- **`TauriExam/question-banks`**：不应该再当权威源，只能是开发/打包种子。

因此后续建议：

1. 所有生成、导入、校验脚本默认写根目录 `question-banks/`。
2. 作者本机调试时显式执行同步到 `%LOCALAPPDATA%`。
3. Tauri 用户版未来不再读取完整本地题库，而是在线按题访问 API。

### 7.3 Python 硬编码的处理方案

本轮已经做了程序化修正：新增 `scripts/Resolve-Python.ps1`，并接入：

- `continue-sc100.ps1`
- `batch-add-interactions.ps1`
- `.github/skills/sc100-vision-md/scripts/continue-sc100-vision.ps1`

新逻辑：

1. 优先使用调用参数 `-Python`。
2. 再看环境变量 `DOCINTEL_PYTHON` / `PYTHON`。
3. 再检测 `python` / `python3` / `py -3` / 常见本机安装路径。
4. 找不到时，如果未传 `-NoInstallPython`，会尝试用 `winget install Python.Python.3.12 --silent` 静默安装。
5. 安装后再次检测，成功则返回新的 `python.exe` 路径。

这满足“纯检测非常快；缺失时由 PS 自动处理”的目标。

注意：这只是过渡方案。长期更好的是把 Markdown→SQLite 导入器迁到 Rust/C#，彻底减少 Python 运行时依赖。

### 7.4 旧脚本 deprecated 的处理

当前已经给根目录旧导入器加了废弃声明：

- `scripts/import_vision_md_to_sqlite.py`
- `scripts/import_exam_gen_to_sqlite.py`

并新增 `scripts/README.md` 指向新版导入器。

是否“改目录名 + 放进忽略文件”的判断：

- 如果这些旧脚本仍要保留给历史可追溯，建议不要直接 `.gitignore`，否则别人看不到为什么废弃。
- 更好的做法是后续把它们移动到 `scripts/legacy/`，并在 `scripts/legacy/README.md` 标明“只读历史参考，不再维护”。
- 如果只是你本机临时放旧脚本，可以把 `scripts/legacy-local/` 放进 `.gitignore`。

建议下一步：等 README 和调用路径完全改完，再统一把旧脚本移动到 `scripts/legacy/`。

### 7.5 不用 Azure SQL，改成纯容器方案是否可行？

可行，而且更贴近当前项目现状。更新后的优先方案可以是：

```text
docker compose
  ├─ exam-web        # Web 前端，React/Vite 静态资源或 SSR
  ├─ exam-api        # Rust API，负责鉴权、题目查询、判分、PDF 页图服务
  ├─ exam-worker     # Rust worker，负责导入、PDF 转图、翻译、缓存预热
  ├─ reverse-proxy   # Caddy/Nginx/Traefik，TLS 和路由
  └─ data volume     # 宿主机挂载目录，存 SQLite/PDF/JPG/用户数据
```

最小落地可以不用单独数据库容器，直接复用当前 SQLite：

```text
/data
  /banks
    /SC-100
      bank.sqlite              # 当前 TauriExam schema，服务端只读打开
      translations.sqlite      # 可选：一题一条或当前 segment 方式
      source.pdf               # 私有，不直接暴露
      /pages
        page-001.jpg           # 服务端私有基础页图缓存
  /app
    app.sqlite                 # 用户、ACL、考试记录、审计
  /cache
    /rendered-pages
    /watermarked-pages
```

这样可以最大化复用现有架构：

- 题库 DB 继续用当前 `questions/options/answer_areas/interaction_options/interaction_targets`。
- API 服务端用只读连接打开题库 SQLite。
- 用户记录和权限写到单独 `app.sqlite`。
- PDF 和 JPG 存宿主机 volume。
- 普通用户不拿文件，只通过 API 获取当前题和页图。

后续如果用户量变大，再考虑：

- 把 `app.sqlite` 换成 Postgres 容器。
- 题库仍保留 SQLite 只读包。
- 多实例部署时，把 `/data` 换成共享文件系统或对象存储。

### 7.6 “不要扩散 drag_options、hotspot_options 多套表”是什么意思？

这里不是说马上删除现有表，也不是说当前导入器做错了。

意思是：未来设计云端主模型时，不要让每种 UI 形态都变成一套独立主表。

当前历史上存在：

```text
drag_options / drag_slots
hotspot_options / hotspot_rows
interaction_options / interaction_targets
```

其中：

- `drag_*` 和 `hotspot_*` 是早期按题型拆出来的兼容表。
- `interaction_options` / `interaction_targets` 是现在更通用的抽象：候选项池 + 目标位。

未来建议：

```text
主模型：interaction_options / interaction_targets
兼容层：需要时生成 drag_* / hotspot_*，或者由 API 根据 interaction_kind 渲染成不同 UI
```

也就是说：

- 当前 SQLite 为了兼容 TauriExam，可以继续保留旧表。
- 新导入器继续可以 backfill 旧表。
- 但服务端设计、API 和新 UI 应该只把 `interaction_*` 当权威数据，避免以后 `drag_*` 改了、`hotspot_*` 改了、`interaction_*` 又改了，三套表不一致。

当前 TauriExam 的实际运行逻辑更准确地说是：

1. 新导入器会解析 `Interaction Options` / `Interaction Targets`，并写入 `interaction_options` / `interaction_targets`。
2. 为了兼容旧 UI/旧查询逻辑，新导入器也可能从 `interaction_*` 自动回填 `drag_options` / `drag_slots` 或 `hotspot_options` / `hotspot_rows`。
3. Tauri 后端 `get_interaction_model` 优先读取 `interaction_options` / `interaction_targets`。
4. 后端再根据 `questions.question_type` 以及 Markdown 中的 `- Interaction:` 判断表现形态：
  - `hotspot` / `dropdown_hotspot` → 返回 `kind = hotspot`，targets 映射成 rows，下拉选择渲染。
  - `drag` / `drop` / `drag_drop` / `ordered_list` → 返回 `kind = drag_drop`，targets 映射成 slots，拖拽/排序渲染。
5. 如果没有统一表，才 fallback 到旧的 `hotspot_*` 或 `drag_*` 表。

所以结论不是“UI 随便换皮”这么简单，而是：**数据抽象统一，题型语义仍保留；运行时用同一组候选项/目标位数据，按题型语义渲染成不同交互组件和判分流程。**

### 7.7 翻译是否可以“一题一条”？

可以。你这个判断合理。

之前按 segment 存，是为了最大化复用字段级缓存；但对考试题来说，一道题的内容大小可控，重翻一整题成本并不高。更贴近当前需求的设计可以是：

```sql
question_translations
- id
- bank_id
- question_id
- language
- source_hash
- provider
- model
- status
- content_json
- created_at
- updated_at
- unique(question_id, language, source_hash)
```

`content_json` 可以放：

```json
{
  "question_text": "...",
  "options": { "A": "...", "B": "..." },
  "answer_areas": [ ... ],
  "interaction_options": { "opt-a": "..." },
  "interaction_targets": { "target-1": "..." },
  "source_answer": "...",
  "recommended_answer": "...",
  "reasoning": "...",
  "notes": "..."
}
```

优点：

- 表更少。
- 一题缺翻译就整题重新发起任务。
- API 返回更简单。
- 前端合并翻译更简单。

保留 `source_hash` 的原因是：题目源内容变化后，旧翻译自动失效。

### 7.8 为什么用 Blob？能不能直接用容器宿主机或数据库？

三种都可以，但适用场景不同。

#### 宿主机 volume

适合你当前预期的“自己用容器命令安装”：

- 最简单。
- 没有云厂商绑定。
- PDF、SQLite、JPG 都是普通文件。
- 备份就是备份 `/data` 目录。

建议当前优先用这个。

#### 数据库存 BLOB

不建议作为 PDF/JPG 主存储：

- PDF 和页图会让数据库膨胀。
- 备份恢复变重。
- 静态文件读取、range、缓存都不如文件系统自然。
- SQLite 大 BLOB 并发访问也不舒服。

数据库适合存元数据、题目结构、用户记录、权限；大文件还是放文件系统/对象存储。

#### Blob / 对象存储

适合以后云化、多实例、自动备份、CDN、生命周期管理。

如果不想 Azure Blob，但又想容器化对象存储，可以用 MinIO。只是第一版没必要。

结论：

```text
第一版纯容器：宿主机 volume
第二版多实例：MinIO 或 S3-compatible object storage
云托管版：Azure Blob / S3 / OSS 等对象存储
不推荐：把 PDF/JPG 全塞数据库
```

### 7.9 当前 Tauri 架构是否适合改成 B/S？难度大吗？

适合，难度中等，不是推倒重来。

原因：TauriExam 前端本质已经是 React/Vite Web App；主要差异在 API 层：

当前：

```text
React UI -> @tauri-apps/api invoke -> Rust Tauri commands -> 本地 SQLite/PDF
```

未来 B/S：

```text
React UI -> fetch/HTTP API -> Rust Web API -> 服务端 SQLite/PDF/cache
```

可复用部分：

- 大部分 React 组件。
- 题目列表、题目面板、DragDrop/Hotspot 组件。
- 翻译/AI 面板的大部分 UI。
- `types.ts` 的大部分数据结构。

需要改的部分：

- `src/api.ts`：从 Tauri `invoke()` 改成 HTTP client。
- 登录：加 AAD/OIDC token 获取和请求头。
- PDF 图片：从 Tauri base64 本地渲染改成服务端图片 URL/数据流。
- 本地目录/打开文件夹功能：普通 Web 版不能有。
- 后端：把 `src-tauri/src/main.rs` 里的查询、判分、翻译、PDF 渲染逻辑抽成 Rust crate，再被 Tauri 和 Web API 共用。

推荐改造路径：

```text
第一步：把 Tauri 后端核心逻辑拆成 Rust library crate：exam_core
第二步：Tauri commands 调 exam_core
第三步：新增 Axum API server，也调 exam_core
第四步：React api.ts 增加两个 adapter：tauriAdapter / httpAdapter
第五步：构建 Web 版只使用 httpAdapter
```

这样 Tauri 和 B/S 可以长期共存。

### 7.10 PDF 转 JPG 应该只做一次，如何缓存？

你的判断正确：PDF 转 JPG 应该按题库版本和页码做服务端共享缓存。张三触发渲染后，李四应复用服务器缓存。

推荐设计：

```text
/data/banks/SC-100/pages/base/page-001.jpg        # 无水印基础图，服务端私有
/data/banks/SC-100/pages/base/page-002.jpg
/data/cache/watermark/<user>/<page>/<hash>.jpg    # 可选短 TTL 水印图
```

请求流程：

```text
GET /api/questions/{id}/source-pages?page=123
  1. 验证用户权限
  2. 根据 question_id 确认 page=123 是否属于该题
  3. 查 base page cache：page-123.jpg 是否存在
  4. 不存在才从 source.pdf 渲染一次并保存
  5. 对 base jpg 叠加动态水印
  6. 返回水印图
```

关键点：

- 基础页图按 `bank_version + page + scale` 缓存，所有用户共享。
- 水印图按 `user + page + time bucket` 可短期缓存，也可以每次动态叠加。
- 如果题库 PDF 版本更新，`bank_version` 变化，缓存自动隔离。
- 如果只允许看题目区域，可以进一步缓存裁剪图：`page-123-q045-crop.jpg`。

### 7.11 Rust 优先还是 C#？

按你的偏好和当前代码资产，建议 Rust 优先，C# 作为可选补充。

Rust-first 方案：

```text
exam_core      # Rust library：SQLite 查询、判分、schema、PDF 渲染、缓存策略
exam_api       # Rust Axum server：HTTP API、AAD JWT 校验、限流、文件响应
exam_worker    # Rust worker：导入、转图、翻译任务、缓存预热
TauriExam      # Tauri desktop，复用 exam_core 或调用 exam_api
React Web      # 复用当前 TauriExam 前端组件
```

C# 适合补充的地方：

- 如果后续 Entra ID、Microsoft Graph、企业权限、管理员后台变复杂，ASP.NET Core 会更省心。
- 但第一版纯容器和现有逻辑迁移，用 Rust 更统一。

更平衡的建议：

```text
第一阶段：Rust-only，快速把 Tauri 后端能力服务化
第二阶段：如果 AAD/Graph/管理后台复杂度上来，再考虑 C# 管身份和后台
第三阶段：Rust 继续负责题库处理、PDF、SQLite、缓存、worker
```

### 7.12 更新后的推荐最小落地路线

结合这轮讨论，最小产品化路线变成：

1. 根目录 `question-banks/` 继续作为作者权威题库输出。
2. 旧 Python 脚本 deprecated；短期 PowerShell 自动解析/安装 Python；中期迁移导入器到 Rust。
3. 新建 Rust `exam_core`，复用当前 SQLite schema。
4. 新建 Rust `exam_api` 容器，只读打开题库 SQLite，写用户/ACL 到 `app.sqlite`。
5. PDF 原文件放 `/data/banks/<bank>/source.pdf`，页图缓存放 `/data/banks/<bank>/pages/base`。
6. 普通用户只能通过 Web/Tauri Cloud 调 API，不下载完整 SQLite/PDF。
7. Tauri 前端抽象 API adapter，逐步支持 HTTP 模式。
8. 翻译改成“一题一条 JSON”会更简单，适合当前规模。

## 8. 配置落地清单

这一节专门记录“设计改变后，当前仓库和未来产品应该怎么配置”，避免脚本、文档、Tauri 和未来容器服务各说各话。

### 8.1 当前仓库内已经调整的脚本配置

新增统一 Python 解析入口：

```text
scripts/Resolve-Python.ps1
```

已经接入的脚本：

```text
continue-sc100.ps1
batch-add-interactions.ps1
.github/skills/sc100-vision-md/scripts/continue-sc100-vision.ps1
```

配置约定：

| 配置项 | 用途 | 优先级 |
|---|---|---:|
| `-Python <path>` | 手动指定 Python | 1 |
| `DOCINTEL_PYTHON` | 仓库级/用户级 Python 路径 | 2 |
| `PYTHON` | 通用 Python 路径 | 3 |
| `python` / `python3` / `py -3` | PATH 或 py launcher 自动发现 | 4 |
| `-NoInstallPython` | 禁止自动 winget 安装 | 开关 |

默认行为：找不到 Python 时，脚本会尝试 `winget install Python.Python.3.12 --silent`。这只是过渡配置，长期目标仍然是把关键导入器迁到 Rust。

### 8.2 题库目录配置约定

当前需要统一以下优先级：

```text
question-banks/                              # 作者/构建阶段权威输出
%LOCALAPPDATA%/TauriExam/question-banks/     # Tauri 本机运行时副本
TauriExam/question-banks/                    # 开发种子/历史兼容副本
```

建议脚本配置：

| 名称 | 建议默认值 | 说明 |
|---|---|---|
| `BANK_ROOT` | `question-banks` | 作者权威题库输出目录 |
| `APPDATA_BANK_ROOT` | `%LOCALAPPDATA%\TauriExam\question-banks` | Tauri 当前实际读取目录 |
| `TAURI_DEV_BANK_ROOT` | `TauriExam/question-banks` | 仅开发/打包种子，不作为权威源 |

后续如果继续保留同步脚本，应明确同步方向：

```text
question-banks/*.sqlite  ->  %LOCALAPPDATA%/TauriExam/question-banks/*.sqlite
question-banks/*.sqlite  ->  TauriExam/question-banks/*.sqlite（仅开发需要）
```

不要反向从 `%LOCALAPPDATA%` 覆盖根目录权威题库，除非脚本显式要求。

### 8.3 旧脚本配置和目录治理

当前已标记 deprecated：

```text
scripts/import_vision_md_to_sqlite.py
scripts/import_exam_gen_to_sqlite.py
```

当前配置约定：

- 新流程入口优先使用 `.github/skills/*/scripts/import_question_md_to_sqlite.py`。
- 根 `scripts/` 里的两个 Python 导入器仅作为历史兼容参考。
- 后续可移动到 `scripts/legacy/`，但不建议直接丢进 `.gitignore`，因为它们仍有历史说明价值。

建议新增忽略项仅用于本机临时废弃文件：

```text
scripts/legacy-local/
```

### 8.4 纯容器第一版配置

建议未来服务端产品使用如下环境变量，而不是写死路径：

| 环境变量 | 示例 | 说明 |
|---|---|---|
| `EXAM_DATA_DIR` | `/data` | 容器内总数据目录 |
| `EXAM_BANKS_DIR` | `/data/banks` | 题库私有资产目录 |
| `EXAM_APP_DB` | `/data/app/app.sqlite` | 用户、ACL、审计、考试记录库 |
| `EXAM_CACHE_DIR` | `/data/cache` | 页图、水印、临时任务缓存 |
| `EXAM_PUBLIC_BASE_URL` | `https://exam.example.com` | 对外 URL |
| `OIDC_ISSUER` | `https://login.microsoftonline.com/<tenant>/v2.0` | AAD/OIDC issuer |
| `OIDC_CLIENT_ID` | `<client-id>` | 前端/API 使用的应用 ID |
| `OIDC_AUDIENCE` | `api://exam-api` | API token audience |
| `AI_PROVIDER` | `none` / `azure-openai` / `openai-compatible` | AI 分析/翻译提供方 |
| `PDF_RENDER_SCALE` | `2.0` | PDF 页图基础渲染倍率 |
| `PAGE_CACHE_POLICY` | `shared-base-dynamic-watermark` | 页图缓存策略 |

推荐容器内目录：

```text
/data
  /app
    app.sqlite
  /banks
    /SC-100
      bank.sqlite
      translations.sqlite
      source.pdf
      manifest.json
      /pages
        /base
          page-001-scale2.jpg
  /cache
    /watermark
    /jobs
```

### 8.5 服务端题库 manifest 配置

每个题库版本建议有服务端私有 `manifest.json`：

```json
{
  "schemaVersion": 1,
  "examCode": "SC-100",
  "bankVersion": "2026.05.11.1",
  "bankSqlite": "bank.sqlite",
  "translationSqlite": "translations.sqlite",
  "sourcePdf": "source.pdf",
  "questionCount": 793,
  "pdfSha256": "...",
  "sqliteSha256": "...",
  "pageCache": {
    "scale": 2.0,
    "format": "jpg",
    "directory": "pages/base"
  },
  "access": {
    "downloadFullSqlite": false,
    "downloadFullPdf": false,
    "servePdfPagesViaApiOnly": true
  }
}
```

注意：普通用户 API 返回的 `client-manifest` 不能包含 `bankSqlite`、`sourcePdf`、完整路径、hash 或可推导下载地址，只能返回题库名、题数、版本、功能开关等安全元数据。

更重要的是：不能把“不返回路径”当作唯一保护手段。正确设计是**就算用户知道内部路径、文件名、题库版本和存储规则，也无法直接下载完整 PDF/SQLite**。需要同时满足：

1. 私有资产目录不挂到 Web 静态站点。
2. 反向代理不配置任何完整文件下载 location。
3. API 没有普通用户可调用的 `download full bank/pdf/sqlite` 能力。
4. 每次页图请求都必须经过 API 鉴权、ACL 校验、题目范围校验。
5. 返回的是当前题允许查看的页图/裁剪图，而不是源 PDF。
6. 如果使用签名 URL，只能签单页图/裁剪图，短 TTL，并且不能签完整源文件。

换句话说，`client-manifest` 不暴露路径只是减少信息泄露；真正的保护来自服务端访问控制和存储层私有化。

### 8.6 Tauri 双模式配置

未来 TauriExam 应该通过配置区分两种模式：

| 模式 | 配置值 | 行为 |
|---|---|---|
| 作者/内部模式 | `EXAM_CLIENT_MODE=local-author` | 允许读取本地完整 SQLite/PDF |
| 普通用户在线模式 | `EXAM_CLIENT_MODE=cloud-user` | 必须登录，只调用 API，不读取完整题库目录 |

建议前端 API adapter 配置：

```text
local-author: React -> Tauri invoke -> 本地 Rust command -> 本地 SQLite/PDF
cloud-user:   React -> HTTP fetch  -> Rust exam-api     -> 服务端 SQLite/PDF/cache
```

这样做可以避免“同一个客户端既能登录云端，又偷偷打开完整本地题库”的边界不清问题。

### 8.7 PDF 页图缓存配置

推荐缓存 key：

```text
bankVersion + pdfSha256 + pageNumber + scale + cropKey + renderEngineVersion
```

推荐策略：

- `pages/base` 保存无水印基础图，所有用户共享，服务端私有。
- API 返回前叠加用户水印。
- 水印图可短 TTL 缓存到 `/data/cache/watermark`。
- PDF 更新后 hash 或版本变化，自动使用新缓存目录。

这保证：PDF 转 JPG 只在服务器上做一次；张三触发渲染后，李四复用基础缓存，但仍拿到带自己水印的响应。
