# 题库梳理流程

本文档面向后续 AI、skill、agent 或自动化脚本使用，说明本项目中“考试 PDF 题库 → 标准 Markdown → SQLite 题库”的整体流程、产物约定和质量边界。重点不是复盘历史操作，而是让 AI 能理解当前工作流，并在后续扩展时保持一致。

## 目标

把图文混排、截图题较多的考试题库 PDF，整理成可审阅、可入库、可做前端练习系统的数据。

最终链路是：

```text
原始 PDF
  -> AI 视觉读题
  -> 标准 Markdown 批次
  -> SQLite 入库
  -> 前端 / API / 复核工具读取
```

核心原则：**Markdown 是质量闸口，SQLite 是应用消费层。**

AI 不应直接从 PDF 一步生成最终数据库，也不应把 OCR 结果当成绝对真相。先生成结构清晰、人工可读的 Markdown，再由脚本稳定入库，是当前流程的关键设计。

## 为什么不用纯 OCR 或纯机械解析

SC-100 这类题库通常包含：

- HOTSPOT
- DRAG DROP
- Case Study
- 表格选择
- 截图题
- 老题库答案和当前 Microsoft 产品命名不一致的情况

Azure Document Intelligence / OCR 可以帮助转录文字，但对题目边界、截图区域、答案区表格和跨页 carryover 并不总是可靠。因此当前流程以 **AI 视觉判断** 为主，OCR 文本只作为辅助。

机械解析可以用于早期切分和探索，但不作为最终可信题库来源。

## 分层产物

### 1. 原始 PDF

原始 PDF 是唯一事实来源。后续每题都必须能追溯到 PDF 页码范围。

关键要求：

- 不修改原始 PDF。
- 每道题保留 `Source pages`。
- SQLite 中保留 `page_from` / `page_to`，方便前端打开原文页或预渲染图片。

### 2. 视觉 Markdown

目录：

```text
output/vision-md/
```

这是最重要的中间层。每个文件一般包含 10 道完整题，最后一个批次可以少于 10 道。

命名约定：

```text
output/vision-md/sc-100-pages-<from>-<to>/sc-100-questions-<start>-<end>.md
```

每道题必须包含：

- `Source pages`
- `Topic`
- `Type`
- `Status`
- `Question`
- `Options` 或 `Answer Area`
- `Source Answer`
- `My Recommended Answer`
- `我的判断（中文）`
- `Reasoning`
- `Notes`

AI 生成 Markdown 时要保留源答案，同时给出自己的推荐答案。两者不一致时，不要强行覆盖，应在中文判断和备注中说明原因。

### 3. SQLite 数据库

默认数据库：

```text
output/vision-db/sc-100.sqlite
```

入库脚本：

```text
scripts/import_vision_md_to_sqlite.py
```

默认入库命令：

```powershell
C:/Users/kukisama/AppData/Local/Programs/Python/Python312/python.exe scripts/import_vision_md_to_sqlite.py --reset
```

SQLite 是前端和 API 的读取层。它不取代 Markdown，而是把 Markdown 中已经稳定的结构转成可查询字段。

主要表：

- `markdown_batches`：批次文件、页码范围、题号范围、carryover、批次题数。
- `questions`：题目主表，包含题号、页码、类型、状态、题干、答案、判断、推理、原始 Markdown。
- `options`：普通选择题选项。
- `answer_areas`：HOTSPOT / DRAG DROP / 表格答案区。

## AI 处理规则

### 视觉优先

AI 处理题库时，应优先查看渲染后的 PDF 页面图片。文本提取结果只能作为转录辅助，不能替代视觉检查。

尤其是以下题型，必须按视觉内容判断：

- HOTSPOT
- DRAG DROP
- 截图题
- 表格题
- 跨页题
- Case Study 场景题

### 不盲信源答案

题库自带答案要保留在 `Source Answer`，但不默认视为正确。

AI 必须另外写 `My Recommended Answer`，并用中文说明判断依据。

如果答案可能受产品改名或功能变化影响，应标记：

```text
version_sensitive
```

如果答案区复杂、截图不清、选项歧义或需要人工确认，应标记：

```text
needs_review
```

### Carryover 必须显式记录

PDF 批处理时，某一页可能只包含题目的一半、答案的一半或评论的一半。此时不能硬切。

要求：

- 当前批次只写完整题。
- 半截题或未读完内容写入 carryover。
- 下一批开始前必须先处理 carryover。
- 不看到明确的 `Question #...` 边界，不应创建新题。

### Markdown 是人工审阅层

Markdown 应该适合人直接打开检查。

不要为了入库方便而牺牲可读性。复杂题型可以用 Markdown 表格表达，尤其是 HOTSPOT / DRAG DROP。入库脚本会从标准 Markdown 中提取结构化字段。

## 自动继续处理

当前项目有一个专门的 skill：

```text
.github/skills/sc100-vision-md/SKILL.md
```

当用户说“继续处理”“下一批”“接着做”等时，AI 应优先使用该 skill。

自动检测脚本：

```text
.github/skills/sc100-vision-md/scripts/detect_next_batch.py
```

它会读取现有 `output/vision-md`，判断：

- 是否已经全部完成。
- 最新 Markdown 文件是哪一个。
- 最新页码范围和题号范围。
- 下一批建议页码。
- 是否存在 carryover。

如果检测结果是 `done: true`，AI 不应继续渲染、读取或改写文件，应直接报告已完成。

根目录还有一个简化的 Copilot CLI 循环：

```text
continue-sc100.ps1
```

它的作用只是重复调用 Copilot；真正的流程控制仍应由 skill 和检测脚本负责。

## 入库和验证标准

Markdown 批次完成后，运行入库脚本生成 SQLite。

入库后至少验证：

- Markdown 题目标题数是否等于数据库题目数。
- `sequence_number` 是否连续。
- 是否有重复题号。
- 是否有缺失题干。
- 是否有缺失页码。
- 是否有缺失源答案。
- 是否有缺失推荐答案。
- 是否有缺失中文判断。
- HOTSPOT / DRAG DROP 是否有 `answer_areas` 记录。
- 普通选择题是否有 `options` 记录。

当前已验证过的 SC-100 数据状态：

- Markdown 批次：32 个。
- 题目总数：314 道。
- 题号范围：1–314。
- 序号连续且无重复。
- 题干、页码、源答案、推荐答案、中文判断均无缺失。

## 前端读取建议

前端或 API 应以 SQLite 为读取来源，不建议直接解析 Markdown。

推荐最小功能：

- 题目列表。
- 题目详情。
- 按题型筛选。
- 按状态筛选。
- 显示源答案和推荐答案。
- 显示中文判断和推理。
- 打开原始 PDF 页或预渲染页面图片。
- 标记复核状态。

题目详情页应优先展示结构化字段；如果结构化显示不够完整，可以回退展示 `question_md` 原文。

## 状态字段含义

常见状态：

- `parsed`：结构清楚，答案相对明确。
- `needs_review`：需要人工或后续 AI 复核。
- `version_sensitive`：可能受 Microsoft 产品演进、命名变化或功能变化影响。
- `carryover`：存在跨页或跨批次上下文。

这些状态不是错误，而是后续复核和前端筛选的重要依据。

## 后续 skill 精细化方向

后续如果把流程拆成多个 skill 或 agent，建议按职责拆分，而不是让一个 skill 做所有事：

- PDF 渲染与页码检测。
- 视觉读题与 Markdown 生成。
- Markdown 质量检查。
- SQLite 入库。
- 数据库完整性验证。
- 前端/API 生成。
- `needs_review` 专项复核。

各 skill 之间最好通过文件产物交接，而不是通过对话记忆交接。推荐交接边界是：

```text
PDF/page images -> vision Markdown -> SQLite -> API/frontend
```

## 最重要的约束

1. 不要跳过 Markdown 质量闸口。
2. 不要直接信任源答案。
3. 不要丢失原始 PDF 页码。
4. 不要把复杂题型硬塞成普通 A/B/C/D。
5. 不要在 `done: true` 后继续处理。
6. 不要让前端重新解析 Markdown；前端应读 SQLite。

遵守这些约束后，整个题库流程就是稳定的：AI 负责视觉理解和判断，Markdown 负责人工可审阅，SQLite 负责应用消费，前端负责练习和复核体验。