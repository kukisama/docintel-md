# 密钥登录说明

这份文档解决两个问题：

1. **如何在当前“只有密码可登录”的前提下，为 Azure Linux 服务器补上 SSH 密钥登录**
2. **如何做到：VS Code 用密钥，其他 SSH 客户端继续用密码**

当前约定：

- 服务器 IP：`40.90.172.14`
- SSH 端口：`65444`
- 登录用户：`kukisama`
- 当前可用方式：**账号密码登录**
- 目标方式：
  - **VS Code Remote SSH：使用密钥**
  - **其他 SSH 客户端：继续使用密码**

---

## 一句话理解

不是去服务器上“找一个 key”，而是：

1. 在 **Windows 本机** 生成一对 SSH 密钥
2. 用你现在已有的**密码登录**先连上服务器
3. 把**公钥**放到服务器的 `~/.ssh/authorized_keys`
4. 以后 VS Code 用密钥登录，其他客户端继续密码登录

---

## 第一步：在 Windows 本机生成密钥

在 PowerShell 中执行：

```powershell
ssh-keygen -t ed25519 -C "kukisama-vscode"
```

### 生成过程中怎么选

#### 1）保存位置

如果提示：

```text
Enter file in which to save the key
```

直接回车即可。

默认一般会保存到：

```text
C:\Users\kukisama\.ssh\id_ed25519
C:\Users\kukisama\.ssh\id_ed25519.pub
```

#### 2）passphrase 要不要设

- 想省事：直接回车，不设
- 想更安全：设一个 passphrase

当前你是开发机场景，如果优先考虑顺手，**不设也可以**。

### 生成结果

会得到两个文件：

- `id_ed25519`：私钥，只留在你本机
- `id_ed25519.pub`：公钥，要放到服务器上

**私钥不要发给任何人，也不要上传到服务器。**

---

## 第二步：继续用密码方式登录服务器

你现在已经能用密码登录，所以先这样连：

```powershell
ssh -p 65444 kukisama@40.90.172.14
```

输入密码后进入服务器。

这一步的作用是：

- 用旧方式先进去
- 再把新的公钥安装进去

---

## 第三步：把本机公钥复制出来

在 Windows PowerShell 执行：

```powershell
Get-Content $env:USERPROFILE\.ssh\id_ed25519.pub
```

你会看到一整行内容，类似：

```text
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAI...... kukisama-vscode
```

复制**整整一行**，不要断行，不要少空格。

如果你只是想复制到剪贴板，也可以：

```powershell
Get-Content $env:USERPROFILE\.ssh\id_ed25519.pub | Set-Clipboard
```

---

## 第四步：把公钥安装到服务器

在你已经登录进去的远程 Linux 终端里执行：

```bash
mkdir -p ~/.ssh
chmod 700 ~/.ssh
nano ~/.ssh/authorized_keys
```

如果系统没有 `nano`，也可以用：

```bash
vi ~/.ssh/authorized_keys
```

然后把刚才复制的那一整行公钥粘贴进去，保存退出。

接着执行：

```bash
chmod 600 ~/.ssh/authorized_keys
```

这样服务器就认识你的公钥了。

---

## 第五步：确认服务器同时允许密码和密钥

你的目标不是“只用密钥”，而是：

- **VS Code 用密钥**
- **其他客户端继续密码**

所以服务器端要同时允许这两种认证。

在远程服务器执行：

```bash
sudo grep -R "PubkeyAuthentication\|PasswordAuthentication\|UsePAM" /etc/ssh/sshd_config /etc/ssh/sshd_config.d 2>/dev/null
```

理想状态是看到类似：

```text
PubkeyAuthentication yes
PasswordAuthentication yes
UsePAM yes
```

如果没有，可以创建一个配置文件：

```bash
sudo sh -c 'cat > /etc/ssh/sshd_config.d/50-auth.conf <<EOF
PubkeyAuthentication yes
PasswordAuthentication yes
UsePAM yes
EOF'
```

然后重启 SSH：

```bash
sudo systemctl restart ssh
```

---

## 第六步：测试密钥登录是否成功

先退出当前远程会话：

```bash
exit
```

回到 Windows PowerShell 后执行：

```powershell
ssh -i $env:USERPROFILE\.ssh\id_ed25519 -p 65444 kukisama@40.90.172.14
```

如果能直接登录进去，说明密钥登录配置已经成功。

---

## 第七步：给 VS Code 和密码登录分别配两个 Host

这是最稳的方案。

编辑本机文件：

```text
C:\Users\kukisama\.ssh\config
```

写成下面这样：

```ssh-config
Host azure-devbox-key
    HostName 40.90.172.14
    User kukisama
    Port 65444
    IdentityFile C:\Users\kukisama\.ssh\id_ed25519
    IdentitiesOnly yes
    PreferredAuthentications publickey
    ServerAliveInterval 30
    ServerAliveCountMax 6

Host azure-devbox-password
    HostName 40.90.172.14
    User kukisama
    Port 65444
    PreferredAuthentications password
    PubkeyAuthentication no
    ServerAliveInterval 30
    ServerAliveCountMax 6
```

### 以后怎么用

#### 在 VS Code 里

只连接：

```text
azure-devbox-key
```

这样 VS Code 就固定走密钥。

#### 在 PowerShell 里如果你还想继续用密码

```powershell
ssh azure-devbox-password
```

#### 在其他独立 SSH 客户端里

如果它们不读取 `~/.ssh/config`，就在它们自己的连接配置里继续填：

- Host：`40.90.172.14`
- Port：`65444`
- User：`kukisama`
- Auth：`Password`

这样也完全没问题。

---

## 最推荐的实际顺序

按这个顺序做最稳：

1. 在本机执行：

```powershell
ssh-keygen -t ed25519 -C "kukisama-vscode"
```

2. 用密码登录服务器：

```powershell
ssh -p 65444 kukisama@40.90.172.14
```

3. 把公钥粘贴到远程：

```bash
~/.ssh/authorized_keys
```

4. 检查服务端同时允许：

- `PasswordAuthentication yes`
- `PubkeyAuthentication yes`

5. 本机测试密钥登录：

```powershell
ssh -i $env:USERPROFILE\.ssh\id_ed25519 -p 65444 kukisama@40.90.172.14
```

6. 最后把 `~/.ssh/config` 改成双入口

---

## 常见错误

### 错误 1：把私钥传到了服务器

不应该这样做。

正确做法：

- 私钥只放你本机
- 服务器只保存公钥

---

### 错误 2：`authorized_keys` 权限不对

如果权限不对，SSH 可能会忽略这个文件。

正确权限：

```bash
chmod 700 ~/.ssh
chmod 600 ~/.ssh/authorized_keys
```

---

### 错误 3：服务端只开了密码，没开公钥

如果服务端没有：

```text
PubkeyAuthentication yes
```

那密钥登录不会生效。

---

### 错误 4：服务端只开了公钥，关了密码

如果你把：

```text
PasswordAuthentication no
```

写死了，那么你别的客户端就不能继续用密码。

这和你的目标相反。

---

## 在这个远程开发场景下，如何维护文件夹/目录

这部分很重要。

### 先记住一条原则

**Remote SSH 模式下，远程机器才是“真正干活的地方”。**

也就是说：

- 代码文件：放在远程 Linux 机器上
- 编译：在远程机上进行
- 容器：在远程机上运行
- Git：通常也在远程机上执行

所以目录维护的思路是：

> **把远程服务器当成你的开发工作机来管理目录，不要把它当一个临时跳板。**

---

## 推荐目录结构

建议把项目统一放到：

```text
/workspace
```

例如：

```text
/workspace/docintel-md
/workspace/other-project
/workspace/scratch
```

### 为什么推荐 `/workspace`

因为：

- 跟系统目录分开
- 跟用户家目录里的配置分开
- 适合长期做开发工作区
- 以后做备份、迁移、挂载也更清楚

---

## 哪些目录别乱动

在远程服务器上，下面这些目录一般不要拿来放项目代码：

- `~/.ssh`
- `~/.vscode-server`
- `~/.cache`
- `~/.config`
- `/etc`
- `/usr`
- `/var`（除非你明确知道自己在做什么）

这些目录更适合：

- SSH 配置
- VS Code Server
- 程序缓存
- 系统配置

不是拿来当项目工作区的。

---

## 日常怎么维护目录

### 方式 1：直接用 VS Code 资源管理器

这是最省心的方式。

连上 Remote SSH 后：

- 打开 `/workspace`
- 在资源管理器里新建文件夹
- 拖动文件
- 重命名目录
- 删除无用目录

这个体验最接近图形界面文件管理。

### 方式 2：在远程终端里用命令

如果是批量操作，命令更快：

```bash
mkdir -p /workspace/docintel-md
mkdir -p /workspace/scratch
mv /workspace/old-name /workspace/new-name
rm -rf /workspace/temp-dir
```

适合：

- 批量建目录
- 重命名项目目录
- 清理临时目录

---

## 推荐的项目管理方式

### 一个项目一个目录

例如：

```text
/workspace/docintel-md
/workspace/my-api
/workspace/demo-app
```

不要把多个项目混在一个目录里到处堆文件。

### 临时实验单独放一个目录

例如：

```text
/workspace/scratch
```

用来放：

- 临时脚本
- 调试文件
- 实验代码

这样主项目不会变成垃圾场。

---

## Git 仓库放哪里

建议直接 clone 到 `/workspace` 下：

```bash
cd /workspace
git clone <repo-url>
```

例如：

```text
/workspace/docintel-md
```

以后你在 VS Code 里也直接打开这个目录。

---

## 编译产物和容器怎么理解

### 编译产物

像 Rust/Node/Tauri 这些项目的编译产物，通常留在项目目录内部即可，例如：

- `target/`
- `node_modules/`
- `dist/`

不需要为了“整洁”去手动挪来挪去。

### 容器

如果你在远程机跑 Docker：

- `Dockerfile`
- `docker-compose.yml`
- 项目源码

都放在项目目录下即可。

如果需要持久化数据，再单独规划卷或数据目录。

---

## 最短版目录维护建议

如果你只记 5 条，就记这 5 条：

1. 项目统一放 `/workspace`
2. 一个项目一个目录
3. 系统目录和隐藏配置目录别乱放项目
4. 日常目录维护优先用 VS Code 资源管理器
5. 批量操作再用远程终端命令

---

## 最短版结论

### 密钥登录这件事

不是去服务器上拿 key，而是：

- 本机生成 key
- 用密码先连上服务器
- 把公钥装到服务器上
- 让 VS Code 用密钥，其他客户端继续密码

### 目录维护这件事

把远程 Linux 机当成真正的开发机来管理。

推荐统一使用：

```text
/workspace
```

作为你的项目根目录。