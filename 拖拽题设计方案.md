# TauriExam 拖拽题设计方案：MD 生成、SQL 导入、UI 作答闭环

日期：2026-05-10  
范围：drag_drop 题型从 Markdown 生成、SQLite 导入到 TauriExam UI 真正作答、自动判分、历史回放的完整方案。

## 1. 目标

拖拽题必须成为一个完整可用功能，而不是只展示答案区。

完整闭环包括：

1. Markdown 阶段明确生成拖拽题结构。
2. SQL 导入阶段写入专用结构表。
3. Tauri 后端读取结构化交互模型。
4. React UI 支持真实作答。
5. 提交时自动判分。
6. 历史记录可回放用户答案。

## 2. 当前状态判断

当前项目已有一部分基础：

- `QuestionDetail.answer_areas` 可展示答案区。
- `get_interaction_model()` 已经预留：
  - `drag_options`
  - `drag_slots`
  - `InteractionModel.kind = "drag_drop"`
- 前端 `InteractionPanel` 已能展示候选项池和槽位。

但当前还不是正式可用的拖拽题：

- 没有真实拖拽或点击填槽交互。
- 没有用户填槽状态。
- 没有拖拽题提交答案格式。
- 没有自动判分接入考试流程。
- 导入脚本没有创建/写入 `drag_options`、`drag_slots`。
- Markdown 约束没有区分候选项、槽位、正确映射、干扰项。

## 3. 总体链路

```text
AI/人工视觉读题
  ↓
Markdown 写入 drag_drop 结构
  ↓
import_vision_md_to_sqlite.py / import_exam_gen_to_sqlite.py 解析
  ↓
SQLite:
  questions
  answer_areas
  drag_options
  drag_slots
  ↓
Tauri command: get_interaction_model
  ↓
React DragDropQuestion UI
  ↓
提交 JSON user_answer
  ↓
前端或后端 compare slot_id → option_id
  ↓
exam_answers 保存 is_correct + user_answer
```

## 4. 第一层：Markdown 生成约束

### 4.1 基本题型元数据

每道拖拽题必须明确：

```markdown
- Type: drag_drop
```

其他字段保持现有模板：

```markdown
- Exam: SC-100
- Topic: Identity and Access
- Type: drag_drop
- Status: parsed | needs_review
```

### 4.2 推荐 Markdown 结构

拖拽题必须包含三个区域：

1. `### Question`
2. `### Drag Options`
3. `### Drag Slots`
4. `### Correct Answer`

推荐格式：

```markdown
## Question 128

- Exam: SC-100
- Topic: Identity and Access
- Type: drag_drop
- Status: parsed

### Question

Drag the appropriate Microsoft Entra feature to each requirement.

### Drag Options

| Option ID | Text | Group | Distractor |
|---|---|---|---|
| opt-a | Access package | identity | No |
| opt-b | Conditional Access policy | identity | No |
| opt-c | Privileged Identity Management | identity | No |
| opt-d | Administrative unit | identity | Yes |

### Drag Slots

| Slot ID | Label | Correct Option ID |
|---|---|---|
| slot-1 | Require manager approval before users can access SaaS apps | opt-a |
| slot-2 | Require MFA when sign-in risk is high | opt-b |
| slot-3 | Just-in-time activation for Global Administrator | opt-c |

### Correct Answer

slot-1 = opt-a
slot-2 = opt-b
slot-3 = opt-c

### 解析（中文）

...
```

### 4.3 兼容旧格式

旧格式可能只有：

```markdown
### Answer Area

| 提示/位置 | 正确选择 |
|---|---|
| requirement 1 | Access package |
| requirement 2 | Conditional Access policy |
```

兼容策略：

- 如果没有 `Drag Options` / `Drag Slots`，导入脚本仍然写 `answer_areas`。
- 该题在 UI 中降级为人工自评。
- 不自动生成 `drag_options` / `drag_slots`，除非能无歧义解析。

### 4.4 AI 生成题时的约束

给 AI 或 skill 的约束必须明确：

1. Option ID 必须稳定且唯一，格式建议 `opt-a`、`opt-b`。
2. Slot ID 必须稳定且唯一，格式建议 `slot-1`、`slot-2`。
3. `Correct Option ID` 必须引用 `Drag Options` 中存在的 Option ID。
4. 每个非干扰选项最多使用一次，除非题干明确允许重复使用。
5. 干扰项必须标记 `Distractor = Yes`。
6. 不允许只写自然语言答案，必须写结构化表。
7. Correct Answer 必须和 Drag Slots 表一致。

### 4.5 sc100-vision-md skill 建议补充

在 `.github/skills/sc100-vision-md/SKILL.md` 的 HOTSPOT/DRAG DROP 规则中补充：

- 对 DRAG DROP 题，必须优先输出 `Drag Options` 与 `Drag Slots` 表。
- 如果视觉页只给答案文字，也要把候选项池和槽位从页面视觉区域整理出来。
- 如果候选项或槽位无法完整确认，`Status` 标记为 `needs_review`，不要伪造结构。
- 原始页面中的拖拽候选项顺序写入 `sort_order`。

### 4.6 exam-question-gen skill 建议补充

在 `.github/skills/exam-question-gen/SKILL.md` 的非标准题型模板中补充：

- `drag_drop` 必须输出 `Drag Options` 和 `Drag Slots`。
- `Answer Area` 可选保留给人类阅读，但 SQL 导入以 `Drag Options` / `Drag Slots` 为准。

## 5. 第二层：SQL 导入要求

### 5.1 新增 SQLite 表

在题库 SQLite 中新增两张表。

```sql
CREATE TABLE IF NOT EXISTS drag_options (
  id TEXT PRIMARY KEY,
  question_id TEXT NOT NULL,
  option_text TEXT NOT NULL,
  option_group TEXT,
  is_distractor INTEGER NOT NULL DEFAULT 0,
  sort_order INTEGER NOT NULL,
  FOREIGN KEY(question_id) REFERENCES questions(id)
);

CREATE TABLE IF NOT EXISTS drag_slots (
  id TEXT PRIMARY KEY,
  question_id TEXT NOT NULL,
  slot_label TEXT NOT NULL,
  correct_option TEXT NOT NULL,
  sort_order INTEGER NOT NULL,
  FOREIGN KEY(question_id) REFERENCES questions(id)
);

CREATE INDEX IF NOT EXISTS idx_drag_options_question ON drag_options(question_id);
CREATE INDEX IF NOT EXISTS idx_drag_slots_question ON drag_slots(question_id);
```

说明：

- `drag_options.id` 对应 Markdown `Option ID`。
- `drag_slots.id` 对应 Markdown `Slot ID`。
- `drag_slots.correct_option` 存 `Option ID`，不是显示文本。
- 题库 SQLite 是只读内容库，用户答案不写这里。

### 5.2 导入脚本解析章节

两个脚本都需要支持：

- `### Drag Options`
- `### Drag Slots`

涉及文件：

- `scripts/import_vision_md_to_sqlite.py`
- `scripts/import_exam_gen_to_sqlite.py`

建议新增解析函数：

```python
def parse_markdown_table(section_md: str) -> list[dict[str, str]]:
    ...

def parse_drag_options(section_md: str) -> list[DragOption]:
    ...

def parse_drag_slots(section_md: str) -> list[DragSlot]:
    ...
```

注意：现有脚本已经有 `TABLE_ROW_RE`，但它只是简化解析，后续要更严格处理表头。

### 5.3 Python dataclass 建议

```python
@dataclass
class DragOption:
    id: str
    text: str
    group: str | None
    is_distractor: bool
    sort_order: int

@dataclass
class DragSlot:
    id: str
    label: str
    correct_option: str
    sort_order: int
```

`ParsedQuestion` 增加：

```python
drag_options: list[DragOption]
drag_slots: list[DragSlot]
```

### 5.4 导入时校验

导入脚本必须校验：

1. `question_type == "drag_drop"` 时，如果存在 Drag Options/Slots，则写入结构表。
2. 所有 `slot.correct_option` 必须能在 `drag_options.id` 中找到。
3. `Option ID` 不允许重复。
4. `Slot ID` 不允许重复。
5. `Correct Answer` 与 `Drag Slots` 不一致时，打印 warning；以 `Drag Slots` 为准。
6. 若结构不完整：
   - 不写 drag 表。
   - 仍写 `answer_areas`。
   - `questions.status` 可保持 `needs_review`。

### 5.5 导入写入逻辑

导入每题前先清理旧结构：

```python
conn.execute("DELETE FROM drag_options WHERE question_id = ?", (question.id,))
conn.execute("DELETE FROM drag_slots WHERE question_id = ?", (question.id,))
```

再插入：

```python
for option in question.drag_options:
    conn.execute(
        """
        INSERT INTO drag_options
        (id, question_id, option_text, option_group, is_distractor, sort_order)
        VALUES (?, ?, ?, ?, ?, ?)
        """,
        (option.id, question.id, option.text, option.group, int(option.is_distractor), option.sort_order),
    )

for slot in question.drag_slots:
    conn.execute(
        """
        INSERT INTO drag_slots
        (id, question_id, slot_label, correct_option, sort_order)
        VALUES (?, ?, ?, ?, ?)
        """,
        (slot.id, question.id, slot.label, slot.correct_option, slot.sort_order),
    )
```

### 5.6 和现有后端的契合

当前 `TauriExam/src-tauri/src/main.rs` 已有：

```rust
if normalized.contains("drag") && table_exists(&conn, "drag_slots") {
    let slots = query_drag_slots(&conn, &question.id)?;
    return Ok(InteractionModel {
        kind: "drag_drop".to_string(),
        can_auto_grade: true,
        message: "检测到 drag_slots，已启用结构化 Drag Drop 框架。".to_string(),
        options: query_interaction_options(&conn, "drag_options", &question.id)?,
        answer_key: slots.iter().filter_map(|slot| slot.correct_option.clone()).collect(),
        rows: Vec::new(),
        slots,
    });
}
```

所以导入脚本只要正确创建并写入 `drag_options` / `drag_slots`，后端模型即可读取。

但需要注意：当前 `table_exists("drag_slots")` 只检查表存在，不检查当前题是否有 slots。建议改成：

```rust
let slots = query_drag_slots(&conn, &question.id)?;
if normalized.contains("drag") && !slots.is_empty() {
    ...
}
```

避免表存在但某题无结构时误判。

## 6. 第三层：UI 拖拽真实可用

### 6.1 交互原则

第一版建议同时支持两种操作：

1. 点击式填槽。
2. 鼠标拖拽填槽。

原因：

- 点击式稳定，适合触控板和桌面端。
- 拖拽体验更接近真实题型。
- 两者共享同一份状态。

### 6.2 状态结构

前端新增类型：

```ts
export type DragDropUserAnswer = {
  kind: 'drag_drop';
  slots: Array<{
    slot_id: string;
    option_id: string | null;
  }>;
};
```

考试状态 `ExamDraft` 当前只有：

```ts
selected: string[];
```

建议扩展：

```ts
selected: string[];
interactionAnswer: DragDropUserAnswer | HotspotUserAnswer | null;
```

或更简单：

```ts
interactionAnswers: Record<string, unknown>;
```

推荐第一版明确类型，不要过早泛化。

### 6.3 新增组件

新增文件建议：

```text
TauriExam/src/DragDropQuestion.tsx
```

Props：

```ts
type DragDropQuestionProps = {
  model: InteractionModel;
  value: DragDropUserAnswer;
  showAnswer?: boolean;
  disabled?: boolean;
  onChange: (value: DragDropUserAnswer) => void;
};
```

### 6.4 初始化答案

根据 slots 初始化：

```ts
function createEmptyDragAnswer(model: InteractionModel): DragDropUserAnswer {
  return {
    kind: 'drag_drop',
    slots: model.slots.map(slot => ({ slot_id: slot.id, option_id: null })),
  };
}
```

### 6.5 点击式填槽

状态：

```ts
const [pickedOptionId, setPickedOptionId] = useState<string | null>(null);
```

规则：

- 点击候选项：选中该 option。
- 点击空槽位：填入当前选中的 option。
- 点击已填槽位：清空或替换。
- 如果 option 已在其他槽位使用，移动到新槽位。
- 干扰项可以被填入，但不会判对。

核心逻辑：

```ts
function assignOptionToSlot(slotId: string, optionId: string | null) {
  const nextSlots = value.slots.map(slot => {
    if (optionId && slot.option_id === optionId) {
      return { ...slot, option_id: null };
    }
    if (slot.slot_id === slotId) {
      return { ...slot, option_id: optionId };
    }
    return slot;
  });
  onChange({ kind: 'drag_drop', slots: nextSlots });
}
```

### 6.6 原生拖拽

不强制引入新库，第一版可以用 HTML5 Drag and Drop。

候选项：

```tsx
<button
  draggable
  onDragStart={(event) => event.dataTransfer.setData('text/plain', option.key)}
>
  {option.text}
</button>
```

槽位：

```tsx
<div
  onDragOver={(event) => event.preventDefault()}
  onDrop={(event) => {
    event.preventDefault();
    const optionId = event.dataTransfer.getData('text/plain');
    assignOptionToSlot(slot.id, optionId);
  }}
>
  ...
</div>
```

### 6.7 可用性要求

必须支持：

- 候选项显示。
- 槽位显示。
- 点击填槽。
- 拖拽填槽。
- 已填选项回显。
- 清空槽位。
- 交换/移动选项。
- 提交前检查是否所有槽位已填。
- 查看答案时标记每个槽位正确/错误。

### 6.8 样式建议

新增类：

```css
.drag-drop-board {}
.drag-option-pool {}
.drag-option-card {}
.drag-option-card.selected {}
.drag-slot-list {}
.drag-slot-card {}
.drag-slot-card.filled {}
.drag-slot-card.correct {}
.drag-slot-card.wrong {}
```

## 7. 自动判分设计

### 7.1 判分函数

前端可先判分，后续可移到后端。

```ts
function gradeDragDrop(model: InteractionModel, answer: DragDropUserAnswer): boolean {
  const slotMap = new Map(answer.slots.map(slot => [slot.slot_id, slot.option_id]));
  return model.slots.every(slot => slotMap.get(slot.id) === slot.correct_option);
}
```

### 7.2 提交答案格式

`ExamAnswerInput.user_answer` 保存 JSON 字符串：

```json
{
  "kind": "drag_drop",
  "slots": [
    { "slot_id": "slot-1", "option_id": "opt-a" },
    { "slot_id": "slot-2", "option_id": "opt-b" }
  ]
}
```

`recommended_answer` 保存后端现有推荐答案即可，也可以在提交时生成结构化文字：

```text
slot-1=opt-a; slot-2=opt-b
```

### 7.3 考试提交流程调整

当前 `submitExamAnswer()` 主要逻辑：

- 有 `detail.options` 时按选择题自动判分。
- 没有 options 时人工自评。

需要新增分支：

```ts
if (interaction?.kind === 'drag_drop' && interaction.can_auto_grade) {
  const answer = exam.interactionAnswer as DragDropUserAnswer;
  const isCorrect = gradeDragDrop(interaction, answer);
  const userAnswer = JSON.stringify(answer);
  ...
}
```

顺序建议：

1. 标准选择题。
2. drag_drop 自动判分。
3. hotspot 自动判分。
4. 人工自评。

### 7.4 防止未完成提交

提交按钮禁用条件：

```ts
const allSlotsFilled = answer.slots.every(slot => slot.option_id);
```

未填完不允许提交，除非后续设计“跳过”。

## 8. 历史回放

历史记录中点击题目时，应该能看到：

- 用户当时填的槽位。
- 正确答案。
- 哪些槽位填错。

第一版可简化：

- 历史列表仍显示 `正确/错误`。
- 打开题目详情时，如果从历史进入，传入 `ExamAnswerDetail.user_answer`。
- `QuestionPanel` 在非考试模式中可显示一个只读 DragDropQuestion。

后续增强：

```ts
type QuestionPanelProps = {
  historicalUserAnswer?: string;
}
```

解析：

```ts
function parseDragAnswer(value: string): DragDropUserAnswer | null {
  try {
    const parsed = JSON.parse(value);
    return parsed?.kind === 'drag_drop' ? parsed : null;
  } catch {
    return null;
  }
}
```

## 9. Hotspot 与 Drag Drop 的边界

拖拽题：

- 有候选项池。
- 用户把候选项放入槽位。
- 一个槽位对应一个 option。

Hotspot：

- 通常每行选择 Yes/No 或下拉项。
- 没有拖拽候选项池。

不要把 Hotspot 强行塞进 drag 表。Hotspot 后续应使用：

- `hotspot_options`
- `hotspot_rows`

当前后端已预留这两个表的读取逻辑。

## 10. 对现有文件的具体改动清单

### 10.1 Markdown skill

文件：

- `.github/skills/sc100-vision-md/SKILL.md`
- `.github/skills/exam-question-gen/SKILL.md`

改动：

- 明确 drag_drop 必须输出 `Drag Options` 和 `Drag Slots`。
- 明确 Option ID / Slot ID / Correct Option ID 约定。
- 明确无法确认时标记 `needs_review` 并降级。

### 10.2 导入脚本

文件：

- `scripts/import_vision_md_to_sqlite.py`
- `scripts/import_exam_gen_to_sqlite.py`

改动：

- schema 增加 `drag_options`、`drag_slots`。
- reset 时 drop 新表。
- `ParsedQuestion` 增加 drag 字段。
- 解析 `Drag Options`、`Drag Slots`。
- 导入时写新表。
- 增加一致性校验和 warning。

### 10.3 后端

文件：

- `TauriExam/src-tauri/src/main.rs`

改动：

- `get_interaction_model()` 中 drag 判断改为 slots 非空。
- 可选：新增后端判分 command，但第一版可以前端判分。

建议修正：

```rust
if normalized.contains("drag") && table_exists(&conn, "drag_slots") {
    let slots = query_drag_slots(&conn, &question.id)?;
    if !slots.is_empty() {
        return Ok(...);
    }
}
```

### 10.4 前端类型

文件：

- `TauriExam/src/types.ts`

新增：

```ts
export type DragDropUserAnswer = {
  kind: 'drag_drop';
  slots: Array<{
    slot_id: string;
    option_id: string | null;
  }>;
};
```

### 10.5 前端组件

新增：

- `TauriExam/src/DragDropQuestion.tsx`

修改：

- `TauriExam/src/QuestionPanel.tsx`
- `TauriExam/src/App.tsx`
- `TauriExam/src/styles.css`

改动：

- `QuestionPanel` 使用真实 `DragDropQuestion` 替代当前只读 `drag-shell`。
- 考试状态保存拖拽答案。
- 提交时自动判分。
- 历史/复习模式可只读展示正确答案。

## 11. 数据一致性约定

### 11.1 ID 约定

Option ID：

```text
opt-a, opt-b, opt-c, opt-d
```

Slot ID：

```text
slot-1, slot-2, slot-3
```

不要使用会随文案变化的完整文本做 ID。

### 11.2 显示文案与判分分离

判分只比较 ID：

```text
slot.id -> option.key
```

不要比较 `option_text`，因为：

- 文案可能翻译。
- 文案可能有标点差异。
- 同义词会导致误判。

### 11.3 干扰项

干扰项参与候选项展示，但不会出现在任何 slot 的 `correct_option` 中。

### 11.4 重复使用

第一版默认每个 option 最多使用一次。

如果未来需要支持重复使用，新增字段：

```sql
allow_reuse INTEGER NOT NULL DEFAULT 0
```

第一版不做。

## 12. 验收标准

### 12.1 Markdown

- drag_drop 题包含 `Drag Options`。
- drag_drop 题包含 `Drag Slots`。
- 每个 slot 的 `Correct Option ID` 都能找到对应 option。
- 缺失结构的题标记为 `needs_review`。

### 12.2 SQL

- 导入后存在 `drag_options` 和 `drag_slots`。
- 每道结构化拖拽题至少有 2 个 options 和 1 个 slot。
- `get_interaction_model()` 返回 `kind = "drag_drop"`。
- `can_auto_grade = true`。

### 12.3 UI

- 可以点击候选项并填入槽位。
- 可以拖拽候选项到槽位。
- 可以清空/替换槽位。
- 未填完时不能提交。
- 填正确时 `is_correct = true`。
- 填错误时 `is_correct = false`。
- 历史记录中能看到该题正确/错误。

### 12.4 回归

- 单选、多选题不受影响。
- 没有结构化 drag 表的旧拖拽题仍可人工自评。
- Hotspot 不被误识别成 drag_drop。

## 13. 推荐实施顺序

### 第一阶段：上游结构和导入

1. 更新两个 skill 的 drag_drop 生成规范。
2. 更新两个导入脚本，支持 `drag_options` / `drag_slots`。
3. 用 1 到 2 道样例 drag_drop Markdown 验证 SQLite 表。

### 第二阶段：Tauri 后端修正

1. 修正 `get_interaction_model()` 的 slots 非空判断。
2. 检查返回的 `InteractionModel` 是否完整。

### 第三阶段：前端真实控件

1. 新增 `DragDropQuestion.tsx`。
2. `QuestionPanel` 展示真实控件。
3. `App.tsx` 考试状态接入拖拽答案。
4. 提交时自动判分并保存 JSON。

### 第四阶段：历史回放

1. 解析历史 `user_answer` JSON。
2. 只读展示用户答案和正确答案。
3. 错误槽位高亮。

## 14. 最小可用版本定义

第一版完成以下能力即可算正式可用：

- Markdown 能生成结构化 drag_drop。
- 导入脚本能写 `drag_options` / `drag_slots`。
- 考试中能点击填槽。
- 提交时自动判分。
- 答案保存为 JSON。

真正鼠标拖拽可以和点击填槽同批完成，但如果时间紧，点击填槽优先级更高，因为它更稳定，且已经满足“可作答、可判分、可复习”的核心目标。
