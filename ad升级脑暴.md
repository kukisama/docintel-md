# AD 升级脑暴

> 用途：记录前期关于 AD 升级、微软工作边界、业务依赖评估、抓包/日志分析、脚本化和入库交付的脑暴要点。  
> 风格：仅作为后续方案设计参考，不作为正式项目计划或合同边界。

---

## 1. 核心判断

| 主题 | 当前判断 | 备注 |
|---|---|---|
| 微软主要工作 | 方案设计、健康检查、业务依赖评估设计、测试环境支持 | 主要价值集中在设计和技术判断，不应被扩大成全量实施 |
| 实施责任 | 客户/实施方主责 | 包括服务器准备、生产操作、业务验证、日志采集执行 |
| 难点 | 业务到底如何依赖 AD | 需要调查表 + 日志 + 流量 + 抓包证据交叉验证 |
| 业务依赖评估目标 | 找到谁在用、怎么用、连谁、用什么协议、风险在哪 | 不追求一次性 100% 自动识别，但要形成证据链 |
| 工具化方向 | 可行 | 先离线分析，再自动采集，最后平台化 |

---

## 2. 微软工作边界脑暴

| 工作块 | 微软应该做 | 客户/实施方应该做 | 注意事项 |
|---|---|---|---|
| 方案设计 | 提供升级路径、缩容思路、备份/回滚设计、依赖识别方法 | 提供环境信息、确认目标和限制 | 微软做设计和审阅，不默认执行 |
| 健康检查 | 提供检查项、解读关键问题、给修复建议 | 执行 dcdiag、repadmin、DNS、事件日志检查 | 历史遗留故障不应自动变成微软全量修复工作 |
| 业务依赖评估 | 设计调查表、证据采集方法、协议判断规则、风险分类 | 业务填表、日志/抓包采集、确认业务影响 | 业务结论最终需要客户确认 |
| 测试环境 | 指导测试项、分析 AD 相关异常 | 搭建测试环境、执行测试、记录结果 | 微软是配合支持，不是测试执行方 |
| 生产升级 | 关键节点 On-demand 支持 | 执行域控提升、迁移、降级、回退 | 不默认驻场或全程值守 |

---

## 3. 业务依赖评估总体方法

| 步骤 | 做什么 | 产出 | 价值 |
|---|---|---|---|
| 1. 调查表 | 让业务方自报系统、用户、使用方式、协议、服务器、验证人 | 业务系统 AD 依赖调查表 | 快速缩小范围 |
| 2. 日志采集 | 从 DC、文件服务器、应用服务器收集认证和访问日志 | 认证/访问事件证据 | 判断 Kerberos、NTLM、SMB 等 |
| 3. 流量分析 | 从防火墙、NetFlow、tshark CSV 获取源/目的/端口 | 流量命中表 | 判断谁还访问旧 DC |
| 4. 定向抓包 | 对争议应用/关键窗口抓取小流量包 | pcap/pcapng + 摘要 | 验证真实协议和连接目标 |
| 5. 配置扫描 | 查 DNS、hosts、LDAP URL、服务账号、计划任务、IIS AppPool | 配置命中项 | 发现采集窗口外的潜在依赖 |
| 6. 证据关联 | 把调查表、日志、流量、配置、抓包关联到应用 | 应用级证据链 | 让结论站得住 |
| 7. 风险分级 | 按旧 DC 命中、协议、业务等级、验证能力打分 | 风险矩阵 | 指导迁移和测试优先级 |

---

## 4. 调查表设计要点

### 4.1 调查表不要只问协议

| 不推荐问法 | 推荐问法 | 原因 |
|---|---|---|
| 你们用 Kerberos 吗？ | 用户如何登录？是否用域账号？ | 业务方可能不懂协议 |
| 你们用 LDAP 吗？ | 应用是否查询用户/组/组织架构？是否配置 AD 地址？ | 更容易让应用负责人理解 |
| 你们访问哪个 DC？ | 配置里是否写了域控主机名/IP？DNS 指向谁？ | 便于识别硬编码风险 |
| 是否有 AD 依赖？ | 是否访问文件共享、VPN、堡垒机、打印、Web SSO？ | 从业务行为反推 AD 依赖 |

### 4.2 建议字段

| 字段 | 用途 | 示例 |
|---|---|---|
| 应用名称 | 应用识别 | MES、OA、VPN |
| 业务等级 | 风险分级 | 核心/重要/一般 |
| 使用范围 | 判断影响面 | 全厂区、XX 厂区、财务部 |
| 使用用户 | 判断验证对象 | 域用户组、部门、服务账号 |
| 使用方式 | 反推协议 | 访问文件共享、Web 登录、客户端登录 |
| 应用服务器 | 定位日志/抓包点 | 10.1.1.10 |
| 访问目标 | 判断依赖对象 | fileserver01、ldap://dc01 |
| 是否域账号登录 | 判断 AD 认证 | 是/否/未知 |
| 是否 AD 组授权 | 判断目录查询/授权 | 是/否/未知 |
| 是否 LDAP/LDAPS | 判断目录协议 | LDAP、LDAPS、未知 |
| 是否硬编码 DC | 判断旧 DC 下线风险 | 是/否/未知 |
| 验证方式 | 设计测试动作 | 登录、查询、交易、批处理 |
| 验证窗口 | 采集和测试时间 | 周六 22:00 |

---

## 5. 业务行为到协议的推导

| 业务描述 | 可能协议/依赖 | 需要验证的证据 |
|---|---|---|
| 用户访问 `\\fileserver\share` | SMB 445、Kerberos/NTLM、AD 组授权 | DC 4769 `cifs/fileserver`、文件服务器 4624、445 流量 |
| Web 应用域账号单点登录 | Kerberos/NTLM、HTTP SPN | DC 4769 `HTTP/app`、4776、应用服务器日志 |
| 应用查询组织架构 | LDAP 389 / LDAPS 636 / GC 3268/3269 | 389/636/3268/3269 流量、LDAP 配置 |
| VPN/堡垒机域账号登录 | LDAP/RADIUS/Kerberos/NTLM | 设备配置、DC 认证日志、防火墙流量 |
| SQL 使用域账号认证 | Kerberos、MSSQLSvc SPN | DC 4769 `MSSQLSvc/sql`、SQL 连接日志 |
| 打印/文件/旧客户端 | SMB、Kerberos/NTLM | 445 流量、4769/4776、客户端来源 |
| 批处理/计划任务 | 服务账号、LDAP、SMB、SQL | 计划任务账号、服务账号认证日志 |

---

## 6. 日志证据来源

| 来源 | 关键事件/字段 | 能判断什么 | 局限 |
|---|---|---|---|
| DC 安全日志 | 4768 | Kerberos TGT 请求 | 只说明初始票据请求 |
| DC 安全日志 | 4769 | Kerberos 服务票据，如 `cifs/`、`HTTP/`、`ldap/`、`MSSQLSvc/` | 判断访问服务类型 | 需要结合账号、时间、来源主机 |
| DC 安全日志 | 4771 | Kerberos 失败 | 判断认证异常 | 需要分析失败码 |
| DC 安全日志 | 4776 | NTLM 认证 | 识别旧协议/回退 | 不一定能精确映射到应用 |
| 服务器安全日志 | 4624 | 登录成功、登录类型、认证包 | 辅助判断 SMB/交互/服务登录 | 日志量大 |
| LDAP 诊断日志 | LDAP 绑定、查询、慢查询 | 识别 LDAP 应用 | 不宜长期高等级开启 |
| 应用日志 | 认证失败、LDAP URL、连接目标 | 直接定位应用行为 | 格式不统一 |
| 防火墙/NetFlow | 源 IP、目标 DC、端口、时间 | 识别访问旧 DC 和端口 | 可能受 NAT/代理影响 |

---

## 7. 抓包与 tshark 脑暴

### 7.1 工具定位

| 工具 | 定位 | 适合用途 |
|---|---|---|
| Wireshark | GUI 分析工具 | 人工打开包、看协议细节 |
| tshark | Wireshark 命令行分析工具 | 脚本化解析 pcap/pcapng |
| dumpcap | 命令行抓包工具 | 稳定抓包、切文件、控制大小 |
| Npcap | Windows 抓包驱动 | Wireshark/tshark/dumpcap 依赖组件 |

### 7.2 定向抓包是否靠谱

| 判断 | 说明 |
|---|---|
| 可行 | 只抓 AD 相关 DC、端口、时间窗口，数据量可控 |
| 推荐方式 | dumpcap 抓包，tshark 解析，脚本入库 |
| 不建议 | 长时间全量抓包、全网无目标抓包 |
| 适合场景 | 应用协议不明、硬编码 DC、LDAPS/TLS、Kerberos/NTLM、旧 DC 下线前确认 |

### 7.3 常见端口

| 协议 | 端口 | 说明 |
|---|---:|---|
| DNS | 53 | 域名解析、SRV 查询 |
| Kerberos | 88 | 认证票据 |
| NTP | 123 | 时间同步，按需关注 |
| RPC Endpoint Mapper | 135 | RPC 入口 |
| LDAP | 389 | 明文 LDAP |
| SMB | 445 | 文件共享、SYSVOL/NETLOGON |
| Kerberos Password Change | 464 | 密码变更 |
| LDAPS | 636 | 加密 LDAP |
| GC LDAP | 3268 | 全局编录查询 |
| GC LDAPS | 3269 | 加密全局编录查询 |

### 7.4 抓包过滤思路

| 场景 | Capture Filter 思路 |
|---|---|
| 应用服务器到指定 DC | `host <DC_IP> and (port 53 or port 88 or port 389 or port 636 or port 3268 or port 3269 or port 464)` |
| 确认是否访问旧 DC | `host <OLD_DC_IP>` |
| 只看 LDAP/LDAPS | `host <DC_IP> and (port 389 or port 636 or port 3268 or port 3269)` |
| 只看 Kerberos | `host <DC_IP> and port 88` |
| 只看 DNS | `host <DC_IP> and port 53` |

> 注意：过滤器应根据实际抓包点调整。如果在应用服务器本机抓包，不一定需要写应用服务器 IP，因为本机流量天然包含该主机。

---

## 8. 证据链设计

| 证据类型 | 例子 | 可信度 | 用途 |
|---|---|---|---|
| 应用配置证据 | LDAP URL、旧 DC IP、服务账号、SPN | 高 | 证明应用设计/配置上存在依赖 |
| 认证日志证据 | 4769、4776、4624 | 高 | 证明采集窗口内发生过认证 |
| 网络流量证据 | AppServer -> DC:389/636/88 | 高 | 证明源和目标通信关系 |
| 抓包证据 | pcap 中的 Kerberos/LDAP/DNS 连接 | 高 | 排查争议点和协议细节 |
| 调查表证据 | 业务方填写使用方式和范围 | 中 | 缩小范围、确定验证对象 |
| 业务验证证据 | 负责人确认登录/交易/查询通过 | 高 | 最终确认业务影响 |

### 8.1 判断原则

| 原则 | 说明 |
|---|---|
| 不以单一调查表为准 | 调查表只能作为线索 |
| 至少两类证据交叉验证 | 例如调查表 + 日志，或配置 + 流量 |
| 采集窗口必须覆盖业务行为 | 登录高峰、换班、批处理、月结等 |
| 结论必须带证据 | 不能只写“疑似使用 LDAP” |
| 不确定就标为需验证 | 不强行下结论 |

---

## 9. 自动化脚本设想

### 9.1 总体流程

| 阶段 | 输入 | 脚本动作 | 输出 |
|---|---|---|---|
| 1. 调查表导入 | Excel/CSV | 读取应用、服务器、用户、协议自报 | 应用清单 |
| 2. 采集计划生成 | 应用清单、DC 清单 | 生成日志/抓包/配置扫描计划 | 采集任务 |
| 3. 日志分析 | EVTX/导出 CSV | 解析 4768/4769/4771/4776/4624 | 认证命中表 |
| 4. 抓包分析 | pcap/pcapng | tshark 提取源、目的、端口、协议 | 流量命中表 |
| 5. 配置扫描 | 服务器配置输出 | 查 DNS、hosts、LDAP URL、服务账号 | 配置命中表 |
| 6. 规则判断 | 所有证据 | 关联应用和证据，判定协议和风险 | 应用依赖结果 |
| 7. 报告生成 | 数据库 | 输出 Markdown/Excel/HTML | 交付报告 |

### 9.2 脚本可判断内容

| 判断项 | 数据来源 | 可靠性 |
|---|---|---|
| 是否访问旧 DC | 流量、抓包、防火墙日志 | 高 |
| 是否使用 Kerberos | DC 4769、88 流量 | 高 |
| 是否存在 NTLM | DC 4776 | 高 |
| 是否使用 LDAP | 389/3268 流量、配置、LDAP 日志 | 中高 |
| 是否使用 LDAPS | 636/3269 流量、配置、证书 | 中高 |
| 是否访问文件共享 | 4769 `cifs/`、445 流量、文件服务器日志 | 高 |
| 是否硬编码 DC | 配置文件、hosts、连接串、流量固定目标 | 中高 |
| 是否依赖 AD 组授权 | 调查表、应用配置、LDAP 查询、业务验证 | 中 |

### 9.3 不应承诺自动判断的内容

| 内容 | 原因 |
|---|---|
| 100% 覆盖所有业务依赖 | 采集窗口外行为可能漏掉 |
| 加密流量中的业务内容 | LDAPS/TLS 无法直接看查询内容 |
| NAT/代理后的真实用户 | 网络层可能隐藏源 |
| 厂商应用内部逻辑 | 配置可能在数据库或专有系统里 |
| 最终业务影响 | 必须由业务负责人验证 |

---

## 10. 入库交付设想

### 10.1 是否入库

| 结论 | 说明 |
|---|---|
| 建议入库 | 便于多轮分析、旧 DC 下线前复查、证据关联和报告生成 |
| 推荐数据库 | SQLite 起步，后续可迁移 PostgreSQL/SQL Server |
| 不建议入库 | 原始 pcap/EVTX 大文件不建议直接塞数据库 |
| 推荐方式 | 原始文件存文件系统，数据库保存摘要、哈希、证据索引 |

### 10.2 数据表脑暴

| 表 | 内容 | 作用 |
|---|---|---|
| applications | 应用基本信息 | 应用维度主表 |
| app_survey | 调查表明细 | 保存业务自报信息 |
| collection_jobs | 采集任务 | 记录每轮采集范围和状态 |
| auth_events | 认证日志命中 | 保存 4769/4776 等解析结果 |
| network_flows | 网络流量命中 | 保存 tshark/防火墙/NetFlow 摘要 |
| config_findings | 配置扫描命中 | 保存 DNS、hosts、LDAP URL、服务账号等 |
| app_dependency_results | 应用依赖结论 | 保存最终协议、风险、建议 |
| evidence_links | 证据关联 | 把结论和证据绑定，防止被质疑 |

### 10.3 原始文件存储建议

| 文件 | 建议存储 | 数据库保存 |
|---|---|---|
| pcap/pcapng | 受控目录 | 文件路径、哈希、采集时间、目标主机 |
| EVTX | 受控目录 | 文件路径、哈希、DC 名称、时间范围 |
| 调查表 | 归档目录 + 入库 | 原始文件路径、导入批次 |
| 防火墙 CSV | 受控目录 | 文件路径、哈希、解析批次 |
| 报告 | 输出目录 | 报告路径、生成时间 |

---

## 11. 报告输出形态

| 报告 | 面向对象 | 核心字段 |
|---|---|---|
| 应用依赖总览 | 管理层/项目组 | 应用、风险等级、是否旧 DC 命中、是否需验证 |
| 协议命中明细 | 技术团队 | 源、目标、端口、协议、次数、证据来源 |
| 旧 DC 下线风险清单 | AD/运维团队 | 旧 DC、源服务器、协议、最近命中、应用 |
| NTLM 风险清单 | 安全/身份团队 | 账号、来源、目标、命中时间、业务归属 |
| LDAP/LDAPS 依赖清单 | 应用/身份团队 | 应用、源服务器、目标 DC、端口、证书风险 |
| 业务验证建议清单 | 业务负责人 | 应用、验证人、验证动作、验证窗口 |
| 整改跟踪表 | 项目经理 | 风险项、负责人、状态、首次发现、最近命中 |

---

## 12. 风险分级脑暴

| 风险等级 | 判断条件 | 建议动作 |
|---|---|---|
| 高 | 核心业务 + 访问旧 DC；硬编码 DC；LDAPS 证书不明确；NTLM 旧系统；无法测试 | 升级前专项验证，必要时单独整改 |
| 中 | 有 AD 依赖但能测试；访问域名但协议明确；服务账号依赖 | 纳入测试计划，变更窗口重点观察 |
| 低 | 无固定 DC；仅通过域名访问；有测试环境且验证简单 | 常规验证 |
| 待确认 | 调查表不完整；采集窗口未覆盖；证据不足 | 补采日志/流量或安排业务验证 |

---

## 13. 典型案例写法

| 场景 | 证据 | 结论 | 建议 |
|---|---|---|---|
| XX 厂区用户访问文件共享 | 调查表：访问 `\\fileserver01\share`；DC 4769：`cifs/fileserver01`；流量：客户端到文件服务器 445 | 使用 SMB 文件共享，认证主要依赖 Kerberos，授权可能依赖 AD 组 | 迁移后验证 XX 厂区用户访问共享和权限 |
| 某应用访问旧 DC LDAP | 配置：`ldap://old-dc01`；流量：AppServer -> old-dc01:389 | 硬编码旧 DC，旧 DC 下线高风险 | 改为域名或新 DC，验证 LDAP 查询 |
| 某系统存在 NTLM | DC 4776 命中来源 app01 | 存在 NTLM 认证，可能是旧组件或回退 | 查应用认证配置，评估是否可改 Kerberos |
| LDAPS 应用 | 流量：AppServer -> dc01:636；证书链未知 | 使用 LDAPS，存在证书/TLS 兼容风险 | 提前验证证书链、TLS 和新 DC LDAPS |

---

## 14. 可以写进方案设计的亮点

| 名称 | 说明 |
|---|---|
| AD 业务依赖证据化识别方法 | 用调查表、日志、流量、抓包、配置扫描交叉验证业务依赖 |
| 旧 DC 下线前命中复查 | 在下线前再跑一轮脚本，确认是否仍有访问旧 DC |
| 应用级风险矩阵 | 按应用输出协议、证据、风险、验证建议 |
| 依赖证据库 | 将调查表、日志命中、流量命中、配置命中和结论入 SQLite |
| 定向抓包模板 | 基于 DC、应用服务器、端口和时间窗口生成小范围抓包任务 |
| 多轮对比 | 对比整改前后是否仍命中旧 DC/NTLM/LDAP |

---

## 15. 需要提前声明的局限

| 局限 | 说明 | 处理方式 |
|---|---|---|
| 采集窗口外行为可能漏掉 | 月结、夜间批处理、换班等可能不在窗口内 | 让业务方提供关键时间窗口，必要时多轮采集 |
| 加密流量不可见内容 | LDAPS/TLS 只能看到连接和握手 | 结合配置、证书和应用日志判断 |
| NAT/代理隐藏真实来源 | 防火墙日志可能只看到代理 | 结合代理日志和应用服务器日志 |
| 应用配置不统一 | 配置可能在数据库或厂商平台 | 让业务/厂商配合确认 |
| 自动结论不是最终验收 | 工具只能辅助判断 | 最终业务影响由业务负责人验证 |
| 抓包涉及安全合规 | 可能包含敏感信息 | 限定范围、时间、端口；保存摘要和哈希 |

---

## 16. 最小可行交付物

| 组件 | 内容 | 优先级 |
|---|---|---|
| 调查表模板 | 应用、用户、使用方式、服务器、协议自报、验证人 | P0 |
| 离线分析脚本 | 读取调查表、EVTX、tshark CSV、防火墙 CSV | P0 |
| SQLite 证据库 | 保存应用、采集、日志、流量、配置、结论 | P0 |
| Markdown/Excel 报告 | 输出应用风险、旧 DC 命中、协议清单、验证建议 | P0 |
| 定向抓包脚本 | dumpcap/tshark 生成小范围采集结果 | P1 |
| 配置扫描脚本 | DNS、hosts、服务账号、计划任务、IIS AppPool | P1 |
| 多轮对比 | 对比整改前后命中情况 | P2 |
| Web UI | 上传调查表/日志，自动生成报告 | P3 |

---

## 17. 一句话总结

| 方向 | 总结 |
|---|---|
| 项目定位 | 微软主要做设计、方法、技术判断和支持，客户/实施方做采集、执行和业务确认 |
| 业务依赖识别 | 调查表只是线索，关键是建立日志/流量/抓包/配置的证据链 |
| 抓包策略 | 用 dumpcap/tshark 做定向抓包和解析，避免全量抓包导致数据不可控 |
| 工具化方向 | 先做离线分析和 SQLite 证据库，再逐步自动采集和平台化 |
| 最大价值 | 旧 DC 下线和 AD 2025 升级前，能回答“谁还在用旧域控、用什么协议、风险在哪里” |
