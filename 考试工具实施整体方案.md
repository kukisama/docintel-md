# 考试工具实施整体方案

本文档说明基于现有题库 SQLite 的考试工具实施方案。目标是做一个本地优先、界面现代、支持多题库和历史学习记录的考试练习工具。技术路线固定为 **Tauri + React**，后端/本地能力按最容易落地的方式实现。

## 目标

基于已经整理好的题库数据库，实现一个可浏览、可练习、可考试、可复盘的本地考试工具。

核心能力：

- 顺序浏览题库，任选题目查看题干和备选答案。
- 答案默认隐藏，可点击查看源答案和推荐答案。
- 根据题目关联的 PDF 页码，只显示对应几页原文内容。
- 预留原文/译文切换逻辑，后续接入 Azure Translator / Speech。
- 支持自定义题数、计时考试、单题耗时统计和总耗时统计。
- 保存历史考试记录，方便复盘正确题、错误题和答题过程。
- 支持未来多题库。

## 技术选型

### 前端与桌面壳

采用：

```text
Tauri + React + TypeScript + Vite
```

理由：

- React 是主流 Web UI 技术，适合复杂交互、状态管理和现代界面。
- Tauri 适合本地桌面工具，体积轻，可以访问本地文件和数据库。
- Vite 开发体验好，启动快。
- TypeScript 方便维护题目、考试、历史记录等数据结构。

推荐 UI：

```text
Tailwind CSS + shadcn/ui 或 DaisyUI + Lucide Icons
```

第一版优先保证清晰、好用，不追求复杂动画。

### 本地能力

Tauri 后端使用 Rust 负责：

- 读取题库 SQLite。
- 读取/写入学习记录 SQLite。
- 根据题目页码读取 PDF 对应页。
- 返回 PDF 页渲染后的图片路径或图片数据。
- 管理多题库注册信息。

如果 PDF 渲染用 Rust 实现成本较高，第一版可以保留一个本地 Python 渲染脚本作为过渡，但最终建议收敛到 Tauri/Rust 命令中，减少运行依赖。

## 总体架构

```text
Tauri Desktop App
  ├─ React UI
  │   ├─ 题库浏览
  │   ├─ 题目详情
  │   ├─ PDF 原文页预览
  │   ├─ 考试模式
  │   ├─ 历史记录
  │   └─ 设置 / 题库管理
  │
  └─ Tauri Commands
      ├─ Bank Store：读取题库 SQLite
      ├─ Exam Store：读写学习记录 SQLite
      ├─ PDF Renderer：按需渲染 PDF 页
      ├─ Translation Service：预留翻译/语音接口
      └─ Settings Store：应用配置
```

数据流：

```text
题库 SQLite -> Tauri 命令 -> React 页面展示
用户答题 -> React 状态 -> Tauri 命令 -> 学习记录 SQLite
题目页码 -> PDF Renderer -> 对应页图片 -> React 内嵌展示
```

## 数据边界

### 题库数据库

每套题库保留一个独立 SQLite，例如：

```text
output/vision-db/sc-100.sqlite
```

题库数据库来自标准 Markdown 入库流程，主要包含：

- `markdown_batches`
- `questions`
- `options`
- `answer_areas`

题库数据库应视为**只读或可重建数据源**。不要把用户考试历史、错题、收藏、翻译缓存直接写入题库数据库，因为题库未来可能重新导入或替换。

### 学习记录数据库

单独维护应用数据库：

```text
output/exam-tool/exam-tool.sqlite
```

用于保存：

- 多题库注册信息。
- 考试 session。
- 每题作答记录。
- 错题/收藏/复核标记。
- 翻译缓存。
- 应用设置。

这样即使重建某套题库，也不会影响历史学习记录。

## 建议应用数据库表

### question_banks

记录所有题库。

```text
id
exam_code
name
db_path
pdf_path
kind
enabled
created_at
updated_at
```

示例：

```text
id: sc-100
exam_code: SC-100
name: SC-100 题库
db_path: output/vision-db/sc-100.sqlite
pdf_path: SC-100 题目+答案+讨论.pdf
kind: sqlite
enabled: true
```

### exam_sessions

记录一次考试。

```text
id
bank_id
title
mode
question_count
started_at
finished_at
duration_seconds
score
total_questions
correct_count
wrong_count
created_at
```

### exam_answers

记录一次考试中的单题作答。

```text
id
session_id
bank_id
question_id
sequence_number
started_at
answered_at
duration_seconds
user_answer
correct_answer
recommended_answer
is_correct
is_manual_review
created_at
```

### question_flags

记录个人学习标记。

```text
id
bank_id
question_id
flag_type
note
created_at
updated_at
```

常见 `flag_type`：

- `favorite`
- `wrong`
- `needs_review`
- `mastered`

### translations

预留翻译缓存。

```text
id
bank_id
question_id
field_name
source_hash
language
translated_text
provider
created_at
updated_at
```

第一版可以只做 UI 切换和数据结构预留；没有译文时回退显示原文。

### app_settings

保存应用配置。

```text
key
value
updated_at
```

例如：默认题库、主题、是否默认显示译文、PDF 缓存目录等。

## 页面与功能模块

### 1. 题库管理页

功能：

- 展示已注册题库。
- 添加题库 SQLite 和对应 PDF。
- 显示题数、题型分布、状态分布。
- 进入题库浏览。

路径示例：

```text
/banks
```

### 2. 题目列表页

功能：

- 顺序加载所有题目。
- 支持按题号跳转。
- 支持按题型、状态、是否错题筛选。
- 支持点击任意题进入详情。

路径示例：

```text
/banks/:bankId/questions
```

显示字段：

- 题号
- 题型
- 状态
- 页码范围
- 是否做过
- 是否错题
- 是否收藏

### 3. 题目详情页

功能：

- 显示题干。
- 显示选项或答案区。
- 答案默认隐藏。
- 点击按钮后显示：
  - Source Answer
  - My Recommended Answer
  - 我的判断（中文）
  - Reasoning
  - Notes
- 显示 PDF 对应页。
- 支持收藏、标记复核、标记已掌握。
- 可从历史记录跳转回来查看当时作答。

路径示例：

```text
/banks/:bankId/questions/:questionId
```

### 4. PDF 原文页预览

根据题目中的：

```text
pdf_file
page_from
page_to
```

只渲染并展示对应页，不展示多余页。

建议策略：

- 首次打开题目时按需渲染。
- 渲染结果缓存到本地目录。
- 再次打开直接读取缓存。

缓存示例：

```text
output/exam-tool/pdf-cache/<bank_id>/page-0262.png
```

页面中可以采用右侧抽屉、下方折叠区域或分屏视图展示 PDF 页。

### 5. 语言切换

第一版只保留逻辑和 UI：

- 原文
- 译文

没有译文时显示原文。

后续可接入：

- Azure AI Translator：题干、选项、解释文字翻译。
- Azure Speech：朗读题目、朗读答案、语音学习模式。

翻译结果写入 `translations`，避免重复调用服务。

### 6. 创建考试页

功能：

- 选择题库。
- 自定义出题数量。
- 选择出题范围：全部题、未做题、错题、收藏题、指定题型、指定状态。
- 选择顺序：顺序 / 随机。
- 是否包含 `needs_review`。
- 是否启用计时。

路径示例：

```text
/exam/new
```

### 7. 考试进行页

功能：

- 显示当前题号和总题数。
- 显示总计时。
- 显示单题计时。
- 选择答案。
- 提交当前题。
- 下一题。
- 可临时标记不确定。

普通单选/多选可自动判分。

HOTSPOT / DRAG DROP / 复杂表格题第一版建议支持人工自评：

```text
我答对了 / 我答错了 / 稍后复核
```

这样比一开始强做复杂题自动判分更稳。

### 8. 考试结果页

功能：

- 总题数。
- 正确数。
- 错误数。
- 正确率。
- 总耗时。
- 平均单题耗时。
- 每题作答结果。
- 错题列表。
- 点击题目跳转题目详情页。

路径示例：

```text
/exam/:sessionId/result
```

### 9. 历史记录页

功能：

- 查看历史考试 session。
- 按题库筛选。
- 查看每次考试正确率和耗时。
- 进入某次考试结果。
- 从错题跳转到题库原始记录。

路径示例：

```text
/history
```

## 多题库策略

从第一版就按多题库设计。

原则：

- 每套题库可以是一个独立 SQLite。
- 应用数据库中只注册题库路径和元信息。
- 所有学习记录都使用 `bank_id + question_id` 关联。

这样未来新增 AZ-500、AI-102、DP-203 等题库时，不需要改考试逻辑。

## 推荐项目结构

可以在当前仓库新增一个应用目录：

```text
exam-tool/
  package.json
  src/
    App.tsx
    main.tsx
    routes/
    components/
    pages/
    api/
    types/
    state/
  src-tauri/
    Cargo.toml
    tauri.conf.json
    src/
      main.rs
      commands/
        banks.rs
        questions.rs
        exams.rs
        pdf.rs
        settings.rs
      db/
        app_db.rs
        bank_db.rs
  public/
  index.html
```

说明：

- `src/` 是 React 前端。
- `src-tauri/` 是 Tauri/Rust 本地命令。
- `api/` 封装前端调用 Tauri command 的逻辑。
- `types/` 定义题目、题库、考试、历史记录等 TypeScript 类型。

## 第一版 MVP 范围

第一版不要一次做满，建议按下面顺序实现。

### 阶段 1：题库浏览器

- 初始化 Tauri + React 项目。
- 注册 SC-100 题库。
- 读取 `sc-100.sqlite`。
- 展示题目列表。
- 展示题目详情。
- 答案隐藏/显示。
- 展示推荐答案和中文判断。

### 阶段 2：PDF 页预览

- 根据 `page_from` / `page_to` 找到对应 PDF 页。
- 只渲染题目关联页。
- 缓存渲染结果。
- 在题目详情页内展示。

### 阶段 3：考试模式

- 创建考试。
- 自定义题数。
- 顺序/随机出题。
- 单题计时。
- 总计时。
- 保存作答。
- 生成结果页。

### 阶段 4：历史记录和错题

- 历史考试列表。
- 考试详情。
- 错题列表。
- 从错题跳转题目详情。
- 收藏/复核/已掌握标记。

### 阶段 5：多题库和翻译预留

- 题库注册管理。
- 支持多个题库 SQLite。
- 原文/译文切换 UI。
- 翻译缓存表。
- 后续接 Azure Translator / Speech。

## 关键实现约束

1. 题库 SQLite 尽量只读，学习记录写入应用 SQLite。
2. 多题库统一使用 `bank_id + question_id` 作为跨库引用。
3. PDF 只渲染题目关联页，不整本加载。
4. 答案默认隐藏，用户点击后再显示。
5. 源答案和推荐答案都要保留，不要只显示一个。
6. HOTSPOT / DRAG DROP 第一版允许人工自评，不强行自动判分。
7. 翻译第一版只做 UI 和缓存结构预留，不急着调用云服务。
8. 前端不要重新解析 Markdown；优先读取 SQLite 结构化字段，必要时展示 `question_md` 原文。

## 后续可扩展方向

- 接入 Azure Translator 做题目和解释翻译。
- 接入 Azure Speech 做朗读和听题模式。
- 对 `needs_review` 题目做专项复核工作流。
- 增加学习统计图表。
- 增加按知识点/Topic 的掌握度分析。
- 支持导出错题集。
- 支持从新题库 SQLite 自动注册题库。

## 结论

本工具建议按 **Tauri + React + SQLite** 实现：

- React 负责现代 UI 和考试交互。
- Tauri 负责本地文件、SQLite、PDF 渲染和设置管理。
- 题库 SQLite 作为只读内容源。
- 应用 SQLite 保存学习历史和个人状态。

这个方案既能做出好看的本地桌面应用，又不会推翻现有“PDF → 标准 Markdown → SQLite”的题库整理流程。先做题库浏览和答案显示，再做 PDF 页预览，最后补考试模式和历史记录，是最稳的实施路线。