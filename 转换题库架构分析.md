# 转换题库架构分析

## 一、系统总览

整套系统的目标是：把一份图片密集的 SC-100 考试 PDF（含题目、答案、社区讨论），自动拆分成 **每 10 题一份** 的结构化 Markdown 文件，并由 AI 逐题视觉审阅、判答、给出中文建议。

核心架构可以总结为 **"外循环驱动 + 内循环智能"**：

```
┌──────────────────────────────────────────────────────┐
│  continue-sc100.ps1 （外循环 · 调度器）                │
│  ┌─────────────────────────────────────────────────┐ │
│  │  每一轮：                                        │ │
│  │  1. detect_next_batch.py → JSON 状态             │ │
│  │  2. done=true? → 退出                            │ │
│  │  3. copilot CLI + 固定 prompt + 页范围            │ │
│  │     ↓ 触发 SKILL                                 │ │
│  │     ┌────────────────────────────────────────┐   │ │
│  │     │  sc100-vision-md SKILL（内循环 · 大脑） │   │ │
│  │     │  A. 渲染 PDF 页面 → PNG 图片             │   │ │
│  │     │  B. 提取辅助文本                         │   │ │
│  │     │  C. 视觉读图 + AI 判题                   │   │ │
│  │     │  D. 写出结构化 Markdown                  │   │ │
│  │     │  E. 处理跨批题目衔接 (carryover)         │   │ │
│  │     └────────────────────────────────────────┘   │ │
│  │  4. 下一轮 → 回到 1                              │ │
│  └─────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────┘
```

---

## 二、为什么"说一句话"就能让系统工作

这是理解整套系统的关键。答案是 **三层机制的协作**：

### 第 1 层：SKILL 文件 = 预置的详细指令手册

[SKILL.md](.github/skills/sc100-vision-md/SKILL.md) 并不是代码，它是写给 Copilot 的 **超长系统提示词**（约 200 行）。当 Copilot 看到 "继续处理" 这类关键词时，会自动加载这份文件作为上下文。SKILL.md 里包含了：

| SKILL.md 中的内容 | 作用 |
|---|---|
| `description` 里的触发词列表 | 告诉 Copilot "什么时候该用这个技能" |
| `Batch Procedure` 步骤 0-5 | 告诉 Copilot **完整的操作流程**，包括先运行哪个脚本、参数怎么填 |
| `Core Principles` | 告诉 Copilot "视觉优先、不盲信答案、必须处理跨题" |
| `question-batch-template.md` | 告诉 Copilot 输出 Markdown 的精确格式 |
| `Quality rules` / `Completion Checklist` | 告诉 Copilot 最终文件必须满足哪些条件 |
| `Stop rule` | 告诉 Copilot 如果 `done=true` 就什么都不做 |

**类比**：SKILL.md 相当于给新员工写的一份标准作业流程（SOP）。你只需要说"继续干"，员工就会照着 SOP 从头到尾执行。

### 第 2 层：detect_next_batch.py = 自动状态感知

用户不需要告诉 Copilot "现在处理到哪了"，因为 [detect_next_batch.py](.github/skills/sc100-vision-md/scripts/detect_next_batch.py) 会扫描 `output/vision-md/` 下所有已有的 Markdown 文件，解析出：

```json
{
  "done": false,
  "existing_batches": 31,
  "latest_markdown": "output/vision-md/sc-100-pages-733-774/sc-100-questions-301-310.md",
  "latest_page_range": [733, 774],
  "latest_question_range": [301, 310],
  "previous_carryover": ["..."],
  "next_page_range": [775, 804],
  "expected_next_question_start": 311,
  "suggested_pages_output": "output/vision-pages/sc-100-pages-775-804",
  "suggested_md_output_dir": "output/vision-md/sc-100-pages-775-804"
}
```

这就是"无状态自动续接"的关键——每次运行时从文件系统反推进度。

### 第 3 层：continue-sc100.ps1 = 自动循环调度

[continue-sc100.ps1](continue-sc100.ps1) 只做两件事：
1. 先调 `detect_next_batch.py` 检查是否已完成
2. 没完成则把下一批页范围注入 prompt，调用 `copilot` CLI

```powershell
# 核心就是这个循环
for ($i = 1; $i -le $Rounds; $i++) {
  $State = (python detect_next_batch.py | ConvertFrom-Json)
  if ($State.done) { break }
  copilot -p "用 sc100-vision-md 继续处理...本轮建议页范围：$($State.next_page_range)"
}
```

**为什么只需要一句话 prompt？** 因为：
- `"用 sc100-vision-md"` → 触发 Copilot 加载 SKILL.md（200 行详细指令）
- `"继续处理"` → 匹配 SKILL.md 里的触发词
- `"本轮建议页范围：775-804"` → 告诉 Copilot 从哪开始

Copilot 加载 SKILL.md 后就知道：要先渲染图片、再提取文本、再视觉读题、再写 Markdown、再检查跨题……全部操作步骤都已经在 SKILL.md 里写好了。

---

## 三、单次处理的详细流程

每一轮 Copilot CLI 调用内部，AI 按照 SKILL.md 的指令执行以下步骤：

```
输入：PDF + 页范围 (如 775-804)
 │
 ├─ Step 1：渲染页面图片
 │   python render_pdf_pages.py --pdf "SC-100.pdf" --from 775 --to 804
 │   → output/vision-pages/sc-100-pages-775-804/page-775.png ~ page-804.png
 │
 ├─ Step 2：提取辅助文本
 │   python extract_page_text.py --pdf "SC-100.pdf" --from 775 --to 804
 │   → output/vision-pages/sc-100-pages-775-804/page-text-775-804.txt
 │
 ├─ Step 3：视觉审阅（AI 核心工作）
 │   - 打开每张 PNG 图片，逐页查看
 │   - 识别 "Question #xxx" 的起止边界
 │   - 对照辅助文本抄录题干和选项
 │   - HOTSPOT/DRAG DROP 题型看图理解答案区
 │   - 判断源答案是否正确，给出自己的推荐答案
 │   - 用中文解释为什么选这个答案
 │
 ├─ Step 4：写 Markdown
 │   → output/vision-md/sc-100-pages-775-804/sc-100-questions-311-320.md
 │   结构：每题包含题干、选项、源答案、推荐答案、中文判断、推理、备注
 │
 └─ Step 5：处理边界
     - 如果 page 804 的题目没结束 → 标记 carryover
     - 如果 page 775 开头是上一题的续 → 先接上上一批的尾巴
     - 实际可能只读到 page 790 就凑齐 10 题，或者读到 820 才凑齐
```

---

## 四、关键 Python 脚本角色

| 脚本 | 角色 | 输入 | 输出 |
|---|---|---|---|
| `detect_next_batch.py` | 状态探测器 | 扫描 `output/vision-md/` + 读 PDF 总页数 | JSON（进度、下一批范围、是否完成） |
| `render_pdf_pages.py` | 页面渲染器 | PDF + 页范围 | PNG 图片（每页一张） |
| `extract_page_text.py` | 文本提取器 | PDF + 页范围 | TXT 文件（按页分段的文本层） |

这三个脚本都是**纯工具**，不含 AI 逻辑。AI 逻辑全在 Copilot + SKILL.md 中。

---

## 五、数据流向图

```
SC-100 题目+答案+讨论.pdf  （源文件，~793页）
        │
        ▼
detect_next_batch.py ──────────────► JSON 状态
        │                              │
        │  ┌───────────────────────────┘
        │  │ next_page_range, carryover, done...
        ▼  ▼
continue-sc100.ps1 ─── done? ──► 退出
        │
        │ copilot CLI + prompt + 页范围
        ▼
┌─ Copilot + SKILL.md ──────────────────────────┐
│                                                 │
│  render_pdf_pages.py ──► PNG 图片               │
│  extract_page_text.py ──► TXT 辅助文本          │
│                                                 │
│  AI 视觉读图 + 判答 + 写 Markdown               │
│                                                 │
└─────────────────────────────────────────────────┘
        │
        ▼
output/vision-md/sc-100-pages-XXX-YYY/
  └── sc-100-questions-NNN-MMM.md
        │
        ▼
  （后续可转 JSON / SQLite → 考试软件导入）
```

---

## 六、如果包装成软件，需要的核心模块

将这套流程包装成 GUI 软件（比如 Tauri/Electron 应用），需要以下模块：

### 模块 1：进度管理器（替代 detect_next_batch.py）

```
职责：
- 扫描已有输出目录，计算当前进度
- 返回下一批应处理的页范围
- 判断是否全部完成
- 显示进度条（已处理/总页数、已完成题数）

实现方式：
- 可以直接调用现有 Python 脚本
- 也可以用 Rust/TypeScript 重写解析逻辑
```

### 模块 2：PDF 处理引擎（替代 render/extract 脚本）

```
职责：
- 将指定页范围渲染为图片
- 提取指定页范围的文本
- 管理输出文件路径

实现方式：
- 可继续调用 Python + PyMuPDF
- 也可以用 pdfium（TauriExam 已集成）替代
```

### 模块 3：AI 调度器（替代 continue-sc100.ps1 的循环）

```
职责：
- 组装 prompt（固定模板 + 动态页范围 + carryover 信息）
- 调用 Copilot CLI（或直接调 OpenAI/Azure OpenAI API）
- 捕获 AI 输出，写入 Markdown 文件
- 错误重试

核心决策：用 Copilot CLI 还是直接调 API？
- Copilot CLI：最简单，保持现有 SKILL 机制，但依赖 copilot 命令行工具
- 直接调 API：更灵活，但需要自己实现 SKILL.md 里的所有提示词注入
```

### 模块 4：SKILL 提示词管理（替代 SKILL.md 的自动加载）

```
职责：
- 如果用 Copilot CLI：不需要改动，SKILL.md 自动被加载
- 如果用 API：需要把 SKILL.md 的内容作为 system prompt 发送给 API
  - 包括：处理流程、质量规则、模板格式、判答指南
  - 需要动态注入：当前页范围、carryover 信息、已有进度

这是整个系统最核心的部分——200行的SKILL.md就是"大脑的指令集"
```

### 模块 5：GUI 界面

```
功能：
- 选择 PDF 文件
- 显示处理进度（进度条、已完成批次列表）
- 开始/暂停/停止处理
- 实时显示当前轮次的 AI 输出
- 浏览已生成的 Markdown（题目预览）
- 配置参数（模型、每批页数、并发数）
```

### 软件整体架构

```
┌─────────────────────────────────────────────┐
│                  GUI 界面                    │
│  [选PDF] [▶开始] [⏸暂停] [进度条████░░ 62%] │
│  ┌─────────────────────────────────────────┐ │
│  │ 实时日志 / 当前批次输出预览              │ │
│  └─────────────────────────────────────────┘ │
│  ┌─────────────────────────────────────────┐ │
│  │ 已完成批次列表 + 题目预览               │ │
│  └─────────────────────────────────────────┘ │
└────────────────┬────────────────────────────┘
                 │
     ┌───────────┴───────────┐
     ▼                       ▼
 进度管理器              AI 调度器
 (detect)            (copilot CLI / API)
     │                       │
     ▼                       ▼
 PDF 处理引擎         SKILL 提示词管理
 (render + extract)  (system prompt 组装)
     │                       │
     └───────────┬───────────┘
                 ▼
          文件系统 I/O
    output/vision-md/  ← Markdown 输出
    output/vision-pages/ ← PNG + TXT
```

---

## 七、核心工作原理总结

| 概念 | 通俗解释 |
|---|---|
| SKILL.md | 一份写给 AI 的"标准操作手册"，200 行详细指令。用户说"继续"，AI 就按手册干活 |
| detect_next_batch.py | "进度探针"——扫描已有文件，算出下一步该做什么 |
| continue-sc100.ps1 | "自动按钮"——循环按 35 次"继续"，每次先检查进度 |
| copilot CLI | AI 的入口——把 prompt 发给 AI，AI 加载 SKILL 后执行全流程 |
| prompt 里的 "用 sc100-vision-md" | 这句话让 Copilot 知道该加载哪个 SKILL 文件 |
| prompt 里的 "继续处理" | 匹配 SKILL.md 里的触发词，确认进入处理流程 |
| prompt 里的 "本轮建议页范围" | 告诉 AI 从哪页开始，避免 AI 自己猜 |
| carryover 机制 | 解决题目跨页/跨批次的衔接问题 |
| 每批 10 题 | 控制单次 AI 输出长度，避免上下文过长出错 |
| done=true 停止规则 | 防止 AI 在所有页面处理完后继续空转 |

---

## 八、要包装成软件，最小可行方案

**最省力的路径**：保持调用 `copilot` CLI，软件只做外壳。

```
软件 = 进度 UI + 循环控制 + 日志展示
         ↓
  底层仍然是:  detect_next_batch.py → copilot CLI → SKILL.md
```

**更完整的路径**：替换 Copilot CLI，直接调 AI API。

```
软件 = 进度 UI + 循环控制 + AI API 调用 + SKILL prompt 注入
         ↓
  底层变成:  detect_next_batch.py → OpenAI API (system=SKILL.md内容) → 解析输出 → 写 Markdown
```

两种路径的取舍：

| 维度 | 保持 Copilot CLI | 直接调 API |
|---|---|---|
| 开发量 | 小（只写 UI 壳） | 大（需要自己管 prompt/输出解析） |
| SKILL 维护 | 自动加载，改 SKILL.md 即生效 | 需要自己读文件注入 system prompt |
| 视觉读图 | Copilot 自动看图 | 需要自己调 vision API，传图片 |
| 依赖 | 必须装 Copilot CLI | 只需 API Key |
| 灵活性 | 受限于 CLI 参数 | 完全可控 |
| 成本 | Copilot 订阅 | API 按量付费 |

---

## 九、TauriExam 打包速度分析（dabao.ps1）

### 打包流程

`dabao.ps1` 执行的核心命令是：

```powershell
npm --prefix TauriExam run tauri -- build --no-bundle
```

这一条命令实际上触发了 **两步编译**：

1. **前端构建**（`tsc && vite build`）：TypeScript 类型检查 + Vite 打包 React 应用 → 生成 `dist/`
2. **Rust 编译**（`cargo build --release`）：编译 Tauri 后端 + 所有 288 个 crate → 生成 `tauri-exam.exe`

前端构建通常几秒就能完成，**绝大部分时间花在 Rust release 编译上**。

### 慢的原因分析

#### 原因 1：`reqwest` (blocking) — 最大的编译时间杀手 ⚠️

```toml
reqwest = { version = "0.12", features = ["blocking", "json", "native-tls"] }
```

`reqwest` 是一个基于 async 生态的 HTTP 客户端。即使你只用 `blocking` 模式（同步调用），它的内部实现仍然会：

- 编译完整的 **tokio** 异步运行时（创建一个隐藏的 runtime 来执行 blocking 调用）
- 编译 **hyper**（HTTP/1.1 + HTTP/2 实现）
- 编译 **h2**（HTTP/2 协议栈）
- 编译 **tower** + **tower-http**（中间件框架）
- 编译 **native-tls**（TLS 加密层，Windows 上包装 SChannel）
- 编译 **encoding_rs**（字符编码）

实际依赖链展开后，`reqwest` 单独就拉入了约 **80+ 个 crate**。

**你的实际用途**：仅仅是同步调用 OpenAI API 和 Microsoft Translator API——这两个功能用 `reqwest` 这种重量级库是大材小用。

**替代方案**：`ureq`（你在根项目的 `docintel-md` 已经在用了！）
```toml
# 替换前：~80 个 crate，编译慢
reqwest = { version = "0.12", features = ["blocking", "json", "native-tls"] }

# 替换后：~15 个 crate，编译快 5-10 倍
ureq = { version = "2", features = ["json", "tls"] }
```

`ureq` 是纯同步的 HTTP 客户端，不依赖 tokio/hyper/tower 等任何 async 组件，非常轻量。

#### 原因 2：`rusqlite` bundled — 每次从 C 源码编译 SQLite

```toml
rusqlite = { version = "0.32", features = ["bundled"] }
```

`bundled` feature 意味着每次编译时都会通过 `cc` crate 从 C 源代码编译完整的 SQLite 引擎（约 25 万行 C 代码合并成一个 `sqlite3.c`）。

这是一个合理选择（避免系统 SQLite 版本不匹配），但它是编译时间的贡献者之一。**这个基本没有好的替代，保留即可**——增量编译时 SQLite 不会重新编译，只在首次或 clean build 时才慢。

#### 原因 3：`pdfium-render` + `image` — PDF 渲染库

```toml
pdfium-render = "0.9.1"
image = { version = "0.25", default-features = false, features = ["png"] }
```

`pdfium-render` 本身是 PDFium 的 Rust 绑定，编译量不算大。`image` 只启用了 `png` feature 也算克制。这两个组件**合理，保留即可**。

#### 原因 4：Release 模式的默认优化级别

Tauri `build` 命令默认使用 `--release`，release 配置默认是：
```
opt-level = 3      # 最高优化
lto = false         # 未开启 LTO（好消息）
codegen-units = 16  # 默认，允许一定并行
incremental = false # release 模式默认关闭增量编译！
```

**`incremental = false` 是关键**——这意味着每次 `dabao.ps1` 即使你只改了一行代码，Rust 编译器仍然会重新编译大量 crate（虽然不是全部，但远多于增量模式）。

#### 原因 5：Tauri 框架自身的体量

`tauri v2` 本身就是一个中型框架，包含 WebView 管理、IPC、事件系统、安全策略、窗口管理等。这部分无法避免，是选择 Tauri 的固有成本。

### 各组件编译权重估算

| 依赖组 | 拉入的 crate 数（约） | 预估编译耗时占比 | 是否有轻量替代 |
|---|---|---|---|
| **reqwest** (blocking+json+native-tls) | ~80 | **35-40%** | ✅ `ureq` |
| **tauri** v2 | ~100 | 30-35% | ❌ 核心框架 |
| **rusqlite** (bundled) | ~6 + C 编译 | 10-15% | ❌ 保留 |
| **pdfium-render** + **image** | ~20 | 5-10% | ❌ 保留 |
| 其他 (serde, chrono, uuid, sha2...) | ~30 | 5-10% | ❌ 合理 |

### 优化建议（按效果排序）

#### 1. 把 `reqwest` 换成 `ureq` — 预期节省 30-40% 编译时间

这是最有效的单一改动。你的代码只用了 `reqwest::blocking::Client` 做同步 HTTP 请求，`ureq` 完全可以替代：

| 能力 | reqwest (blocking) | ureq |
|---|---|---|
| 同步 GET/POST | ✅ | ✅ |
| JSON 序列化 | ✅ | ✅ |
| TLS (HTTPS) | ✅ native-tls | ✅ rustls 或 native-tls |
| 流式读取 | ✅ | ✅ |
| 异步支持 | ✅（但你没用） | ❌（不需要） |
| 编译体积 | ~80 crate | ~15 crate |

#### 2. 开启 release 增量编译 — 大幅加速改动后的重编译

在 `TauriExam/src-tauri/Cargo.toml` 末尾添加：

```toml
[profile.release]
incremental = true
```

这样每次只改了少量代码后重新 `dabao.ps1`，Rust 编译器会复用上次的编译缓存，**改动后的重编译可从 60s+ 降到 10-20s**。

代价：release 可执行文件可能略大，但对本地工具无影响。

#### 3. 增加 codegen-units（换编译速度为少量运行时性能）

```toml
[profile.release]
incremental = true
codegen-units = 16    # 默认值，但可以显式写上确保不被覆盖
opt-level = 2         # 用 2 代替默认的 3，编译快一些，性能差异很小
```

#### 4. 根目录 Cargo.toml 的影响确认

根项目 `docintel-md` 的 Cargo.toml 有极端的 release 设置：
```toml
[profile.release]
lto = true          # 全程序链接时优化 — 非常慢
codegen-units = 1   # 单线程编译 — 最慢
opt-level = "z"     # 最小体积优化
```

好消息：TauriExam 是独立项目（不在同一个 workspace 里），这些设置**不影响 TauriExam 的编译**。但如果未来将它们合并为 workspace，这些设置会拖慢 TauriExam 编译。

### 总结

| 瓶颈 | 严重度 | 行动 |
|---|---|---|
| `reqwest` 拉入整个 async 生态但只用同步 | 🔴 高 | 换成 `ureq` |
| release 模式默认关闭增量编译 | 🔴 高 | 添加 `[profile.release] incremental = true` |
| `rusqlite bundled` 编译 SQLite C 源码 | 🟡 中 | 保留（无好替代） |
| Tauri 框架本身 | 🟡 中 | 无法优化（架构选择） |
| pdfium-render / image | 🟢 低 | 已经很精简 |

**预期效果**：将 `reqwest` → `ureq` + 开启增量编译后，日常改动后的打包时间应从 **60s+ 降到 15-25s**（首次全量编译仍需约 40-50s）。
