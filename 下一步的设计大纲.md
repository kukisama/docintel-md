# TauriExam 下一步设计大纲

本文档用于指导下一轮 AI 或开发者继续完善 `TauriExam`。目标不是一次做完所有功能，而是把后续方向拆成清晰、可执行、可验证的模块，优先补齐本地刷题闭环，再逐步接入 AI、翻译、统计和复杂题型能力。

## 设计目标

下一阶段围绕四个核心目标推进：

1. **本地题库工具稳定化**：题库目录、数据目录、PDF 渲染、错误提示都要更像正式软件。
2. **学习闭环完善**：错题、收藏、复习、历史回跳与返回路径要连起来。
3. **复杂题型框架预留**：Hotspot 和 Drag Drop 先把数据接口和 UI 框架留好，等 SQLite 有原始结构化数据后即可上线。
4. **AI 学习助手预留并逐步落地**：提供 OpenAI 兼容 Response API 的分析窗、追问、翻译缓存和学习统计分析能力。

优先原则：

- 题库内容保持只读。
- 用户学习数据、AI 缓存、翻译、设置都写入应用数据库。
- 先保证离线刷题体验，再做 AI 增强。
- 复杂题型不要强行从现有不完整数据里推断，先设计接口和降级自评。

## 当前基础状态

当前已经具备：

- Tauri + React + TypeScript 桌面应用。
- SQLite 题库浏览。
- 单选/多选考试行级点选。
- Hotspot / 非标准题人工自评。
- 历史考试记录和历史题目跳转到题库详情。
- 深浅主题。
- `question-banks/<name>.sqlite + <name>.pdf` 同名文件自动识别。
- PDF 页面按需渲染缓存的过渡方案。

当前仍需注意：

- PDF 渲染目前是过渡方案，依赖本机 Python + PyMuPDF，后续应改成 Rust 原生。
- App 数据库位置仍需正规化并迁移。
- 历史记录跳到题库后，目前缺少“返回刚才历史位置”的反向路径。
- Hotspot / Drag Drop 缺少完整原始选项结构，暂时不能做真实交互判分。
- AI、翻译、统计、配置面板尚未系统化。

## 阶段 1：基础设施稳定化

### 1.1 App 数据目录正规化

当前开发阶段依赖项目或 release 目录。正式设计应将用户数据迁移到系统用户数据目录。

Windows 推荐：

```text
%LOCALAPPDATA%/TauriExam/
  app.sqlite
  question-banks/
  cache/
    pages/
  logs/
  backups/
```

应用目录职责：

- `app.sqlite`：学习记录、设置、AI 缓存、翻译缓存、题目标记。
- `question-banks/`：用户放置同名 SQLite/PDF 题库文件。
- `cache/pages/`：PDF 页面渲染缓存。
- `logs/`：错误和诊断日志。
- `backups/`：应用数据库自动备份。

迁移策略：

1. 启动时检查新目录是否存在。
2. 如果新目录没有 `app.sqlite`，但旧目录有 `output/exam-tool/exam-tool.sqlite`，复制一份到新目录。
3. 不覆盖已有新库。
4. 题库文件不强制迁移，但可以提示用户把旧 `question-banks` 复制到新目录。
5. UI 提供“打开数据目录”和“打开题库目录”。

### 1.2 题库刷新和健康检查

控制面板或题库区域需要提供：

- 打开题库目录。
- 刷新题库。
- 题库健康检查。
- 当前题库目录路径显示。

题库扫描规则：

```text
question-banks/
  <题库名>.sqlite
  <题库名>.pdf
```

识别逻辑：

1. 扫描 `*.sqlite`、`*.sqlite3`、`*.db`。
2. 打开 SQLite，检查是否存在 `questions` 表。
3. 查找同名 PDF；PDF 缺失时仍注册题库，但禁用 PDF 原文页按钮或显示提示。
4. 统计题数、题型、状态、页码覆盖情况。
5. 返回前端显示健康状态。

建议新增 Tauri 命令：

```text
list_banks()
refresh_banks()
open_question_banks_dir()
check_bank_health(bank_id)
```

健康检查返回结构建议：

```text
BankHealth
- bank_id
- sqlite_ok
- pdf_found
- question_count
- empty_question_count
- empty_answer_count
- missing_page_count
- max_question_page
- pdf_page_count
- warnings[]
```

### 1.3 错误提示优化

所有底层错误应转换成用户能理解的提示。

示例：

```text
未发现题库
请将 <题库名>.sqlite 放入 question-banks 目录。

未找到同名 PDF
可以继续刷题，但无法加载原文页。

PDF 页面渲染失败
请确认 PDF 文件未损坏。若仍失败，请查看日志。
```

## 阶段 2：PDF 渲染改为 Rust 原生

### 2.1 目标

将当前 Python + PyMuPDF 过渡方案替换为 Rust 原生方案，避免安装版依赖外部 Python 环境。

推荐方案：

```text
Rust + pdfium-render + PDFium 动态库
```

### 2.2 实现要点

后端新增模块：

```text
src-tauri/src/pdf.rs
```

职责：

- 根据 `bank_id + page_number` 定位 PDF。
- 检查页面缓存是否存在。
- 不存在时用 PDFium 渲染指定页。
- 保存为 PNG 或 WebP。
- 返回图片路径或 base64 data URL。

缓存路径：

```text
%LOCALAPPDATA%/TauriExam/cache/pages/<bank-id>/<pdf-fingerprint>/page-001-scale-160.png
```

PDF 指纹建议由以下信息组成：

- 文件大小
- 修改时间
- 文件路径 hash

这样 PDF 更新后不会误用旧缓存。

### 2.3 性能预期

只按需渲染题目相关页。大多数题只需要 1–3 页。

预期体验：

- 首次加载：几十毫秒到一两秒，取决于 PDF 复杂度。
- 再次加载：直接读缓存，通常很快。

不建议默认全量渲染整本 PDF，因为页数多、占空间、很多页可能永远不会看。

### 2.4 过渡兼容

如果 Rust PDFium 集成遇到打包问题，可以暂时保留 Python 渲染作为开发模式 fallback，但 Release 应尽量不依赖 Python。

## 阶段 3：学习闭环完善

### 3.1 错题本

错题来源：

- 考试中 `is_correct = false` 的题。
- 用户手动标记为错题。

错题本能力：

- 按题库筛选。
- 按题型筛选。
- 按 Topic 筛选。
- 只看最近错题。
- 重做错题。
- 标记已掌握。
- 查看错题次数和最近错误时间。

可以从 `exam_answers` 动态统计，也可以维护汇总表。

建议先动态统计，后续性能需要时再加汇总表。

### 3.2 收藏和标记

复用或扩展 `question_flags`。

推荐 flag 类型：

```text
favorite
wrong
needs_review
mastered
important
answer_suspect
```

前端题目详情顶部提供：

```text
收藏 / 重点 / 待复习 / 已掌握 / 答案存疑
```

### 3.3 复习模式

新增独立的复习模式，不等同考试。

入口：

```text
复习
```

范围：

- 全部题。
- 错题。
- 收藏题。
- 待复习题。
- 未掌握题。
- 指定题型。
- 指定 Topic。

复习模式可显示答案、PDF 和 AI 解释，不强制计分。

### 3.4 历史跳转后的返回路径

当前：历史记录 → 题库详情，是单向跳转。

需要设计“上下文返回”。

推荐实现：前端维护一个 `returnContext`。

结构：

```text
ReturnContext
- from_view: history | exam_result | review
- session_id
- expanded_session_id
- scroll_position
- label
```

当用户从历史某题跳到题库详情时：

1. 设置 `returnContext`。
2. 进入题库详情。
3. 题库详情顶部显示：

```text
← 返回本次考试记录
```

点击后：

- 回到历史页。
- 自动展开原 session。
- 尽量恢复滚动位置或定位到原题。

这比浏览器式全局后退更稳定，因为 Tauri 当前是单页状态应用。

## 阶段 4：考试结果页和统计增强

### 4.1 考试结果页

考试提交后不要只跳历史列表，建议进入结果页。

结果页显示：

- 总题数。
- 正确数。
- 错误数。
- 正确率。
- 总耗时。
- 平均耗时。
- 每题耗时。
- 每题用户答案。
- 每题正确答案 / 推荐答案。
- 点击题目跳学习区。
- 一键重做错题。

结果页和历史记录复用同一套 session detail 数据。

### 4.2 学习统计

基础统计：

- 总做题数。
- 总考试次数。
- 正确率趋势。
- 错题排行榜。
- 最慢题排行榜。
- 按题型正确率。
- 按 Topic 正确率。
- 最近 7/30 天学习情况。

AI 统计增强：

- 总结最近错题中薄弱知识点。
- 分析用户常选错的干扰项。
- 输出复习建议。

AI 统计可以将以下信息提交给模型：

- 题干摘要。
- 用户答案。
- 推荐答案。
- 正误。
- Reasoning。
- Topic / 类型。

不要一次提交全部历史，先做最近一次考试或最近 N 道错题。

## 阶段 5：Hotspot 和 Drag Drop 框架预留

### 5.1 现状

当前 SQLite 中 Hotspot 和 Drag Drop 多数只有 `answer_areas` 或推荐答案映射，没有完整原始候选项池或选项组。

因此现在无法可靠实现真实交互判分。

### 5.2 Hotspot 数据接口预留

未来 SQLite 可新增：

```text
hotspot_rows
- id
- question_id
- prompt
- option_group
- correct_selection
- sort_order

hotspot_options
- id
- question_id
- option_group
- option_text
- sort_order
```

前端框架先预留：

- 如果存在 `hotspot_rows/options`，展示表格交互。
- 如果不存在，降级为当前人工自评。

交互类型：

- Yes/No 表格。
- 每行下拉选择。
- 多列匹配。

### 5.3 Drag Drop 数据接口预留

未来 SQLite 可新增：

```text
drag_slots
- id
- question_id
- slot_label
- correct_option
- sort_order

drag_options
- id
- question_id
- option_text
- sort_order
- is_distractor
```

前端框架先预留：

- 左侧候选项池。
- 右侧槽位。
- 支持拖拽或点击填入。
- 自动判分。
- 无结构化数据时降级人工自评。

### 5.4 后端命令预留

```text
get_interaction_model(bank_id, question_id)
```

返回：

```text
InteractionModel
- kind: choice | hotspot | drag_drop | manual
- options
- rows
- slots
- answer_key
- can_auto_grade
```

这样前端不需要直接猜数据库表结构。

## 阶段 6：控制面板和配置

### 6.1 控制面板页面

新增页面：

```text
控制面板 / 设置
```

功能分组：

1. 数据目录
   - 打开数据目录。
   - 打开题库目录。
   - 刷新题库。
   - 清理 PDF 缓存。

2. 题库管理
   - 题库健康检查。
   - PDF 缺失提示。
   - 题库统计。

3. AI 设置
   - 启用 / 禁用 AI。
   - Base URL。
   - API Key。
   - Model。
   - 默认温度。
   - 测试连接。

4. 翻译设置
   - 翻译提供商：AI / Microsoft Translator。
   - 目标语言。
   - 是否自动缓存。
   - 重新翻译策略。

5. 学习设置
   - 默认考试题数。
   - 默认题型。
   - 答案显示偏好。
   - 主题和字体。

### 6.2 app_settings 表

建议所有设置写入 `app_settings`：

```text
key TEXT PRIMARY KEY
value TEXT NOT NULL
updated_at TEXT NOT NULL
```

敏感信息如 API Key 不建议明文写 SQLite。第一版可先写入本地，后续应改为系统凭据或加密存储。

## 阶段 7：AI 接口窗和题目分析

### 7.1 AI 接口模式

用户要求支持 OpenAI 兼容模式的 Response API。

建议支持配置：

```text
base_url
api_key
model
```

请求模式以 Responses API 为主：

```text
POST /v1/responses
```

发送内容包括：

- 题干。
- 选项。
- 用户选择。
- 源答案。
- 推荐答案。
- 我的判断。
- Reasoning。
- 题型。
- Topic。
- 页码。

### 7.2 AI 分析窗

题目详情页增加：

```text
AI 详细分析
```

面板支持：

- 一键分析当前题。
- 用户追问。
- 多轮上下文。
- 复制回答。
- 保存分析结果。

首轮提示词目标：

```text
请用中文详细分析这道考试题：
1. 解释题干问什么。
2. 解释正确答案为什么正确。
3. 分析每个错误选项为什么错。
4. 提炼知识点。
5. 给出记忆方法。
6. 如果题目涉及过时产品名，请指出新旧名称。
```

### 7.3 AI 缓存

建议表：

```text
ai_conversations
- id
- bank_id
- question_id
- title
- provider
- model
- created_at
- updated_at

ai_messages
- id
- conversation_id
- role
- content
- created_at
```

也可以先做轻量缓存：只保存最后一次分析结果。

## 阶段 8：AI 翻译与 Microsoft Translator 对比

### 8.1 翻译需求

用户需要：

- 多段落缓存。
- 翻译后可中英文切换。
- 翻译不好时可重新翻译。
- 不重复调用。

翻译对象包括：

- 题干。
- 选项。
- 答案区。
- 推荐答案。
- 我的判断。
- Reasoning。
- Notes。

### 8.2 翻译缓存设计

不要把整题混成一个大字段。应按字段和段落缓存。

建议表：

```text
translations
- id
- bank_id
- question_id
- field_name
- segment_index
- source_hash
- language
- translated_text
- provider
- model
- version
- created_at
- updated_at
```

说明：

- `field_name`：`question_text`、`option:A`、`reasoning` 等。
- `segment_index`：同字段多段文本的顺序。
- `source_hash`：源文本变更后自动失效旧缓存。
- `version`：同一段可多次重新翻译，默认取最新版本。

### 8.3 AI 翻译方案

优点：

- 可以结合上下文，考试术语翻译更自然。
- 可以保持专有名词，如 Microsoft Entra ID、Conditional Access。
- 可以要求输出结构化 JSON，逐段对齐。

缺点：

- 成本较高。
- 速度较慢。
- 输出格式偶尔不稳定。

适合：

- 题干、解析、复杂 reasoning。
- 需要语义准确的技术说明。

### 8.4 Microsoft Translator 方案

优点：

- 简单、稳定、速度快。
- 适合批量短文本。
- 输出格式可控。

缺点：

- 对考试语境和云产品术语理解不如 AI。
- 不能解释，只是翻译。

适合：

- 题干和选项的基础翻译。
- 批量预翻译。

### 8.5 推荐策略

采用混合策略：

1. 默认使用 AI 翻译当前题，质量更好。
2. 控制面板预留 Microsoft Translator 作为可选 provider。
3. 翻译缓存表统一，不管 provider 是 AI 还是 Translator。
4. 用户可点击“重新翻译”，生成新 version。
5. UI 中支持：

```text
原文 / 译文 / 对照
```

这样未来切换 provider 不影响前端。

## 阶段 9：AI 学习统计分析

### 9.1 基础统计先行

先用 SQL 做基础统计：

- 错题列表。
- 用户选了什么。
- 推荐答案是什么。
- 哪些题型错得多。
- 哪些 Topic 正确率低。
- 哪些题耗时最长。

### 9.2 AI 统计摘要

在统计页提供：

```text
生成学习诊断
```

提交最近考试或最近错题给 AI，让它输出：

- 薄弱知识点。
- 常见误区。
- 需要复习的 Microsoft 安全产品。
- 建议复习顺序。
- 推荐重做题目列表。

### 9.3 数据边界

AI 统计不要默认上传全部题库。只上传必要数据：

- 错题题干。
- 选项。
- 用户答案。
- 推荐答案。
- reasoning。

并在控制面板中明确提示：启用 AI 后，当前题目内容会发送到配置的 AI 服务。

## 推荐实施顺序

### 第一步：基础设施

1. App 数据目录正规化和旧数据迁移。
2. 控制面板基础页。
3. 打开 / 刷新题库目录。
4. 题库健康检查。
5. 错误提示优化。

### 第二步：PDF 原生化

1. 集成 Rust PDFium。
2. 替换 Python 渲染。
3. 保留缓存策略。
4. 提供清理缓存按钮。

### 第三步：学习闭环

1. 错题本。
2. 收藏 / 标记。
3. 复习模式。
4. 历史跳转后的返回上下文。
5. 考试结果页。

### 第四步：复杂题型框架

1. `get_interaction_model` 后端接口。
2. Hotspot 框架 UI。
3. Drag Drop 框架 UI。
4. 数据缺失时自动降级人工自评。

### 第五步：AI 与翻译

1. 控制面板 AI 设置。
2. OpenAI 兼容 Responses API 客户端。
3. 当前题 AI 分析窗和追问。
4. 翻译缓存表升级。
5. 原文/译文/对照切换。
6. AI 学习统计诊断。

## 下一轮 AI 的工作建议

如果下一轮只做一个阶段，建议优先做：

```text
App 数据目录正规化 + 控制面板基础页 + 打开/刷新题库目录 + 题库健康检查
```

理由：

- 这是所有后续功能的基础。
- 对当前使用体验提升明显。
- 风险低、依赖少。
- 能让多题库使用方式更清楚。

如果下一轮要做 PDF，则优先做：

```text
Rust PDFium 按需渲染 + 缓存清理
```

但要先确认 PDFium 在 Windows Release 打包中的动态库分发方式。

## 验收标准

基础设施阶段完成后，应满足：

- 用户知道题库目录在哪里。
- 用户把同名 SQLite/PDF 放进去，点击刷新即可识别。
- 缺 PDF 时能刷题并给出明确提示。
- App 学习记录在固定用户数据目录，不依赖源码目录。
- 旧考试记录能迁移。
- 题库健康检查能指出常见问题。

学习闭环阶段完成后，应满足：

- 错题可集中查看和重做。
- 题目可收藏、标记、已掌握。
- 从历史跳题库后能一键回到原历史记录。
- 考试结束后能看到完整结果页。

AI 阶段完成后，应满足：

- 用户可配置 OpenAI 兼容接口。
- 当前题可生成详细中文分析。
- 用户可追问。
- 翻译可缓存、切换、重新翻译。
- AI 统计能基于错题给出薄弱点分析。

## 结论

下一步不应急着堆功能，而应先把基础设施和学习闭环打牢：

```text
数据目录正规化 → 题库管理 → 错题/收藏/复习 → 历史返回闭环 → PDF 原生渲染 → AI 分析/翻译/统计
```

这样 `TauriExam` 会从“能刷题的原型”逐步变成“可长期使用的本地考试学习平台”。
