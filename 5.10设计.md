# TauriExam 5.10 设计：错题优先的程序化学习分析

日期：2026-05-10  
范围：TauriExam 的考试/复习数据增强，不引入 AI 分析，不做复杂考试复盘大屏。

## 1. 设计目标

TauriExam 当前定位是本地题库考试与复习工具。本阶段不追求“AI 学习报告”，而是基于已有考试记录做程序化、确定性的错题辅助。

核心目标：

1. 错题列表中直接显示历史错误次数。
2. 考试中提醒当前题历史错过几次。
3. 支持按知识点/Topic 筛选题目。
4. 不额外做复杂考试复盘页。
5. 本次答对的题，不在历史/复习场景中突出显示历史错误次数，避免干扰。

## 2. 现状基础

当前已有数据表和字段足够支撑第一阶段：

- `exam_sessions`
  - 记录一次考试。
  - 已有 `bank_id`、`duration_seconds`、`total_questions`、`correct_count`、`wrong_count`。
- `exam_answers`
  - 记录每道题的考试结果。
  - 已有 `bank_id`、`question_id`、`sequence_number`、`user_answer`、`is_correct`、`duration_seconds`、`created_at`。
- `questions`
  - 题库题目表。
  - 已有 `topic`、`question_type`、`status`、`recommended_answer`。
- `question_flags`
  - 支持收藏、错题、待复习、已掌握等人工标记。

当前已经能做：

- 保存每题耗时。
- 保存每题对错。
- 复习模式列出错题。
- 历史中展开某次考试的答题记录。

本阶段主要是在这些数据上增加聚合查询和 UI 显示，不优先改数据库结构。

## 3. 明确不做的内容

### 3.1 暂不做复杂考试复盘页

原因：一个题库通常最多 300 题，用户更关注错题本身。当前错题排序 + 错误次数显示已经足够支持人工复习。

暂不做：

- 考试结束后的大型图表页。
- 本次考试知识点雷达图。
- AI 总结式学习报告。
- 大量“已答对但历史错过”的干扰信息。

### 3.2 暂不单独列出“新错题”

新错题可以由错误次数自然表达：

- 错误次数 = 1：说明这是最新发现/首次记录的错题。
- 错误次数 > 1：说明是历史反复错题。

因此不需要单独创建“新错题”列表。

### 3.3 本次答对时不突出历史错误次数

如果用户在本次考试中答对了某题，即使该题历史错过，也不在本次历史详情中突出错误次数。

原则：

- 错题场景：显示错误次数。
- 考试中：可以提醒历史错过次数。
- 本次答对的历史记录行：不强调错误次数。

这样避免用户已经修正的题继续制造视觉噪音。

## 4. 功能一：错题错误次数字段

### 4.1 UI 目标

在错题列表、复习列表或历史错题筛选结果中，每道题显示一个数字标志：

- `错 1`：第一次错。
- `错 2`：历史错过两次。
- `错 3+`：反复错题，需要重点复习。

推荐显示位置：

- 题号右侧。
- 或题卡标签区。
- 样式使用小 badge，不占主内容。

示例：

```text
Q128  [multiple_choice]  [错 3]
某公司需要设计零信任访问策略...
```

### 4.2 错误次数定义

`wrong_count_for_question` 定义为：

```sql
SELECT COUNT(*)
FROM exam_answers
WHERE bank_id = ?
  AND question_id = ?
  AND is_correct = 0;
```

注意：

- 只统计考试记录里的真实错误。
- `question_flags.flag_type = 'wrong'` 是人工标记，不计入次数。
- 如果需要显示人工错题但没有考试错误记录，可显示 `错 0 / 手动` 或只显示手动标记，不混入错误次数。

### 4.3 推荐后端实现

新增一个聚合结构：

```rust
struct QuestionPracticeStats {
    bank_id: String,
    question_id: String,
    attempt_count: i64,
    wrong_count: i64,
    latest_is_correct: Option<bool>,
    latest_answered_at: Option<String>,
    avg_duration_seconds: Option<f64>,
    max_duration_seconds: Option<i64>,
}
```

新增命令建议：

```rust
#[tauri::command]
fn get_question_practice_stats(bank_id: String, question_ids: Vec<String>) -> Result<Vec<QuestionPracticeStats>, String>
```

用途：

- `QuestionList` / `ReviewList` 一次性传入当前列表题目 id。
- 避免每道题单独查询。

### 4.4 SQL 写法

SQLite 可以用聚合查询：

```sql
SELECT
  question_id,
  COUNT(*) AS attempt_count,
  SUM(CASE WHEN is_correct = 0 THEN 1 ELSE 0 END) AS wrong_count,
  AVG(duration_seconds) AS avg_duration_seconds,
  MAX(duration_seconds) AS max_duration_seconds,
  MAX(created_at) AS latest_answered_at
FROM exam_answers
WHERE bank_id = ?
  AND question_id IN (...)
GROUP BY question_id;
```

`latest_is_correct` 单独查最近一条：

```sql
SELECT is_correct
FROM exam_answers
WHERE bank_id = ? AND question_id = ?
ORDER BY created_at DESC, rowid DESC
LIMIT 1;
```

如果担心逐题查最近结果，可以用窗口函数；但考虑题库最多约 300 题，简单实现即可。

### 4.5 前端类型

在 `TauriExam/src/types.ts` 增加：

```ts
export type QuestionPracticeStats = {
  bank_id: string;
  question_id: string;
  attempt_count: number;
  wrong_count: number;
  latest_is_correct: boolean | null;
  latest_answered_at: string | null;
  avg_duration_seconds: number | null;
  max_duration_seconds: number | null;
};
```

在 `api.ts` 增加：

```ts
getQuestionPracticeStats: (bankId: string, questionIds: string[]) =>
  invoke<QuestionPracticeStats[]>('get_question_practice_stats', { bankId, questionIds })
```

### 4.6 前端状态建议

在 `App.tsx` 增加：

```ts
const [practiceStats, setPracticeStats] = useState<Record<string, QuestionPracticeStats>>({});
```

当以下列表变化时刷新：

- `filtered`
- `reviewFiltered`
- `exam.questions`

但为了简单，第一版可以在 `loadQuestions`、`loadReview`、`loadHistory` 后刷新当前题库全部题目的 stats。

题库最多 300 题，全量读取可接受。

### 4.7 列表显示约定

只在以下场景显示错误次数：

1. 复习模式中的错题列表。
2. 历史考试展开后，且该行 `is_correct === false`。
3. 普通题库浏览中可选显示，但建议只显示轻量 badge。
4. 考试中显示历史错过次数。

不显示或弱化：

1. 本次答对的历史记录行。
2. 已掌握题列表。

## 5. 功能二：错题排序

### 5.1 设计原则

错题排序可以继续使用现有复习列表，不需要新增一个独立“反复错题”页面。

推荐排序：

1. 错误次数多的在前。
2. 错误次数相同，最近错的在前。
3. 再按题号升序。

### 5.2 后端实现方式

当前 `review_question_ids()` 的 `wrong` 模式使用：

```sql
SELECT DISTINCT question_id
FROM exam_answers
WHERE bank_id = ?1 AND is_correct = 0
UNION
SELECT question_id
FROM question_flags
WHERE bank_id = ?1 AND flag_type = 'wrong'
```

建议替换为带统计的排序查询。

伪 SQL：

```sql
SELECT question_id
FROM (
  SELECT
    question_id,
    COUNT(*) AS wrong_count,
    MAX(created_at) AS last_wrong_at,
    0 AS manual_only
  FROM exam_answers
  WHERE bank_id = ?1 AND is_correct = 0
  GROUP BY question_id

  UNION ALL

  SELECT
    question_id,
    0 AS wrong_count,
    updated_at AS last_wrong_at,
    1 AS manual_only
  FROM question_flags
  WHERE bank_id = ?1 AND flag_type = 'wrong'
    AND question_id NOT IN (
      SELECT question_id
      FROM exam_answers
      WHERE bank_id = ?1 AND is_correct = 0
    )
)
ORDER BY wrong_count DESC, last_wrong_at DESC, manual_only ASC;
```

然后 `list_review_questions()` 再按这个 id 顺序返回题目。注意当前代码最后有 `questions.sort_by_key(sequence_number)`，这会破坏错题排序，需要改为保持 `ids` 顺序。

### 5.3 保持 id 顺序的写法

Rust 端读取题目后：

```rust
let order = ids.iter().enumerate().map(|(i, id)| (id.clone(), i)).collect::<HashMap<_, _>>();
questions.sort_by_key(|q| order.get(&q.id).copied().unwrap_or(usize::MAX));
```

## 6. 功能三：考试中提醒历史错过次数

### 6.1 UI 目标

考试中当前题顶部显示：

```text
第 12 / 50 题   当前题 42s   历史错过 2 次
```

显示规则：

- `wrong_count > 0`：显示 `历史错过 X 次`。
- `wrong_count = 0`：不显示。
- 如果是模拟考试模式，未来可以通过设置隐藏；当前工具以复习为主，默认显示。

### 6.2 实现方式

考试开始时，针对 `picked` 题目批量加载 stats：

```ts
const ids = picked.map(q => q.id);
const stats = await api.getQuestionPracticeStats(bankId, ids);
```

然后在 `exam-head` 中：

```tsx
const currentStats = practiceStats[detail.id];
{currentStats?.wrong_count ? <span>历史错过 {currentStats.wrong_count} 次</span> : null}
```

### 6.3 注意计时器刷新

当前保存耗时是准确的，但 UI 中的秒数不一定每秒刷新，因为表达式依赖渲染。

建议增加：

```ts
const [nowTick, setNowTick] = useState(Date.now());
useEffect(() => {
  if (view !== 'exam' || !exam) return;
  const id = window.setInterval(() => setNowTick(Date.now()), 1000);
  return () => window.clearInterval(id);
}, [view, exam]);
```

显示时使用 `nowTick - exam.questionStartedAt`。

## 7. 功能四：知识点/Topic 筛选

### 7.1 是否依赖 AI

Topic 字段本身来源于题库生成阶段：

- `sc100-vision-md` skill 要求每题有 `Topic`。
- `exam-question-gen` skill 也要求每题有 `Topic`。
- 两个导入脚本都会把 `Topic` 写入 `questions.topic`。

因此 TauriExam 阶段不需要 AI 重新计算 Topic，只做读取和筛选。

AI 只在上游生成/整理 Markdown 时负责把 Topic 写好。

### 7.2 Markdown 生成约束

所有题目必须保留 metadata：

```markdown
- Topic: <稳定知识点名称>
```

Topic 命名建议：

- 使用英文或中英混合均可，但同一题库必须稳定。
- 不要一题一个过细 Topic。
- 推荐 8 到 20 个 Topic 覆盖整套题库。
- SC-100 可按官方技能域或安全架构主题，例如：
  - Zero Trust
  - Identity and Access
  - Governance Risk Compliance
  - Security Operations
  - Data and Applications
  - Infrastructure Security
  - Microsoft Defender
  - Microsoft Sentinel

### 7.3 SQL 导入要求

现有两个脚本已经导入 `topic`：

- `scripts/import_vision_md_to_sqlite.py`
- `scripts/import_exam_gen_to_sqlite.py`

后续要求：

1. 如果 Markdown 缺少 Topic，导入脚本应写入空字符串或 `Uncategorized`。
2. 推荐在导入完成后输出 Topic 覆盖统计。
3. 推荐增加可选校验：如果 Topic 为空的题超过 0，打印 warning。

### 7.4 UI 筛选方式

当前题库浏览已有：

- `typeFilter`
- `statusFilter`

建议增加：

- `topicFilter`

前端计算：

```ts
const topics = uniqueSorted(questions.map(q => q.topic || 'Uncategorized'));
```

但当前 `QuestionSummary` 没有 `topic` 字段，需要扩展：

后端 `QuestionSummary` 增加：

```rust
topic: Option<String>,
```

前端 `QuestionSummary` 增加：

```ts
topic: string | null;
```

后端 `list_questions()` 和 `list_review_questions()` SQL 增加 `topic`。

筛选规则：

```ts
const matchesTopic = topicFilter === 'all' || (question.topic || 'Uncategorized') === topicFilter;
```

### 7.5 和 MD/SQL 组合的关系

完整链路：

```text
Markdown metadata: - Topic: Identity and Access
        ↓
import_*.py parses meta_map(block)["topic"]
        ↓
SQLite questions.topic
        ↓
Tauri backend list_questions/list_review_questions returns topic
        ↓
React QuestionList adds topic filter
```

只要上游 Markdown 写得稳定，TauriExam 不需要任何 AI 参与。

## 8. 推荐实施顺序

### 第一步：增加题目统计接口

文件：

- `TauriExam/src-tauri/src/main.rs`
- `TauriExam/src/types.ts`
- `TauriExam/src/api.ts`

新增：

- `QuestionPracticeStats`
- `get_question_practice_stats`
- 前端 stats map

### 第二步：错题排序和 badge

文件：

- `TauriExam/src-tauri/src/main.rs`
- `TauriExam/src/QuestionList.tsx`
- `TauriExam/src/styles.css`

改动：

- `wrong` 复习列表按错误次数排序。
- 列表行显示 `错 X` badge。
- 本次答对的历史行不显示错误次数。

### 第三步：考试中历史错题提醒

文件：

- `TauriExam/src/App.tsx`
- `TauriExam/src/styles.css`

改动：

- 考试开始后加载当前考试题目的 stats。
- `exam-head` 显示历史错过次数。
- 增加每秒刷新计时器。

### 第四步：Topic 筛选

文件：

- `TauriExam/src-tauri/src/main.rs`
- `TauriExam/src/types.ts`
- `TauriExam/src/App.tsx`
- `TauriExam/src/QuestionList.tsx`
- `scripts/import_vision_md_to_sqlite.py`
- `scripts/import_exam_gen_to_sqlite.py`

改动：

- `QuestionSummary` 带 `topic`。
- 题库/复习列表增加 Topic 筛选。
- 导入脚本增加 Topic 缺失 warning。

## 9. 验收标准

### 错误次数

- 同一道题错 1 次，错题列表显示 `错 1`。
- 同一道题错 3 次，错题列表显示 `错 3`。
- 手动标记错题但没有考试错误记录，不误显示 `错 1`。

### 错题排序

- 错 3 次的题排在错 1 次之前。
- 错误次数相同，最近错的题排在前面。

### 考试中提醒

- 当前题历史错过 2 次，考试题头显示 `历史错过 2 次`。
- 当前题从未错过，不显示该提示。

### Topic 筛选

- 题库浏览可按 Topic 筛选。
- 复习列表可按 Topic 筛选。
- Markdown 中缺少 Topic 的题归入 `Uncategorized` 或显示为空分类。

## 10. 不变约定

- 不用 AI 计算本地学习分析。
- 不新增大而全的复盘页。
- 错题才是核心入口。
- 错误次数是题目级聚合数据，不写回原题库 SQLite。
- 用户考试历史写入 app.sqlite，题库 SQLite 保持只读。
