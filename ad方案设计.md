# AD 方案设计

> 本文档仅覆盖截图中“方案设计与准备”部分，不扩展到 Assessment、Test、Deploy、Hypercare 或运维交接等后续阶段。  
> 设计目标是把客户在表格中明确提出的内容逐项拆开，说明微软侧应如何设计、如何确认、需要哪些输入、输出什么方案，尽量做到范围清楚、逻辑完整、可被评审。

---

## 1. 本阶段设计范围

本阶段聚焦以下 8 项内容：

| 序号 | 表格事项 | 本文档处理方式 |
|---|---|---|
| 1 | AD 节点缩容 | 输出 AD 域控节点缩容设计思路和缩容方案内容 |
| 2 | 梳理域控信息，包括 P/V、数量、站点、OS 及 Patch 版本、FSMO、功能级别等 | 明确需要客户提供的信息字段、核对方法和设计依据 |
| 3 | 设计 AD 升级计划 / Timeline | 明确升级计划如何拆阶段、如何设置检查点和暂停点 |
| 4 | 设计 AD 升级解决方案，Upgrade Active Directory 2025 design | 输出升级到 Windows Server 2025 域控的总体设计 |
| 5 | 设计 AD 备份方案 | 输出升级前备份、验证和保留策略设计 |
| 6 | 设计 AD 恢复及回滚方案 | 输出故障场景、回滚策略和恢复路径设计 |
| 7 | 设计业务应用及访问影响 | 输出业务依赖、认证、访问、授权、查询影响分析设计 |
| 8 | 设计业务使用 AD 依赖类型 checklist | 输出可让客户和应用团队填写的依赖分类清单 |

---

## 2. 设计输入与责任说明

虽然本部分多数设计方案由微软侧提供，但设计不能脱离客户环境实际信息。因此建议按以下责任边界推进：

| 内容 | 客户/实施方输入 | 微软侧输出 |
|---|---|---|
| AD 现状信息 | 域控清单、站点、IP、OS、Patch、FSMO、DNS、GC、虚拟化/物理机信息 | 基于现状的设计判断、风险识别、缩容和升级建议 |
| 升级目标 | 目标 OS、目标时间窗口、是否提升功能级别、是否替换旧域控 | AD 2025 升级总体方案、阶段化 Timeline |
| 业务依赖 | 应用清单、认证方式、访问域控方式、业务负责人、验证窗口 | 业务影响分析方法、依赖类型 checklist、验证建议 |
| 备份能力 | 备份工具、备份周期、恢复方式、是否支持 System State 恢复 | AD 备份方案、恢复点设计、回滚策略建议 |
| 变更约束 | 停机窗口、不可中断系统、网络限制、安全审批要求 | 升级批次、暂停点、回退点、风险控制设计 |

---

## 3. AD 节点缩容设计

### 3.1 设计目标

AD 节点缩容不是简单减少域控数量，而是在保证认证、DNS、目录查询、组策略、站点访问和容灾能力的前提下，减少冗余或低价值域控，降低后续升级和运维复杂度。

微软侧应输出的缩容方案建议包括：

1. 当前域控节点是否存在重复、低利用率或历史遗留节点。
2. 每个站点是否仍需要本地域控。
3. 哪些域控可以下线，哪些必须保留或替换。
4. 缩容前需要迁移的角色和依赖，例如 FSMO、DNS、GC、应用绑定、监控配置。
5. 缩容应放在升级前、升级中还是升级后执行。

### 3.2 缩容判断维度

| 判断维度 | 需要检查的内容 | 设计考虑 |
|---|---|---|
| 站点与网络 | AD Site、Subnet、WAN 链路、跨站点延迟 | 分支站点如果存在本地登录、低带宽或高延迟，不能简单移除本地域控 |
| 认证压力 | 用户数量、服务器数量、登录峰值、服务账号认证频率 | 缩容后剩余域控需要承载峰值认证压力 |
| DNS 依赖 | 客户端 DNS 指向、服务器 DNS 指向、应用是否固定指向某台域控 | 下线域控前必须迁移 DNS 指向和 SRV 解析依赖 |
| GC 依赖 | 是否为 Global Catalog，是否承载跨域查询 | 多域林中不能随意移除 GC 能力 |
| FSMO 角色 | 当前 FSMO 角色持有者 | 计划下线的域控不能继续持有 FSMO |
| 应用绑定 | LDAP/LDAPS、Kerberos、NTLM、硬编码 DC、固定 IP | 应用若固定绑定某台旧域控，需要先调整应用配置 |
| 运维与监控 | SCOM、SCCM、Splunk、Zabbix、备份、杀软、安全审计 | 相关代理、监控、告警、日志采集对象需要同步调整 |
| 容灾能力 | 每个核心站点至少保留多少域控 | 核心站点通常不建议只保留单台域控 |

### 3.3 缩容方案建议结构

微软侧建议输出缩容方案时，至少包含以下内容：

| 模块 | 方案内容 |
|---|---|
| 当前节点清单 | 列出所有域控、站点、IP、OS、是否 GC、是否 DNS、FSMO 角色、业务依赖 |
| 目标节点清单 | 列出升级/缩容后的目标域控数量、角色分布、站点分布 |
| 保留节点 | 说明必须保留或替换的域控，以及保留原因 |
| 下线候选节点 | 说明可缩容节点、下线前置条件、依赖迁移项 |
| 缩容顺序 | 建议按站点、依赖、风险分批处理，避免一次性移除多个关键节点 |
| 风险控制 | 说明认证、DNS、复制、业务应用、监控告警的风险控制措施 |
| 回退点 | 在正式降级旧域控前设置暂停点，确认 DNS、应用、认证均已切走 |

### 3.4 不建议的缩容方式

1. 不建议在未确认应用依赖前直接下线旧域控。
2. 不建议同一窗口同时下线多个站点的域控。
3. 不建议将核心站点缩容到单台域控。
4. 不建议把域控 OS 升级、功能级别提升和大规模缩容放在同一窗口完成。
5. 不建议在 DNS 指向尚未迁移完成前降级 DNS 域控。

---

## 4. 域控信息梳理设计

### 4.1 需要梳理的信息字段

微软侧应要求客户至少提供以下信息，作为后续所有设计的基础：

| 信息类别 | 字段 | 用途 |
|---|---|---|
| 基础信息 | 域控主机名、域名、IP、站点、所在机房/区域 | 判断拓扑和站点覆盖 |
| 类型信息 | 物理机/虚拟机、虚拟化平台、资源规格、存储位置 | 判断迁移、备份和回退方式 |
| 系统信息 | Windows Server 版本、Patch 级别、安装语言、是否 Core | 判断升级兼容性和替换策略 |
| AD 角色 | 是否 DC、是否 GC、是否 DNS、是否 RODC | 判断角色迁移和缩容风险 |
| FSMO | 是否持有 Schema、Domain Naming、PDC、RID、Infrastructure | 判断迁移顺序和关键检查点 |
| 功能级别 | Forest Functional Level、Domain Functional Level | 判断可升级路径和限制 |
| 复制信息 | 复制伙伴、站点链接、最近复制状态、复制错误 | 判断升级前健康度 |
| DNS 信息 | AD 集成区域、转发器、客户端指向、动态注册 | 判断 DNS 迁移和验证范围 |
| 时间同步 | PDC 时间源、NTP 配置、成员服务器时间偏差 | 避免 Kerberos 认证异常 |
| 业务依赖 | 应用绑定、LDAP/LDAPS、证书、服务账号、监控代理 | 判断下线和迁移影响 |

### 4.2 信息核对方法

| 核对项 | 建议方式 | 设计关注点 |
|---|---|---|
| 域控清单 | 从 ADUC、PowerShell、CMDB、监控平台交叉核对 | 防止遗漏长期未维护但仍被使用的域控 |
| FSMO | 查询当前 FSMO 持有者 | 计划替换/下线前必须先迁移 |
| GC/DNS | 查询每台域控角色 | 确保升级后每个关键站点仍有 GC/DNS 能力 |
| 复制状态 | repadmin /showrepl、repadmin /replsummary | 有严重复制错误时不建议进入升级 |
| 健康状态 | dcdiag、事件日志、DNS 检查 | 作为升级前置门槛 |
| 功能级别 | 查询林/域功能级别 | 功能级别提升应单独评估，不与域控替换强绑定 |
| 应用连接 | 防火墙日志、LDAP 日志、应用配置、流量分析 | 识别硬编码域控和固定 IP 依赖 |

### 4.3 信息输出模板

建议最终形成《域控信息梳理表》，至少包括：

| 域控 | 站点 | IP | P/V | OS/Patch | GC | DNS | FSMO | 功能级别 | 业务依赖 | 处理建议 |
|---|---|---|---|---|---|---|---|---|---|---|
| 待客户填写 | 待客户填写 | 待客户填写 | 待客户填写 | 待客户填写 | 是/否 | 是/否 | 是/否 | 待客户填写 | 待客户填写 | 保留/替换/下线/待确认 |

---

## 5. AD 升级计划 / Timeline 设计

### 5.1 设计原则

AD 升级计划建议采用“准备 — 新建 — 验证 — 迁移 — 观察 — 下线”的阶段化方式，避免一次性完成过多不可逆操作。

建议原则如下：

1. 优先通过新增 Windows Server 2025 域控方式完成升级，而不是原地升级旧域控。
2. 新域控加入后先观察复制、DNS、认证和事件日志，再迁移关键角色。
3. FSMO、GC、DNS、客户端指向、应用绑定迁移应有明确顺序。
4. 旧域控下线应放在新域控稳定运行之后。
5. 林/域功能级别提升不建议与域控替换放在同一变更窗口。

### 5.2 建议 Timeline

| 阶段 | 工作内容 | 关键检查点 | 是否可暂停 |
|---|---|---|---|
| T0：方案确认 | 确认设计、缩容、备份、回滚、业务依赖 checklist | 客户确认输入完整，微软确认设计可行 | 是 |
| T1：升级前检查 | 检查 dcdiag、repadmin、DNS、FSMO、备份、事件日志 | 无阻塞性 AD 健康问题 | 是 |
| T2：准备新域控 | 准备 Windows Server 2025 服务器、补丁、网络、DNS、时间同步 | 服务器满足域控提升条件 | 是 |
| T3：提升新域控 | 将新服务器提升为域控，配置 DNS/GC | AD 复制正常，SYSVOL/NETLOGON 正常 | 是 |
| T4：稳定观察 | 观察新域控认证、DNS、事件日志、复制 | 无持续性严重错误 | 是 |
| T5：迁移角色 | 迁移 FSMO、调整 GC/DNS、调整监控和备份 | FSMO 查询正确，DNS/GC 可用 | 是 |
| T6：业务验证 | 验证登录、LDAP/LDAPS、Kerberos/NTLM、关键应用 | 业务系统负责人确认通过 | 是 |
| T7：旧域控处理 | 分批降级或保留旧域控观察 | 无应用继续依赖旧域控 | 是 |
| T8：功能级别评估 | 单独评估是否提升林/域功能级别 | 所有旧版本域控已移除，应用兼容性确认 | 是，建议单独窗口 |

### 5.3 Timeline 中的暂停点

必须设置以下暂停点，防止方案被质疑为“直接上生产”：

1. **新域控提升完成后暂停**：确认复制、DNS、SYSVOL 和事件日志。
2. **FSMO 迁移前暂停**：确认目标域控健康并完成备份。
3. **DNS 指向迁移前暂停**：确认应用依赖和客户端批次。
4. **旧域控降级前暂停**：确认无客户端和应用继续依赖旧域控。
5. **功能级别提升前暂停**：确认该动作必要性和不可逆风险。

---

## 6. AD 2025 升级解决方案设计

### 6.1 推荐目标架构

建议采用新增 Windows Server 2025 域控的方式完成 Active Directory 升级。目标架构需要明确：

1. 每个核心站点至少保留两台可用域控。
2. 每个需要本地认证的站点应明确是否保留本地域控。
3. DNS 与 GC 角色应按站点和业务访问路径合理分布。
4. FSMO 角色建议集中在稳定、受控、备份完善的核心站点域控上。
5. 旧域控逐步替换，不建议长时间混用过多版本域控。

### 6.2 升级路径设计

| 步骤 | 设计内容 | 关键注意事项 |
|---|---|---|
| 1 | 升级前健康检查 | 复制、DNS、FSMO、SYSVOL、时间同步必须无阻塞问题 |
| 2 | 准备 Windows Server 2025 新服务器 | IP、DNS、补丁、安全基线、时间同步、管理权限确认 |
| 3 | 加入域并提升为域控 | 确认 Schema/AD DS 兼容性和权限要求 |
| 4 | 配置 DNS/GC | 按站点需要配置，避免 DNS 服务能力不足 |
| 5 | 验证复制与认证 | repadmin、dcdiag、事件日志、登录测试、LDAP/Kerberos 测试 |
| 6 | 分批迁移客户端 DNS 指向 | 先小范围试点，再扩大范围 |
| 7 | 迁移 FSMO | 迁移后立即验证五大 FSMO 角色位置 |
| 8 | 观察稳定性 | 至少经历一个业务高峰或约定观察期 |
| 9 | 旧域控降级/下线 | 降级前确认无业务依赖、无 FSMO、非唯一 DNS/GC |
| 10 | 功能级别提升评估 | 单独评估，确认无旧版本域控和应用兼容问题 |

### 6.3 设计中必须回答的问题

微软侧方案需要明确回答以下问题：

1. 为什么采用新增域控替换，而不是原地升级。
2. 新域控数量和站点分布如何确定。
3. 哪些旧域控保留、替换或下线。
4. DNS 和 GC 如何迁移。
5. FSMO 迁移到哪台域控，为什么。
6. 是否提升林/域功能级别，若不提升，后续何时评估。
7. 对业务应用、认证协议和访问路径有什么影响。
8. 升级失败时如何暂停、回退或恢复。

### 6.4 功能级别设计建议

功能级别提升应谨慎处理。建议方案中明确：

1. 域控 OS 升级与功能级别提升是两个不同动作。
2. 本阶段可先完成 Windows Server 2025 域控替换，但不必立即提升功能级别。
3. 若客户要求提升功能级别，需要先确认所有域控版本、应用兼容性、回退限制和业务影响。
4. 功能级别提升建议安排独立变更窗口，并在升级后稳定观察完成后再执行。

---

## 7. AD 备份方案设计

### 7.1 备份目标

AD 升级前备份方案的目标不是“有备份即可”，而是确保在升级、角色迁移、DNS 调整或旧域控下线过程中出现问题时，客户具备可恢复能力。

### 7.2 备份范围

| 备份对象 | 备份内容 | 设计要求 |
|---|---|---|
| 现有域控 | System State、系统盘、AD DS 数据库、SYSVOL | 升级前完成，并确认备份成功 |
| FSMO 持有者 | System State、系统状态和事件日志 | 迁移 FSMO 前必须重点备份 |
| DNS 服务器 | DNS 区域、转发器、条件转发、客户端指向记录 | DNS 变更前导出配置 |
| GPO | GPO 备份、链接关系、权限设置 | 防止策略丢失或误改 |
| ADCS/证书相关服务器 | CA 配置、证书模板、私钥、数据库 | 如果存在 ADCS，应纳入专项备份 |
| 配置文档 | IP、站点、FSMO、GC、DNS、应用绑定 | 便于故障时人工恢复 |

### 7.3 备份验证要求

备份方案中应明确以下验证要求：

1. 确认备份任务完成且无错误。
2. 确认备份介质、权限和恢复路径可用。
3. 确认至少一台关键域控具备 System State 恢复能力。
4. 确认虚拟机快照不作为唯一回退手段。
5. 确认备份保留时间覆盖升级窗口和观察期。

### 7.4 备份时间点设计

| 时间点 | 备份动作 | 目的 |
|---|---|---|
| 升级前 3~5 天 | 完整备份关键域控 | 建立基础恢复点 |
| 升级前 24 小时内 | 重新备份 FSMO 持有者和关键域控 | 保证恢复点足够新 |
| FSMO 迁移前 | 确认源和目标域控备份可用 | 降低角色迁移风险 |
| 旧域控降级前 | 备份旧域控和配置文档 | 防止下线后发现依赖 |
| 功能级别提升前 | 单独备份和审批 | 功能级别动作风险更高 |

---

## 8. AD 恢复及回滚方案设计

### 8.1 回滚设计原则

AD 恢复与回滚方案必须区分“暂停”“回退配置”“恢复服务”和“灾难恢复”，不能把所有问题都设计成从备份恢复。

建议原则：

1. 能暂停观察的，不立即执行破坏性回滚。
2. 能通过配置回退解决的，不直接恢复 AD。
3. 涉及 FSMO、复制、功能级别、旧域控降级的动作，必须提前设置回退点。
4. 功能级别提升等不可逆或难回退动作，必须单独审批。

### 8.2 典型故障场景与处理设计

| 场景 | 可能原因 | 优先处理方式 | 是否需要恢复 |
|---|---|---|---|
| 新域控无法复制 | DNS、站点、权限、防火墙、时间同步问题 | 暂停升级，修复复制链路 | 通常不需要 |
| 新域控事件日志异常 | AD DS、DNS、Netlogon、Kerberos 配置异常 | 分析日志，修复配置后观察 | 通常不需要 |
| 客户端登录异常 | DNS 指向、时间偏差、站点定位错误 | 回退客户端 DNS 指向，检查站点配置 | 通常不需要 |
| 应用 LDAP/LDAPS 失败 | 证书、绑定地址、TLS、硬编码 DC | 回退应用连接目标或修复证书 | 通常不需要 |
| FSMO 迁移异常 | 源/目标域控状态异常、权限问题 | 暂停，确认角色位置和复制状态 | 视情况而定 |
| 旧域控降级后业务异常 | 应用仍依赖旧域控 | 恢复应用指向或重新启用替代路径 | 视情况而定 |
| 功能级别提升后兼容问题 | 旧系统或旧域控依赖 | 该类风险应提前避免 | 难以简单回滚 |

### 8.3 回滚点设计

| 回滚点 | 触发条件 | 回滚动作 |
|---|---|---|
| 新域控提升后 | 复制、DNS、SYSVOL、事件日志异常 | 暂停后续步骤，修复或降级新域控 |
| DNS 指向迁移后 | 客户端登录或业务访问异常 | 回退客户端/服务器 DNS 指向到原域控 |
| FSMO 迁移后 | FSMO 角色异常或复制异常 | 根据角色状态决定是否迁回原持有者 |
| 旧域控降级前 | 发现仍有业务依赖旧域控 | 暂停降级，先完成依赖迁移 |
| 旧域控降级后 | 出现未识别依赖 | 优先修复依赖指向，必要时评估恢复旧域控 |

### 8.4 明确不可轻易回滚的事项

方案中必须提醒客户：

1. 林/域功能级别提升不应视为普通回滚动作。
2. 旧域控降级后如需恢复，不应简单通过快照回滚域控。
3. AD 恢复涉及复制一致性，必须严格按 AD 恢复规范处理。
4. 虚拟机快照不能替代 System State 备份和恢复流程。

---

## 9. 业务应用及访问影响设计

### 9.1 影响分析目标

本项设计目标是明确每个业务系统如何使用 AD，并判断 AD 升级、域控替换、DNS 迁移、LDAPS 证书变化、旧域控下线是否会影响业务访问。

### 9.2 业务影响分析维度

| 维度 | 需要客户提供 | 设计关注点 |
|---|---|---|
| 应用基本信息 | 应用名称、负责人、业务等级、验证窗口 | 高优先级应用需提前验证 |
| AD 使用方式 | 登录认证、目录查询、权限组查询、服务账号、组策略 | 判断影响类型 |
| 协议类型 | Kerberos、NTLM、LDAP、LDAPS、DNS、SMB、RPC | 判断兼容性和端口依赖 |
| 访问目标 | 域名、域控主机名、固定 IP、VIP、DNS 轮询 | 识别硬编码域控风险 |
| 端口依赖 | 53、88、135、389、445、464、636、3268、3269 等 | 判断防火墙和访问策略 |
| 证书依赖 | LDAPS 证书、CA 链、证书过期时间 | 判断 Windows Server 2025 域控证书需求 |
| 服务账号 | 账号名称、权限、密码策略、SPN、委派 | 判断 Kerberos 和授权风险 |
| 验证方式 | 登录、查询、交易、批处理、接口调用 | 明确业务验收标准 |

### 9.3 常见影响与设计措施

| 影响类型 | 可能问题 | 设计措施 |
|---|---|---|
| DNS 指向变化 | 应用仍指向旧域控 IP | 提前识别固定 DNS/LDAP 目标，分批切换 |
| LDAP/LDAPS | 证书不受信、TLS 不兼容、绑定失败 | 提前验证证书链和 LDAPS 连接 |
| Kerberos | SPN、委派、时间偏差导致认证失败 | 检查时间同步、SPN、委派配置 |
| NTLM | 旧系统依赖 NTLM 或弱加密 | 识别旧系统，避免升级后策略变化影响 |
| 组策略 | GPO 未同步或客户端应用失败 | 检查 SYSVOL、GPO 备份和策略应用结果 |
| 跨站点访问 | 客户端定位到远端域控导致登录慢 | 核对 AD Site/Subnet 和 DNS SRV 记录 |
| 证书服务 | ADCS 模板或自动注册异常 | 若存在 ADCS，单独确认 CA 和模板依赖 |

### 9.4 业务验证设计

业务验证不应只做“能登录”检查，建议至少包括：

1. 用户登录验证。
2. 管理员登录验证。
3. 服务账号认证验证。
4. LDAP/LDAPS 查询验证。
5. Kerberos 认证和 SPN 相关应用验证。
6. NTLM 旧应用验证。
7. 基于 AD 组的授权验证。
8. 关键业务交易或接口调用验证。
9. 批处理、计划任务和中间件连接验证。
10. 升级后业务高峰期观察。

---

## 10. 业务使用 AD 依赖类型 Checklist 设计

### 10.1 Checklist 使用方式

该 checklist 由微软侧提供设计模板，客户/业务系统负责人填写。实施方根据填写结果安排验证和变更批次，微软侧根据结果识别高风险依赖。

### 10.2 Checklist 模板

| 字段 | 填写说明 | 示例 |
|---|---|---|
| 应用名称 | 业务系统名称 | OA、VPN、堡垒机、Exchange |
| 业务等级 | 核心/重要/一般 | 核心 |
| 应用负责人 | 业务或系统负责人 | 张三 |
| 是否使用 AD 登录 | 是/否 | 是 |
| 认证协议 | Kerberos/NTLM/LDAP/LDAPS/SAML/其他 | Kerberos + LDAP |
| AD 访问目标 | 域名/域控主机名/IP/VIP | dc01.contoso.com 或 10.x.x.x |
| 是否硬编码域控 | 是/否/未知 | 是 |
| 使用端口 | 53/88/389/636/3268/3269/445/其他 | 389、636 |
| 是否使用 LDAPS | 是/否 | 是 |
| 是否依赖证书 | 是/否 | 是 |
| 服务账号 | 账号名称和用途 | svc_app_query |
| 是否配置 SPN | 是/否/未知 | 是 |
| 是否使用委派 | 是/否/未知 | 否 |
| 是否依赖 AD 组授权 | 是/否 | 是 |
| 是否依赖 GPO | 是/否 | 否 |
| 是否依赖 DNS SRV 记录 | 是/否/未知 | 是 |
| 是否可切换到新域控 | 是/否/需验证 | 需验证 |
| 验证方式 | 登录/查询/交易/接口/批处理 | 登录 + 查询 |
| 验证负责人 | 执行业务验证的人 | 李四 |
| 验证窗口 | 可执行验证的时间 | 周六 22:00 |
| 风险等级 | 高/中/低 | 高 |
| 处理建议 | 保持域名访问/调整硬编码/提前证书验证等 | 去除硬编码 DC |

### 10.3 高风险标记规则

以下情况应在 checklist 中标记为高风险：

1. 应用硬编码旧域控主机名或 IP。
2. 应用使用 LDAPS，但证书链或 TLS 版本不明确。
3. 应用使用 NTLM 或旧版认证组件。
4. 应用使用固定服务账号且权限较高。
5. 应用依赖特定站点或特定域控。
6. 应用负责人无法确认验证方式。
7. 业务等级为核心，但没有测试环境。
8. 应用无法在变更窗口内验证。

---

## 11. 本阶段输出物清单

| 序号 | 输出物 | 说明 |
|---|---|---|
| 1 | AD 节点缩容方案 | 明确保留、替换、下线域控及原因 |
| 2 | 域控信息梳理表 | 作为升级、缩容、备份、回滚设计依据 |
| 3 | AD 升级 Timeline | 明确阶段、检查点、暂停点和回退点 |
| 4 | AD 2025 升级解决方案 | 明确目标架构、升级路径、FSMO/DNS/GC 迁移策略 |
| 5 | AD 备份方案 | 明确备份范围、时间点、验证要求和保留策略 |
| 6 | AD 恢复及回滚方案 | 明确故障场景、回滚点和恢复方式 |
| 7 | 业务应用及访问影响分析方案 | 明确业务依赖识别维度和验证方式 |
| 8 | 业务使用 AD 依赖类型 Checklist | 供客户和业务系统负责人填写，识别高风险依赖 |

---

## 12. 评审时需要客户确认的问题

为避免后续被质疑遗漏，方案评审时建议客户逐项确认：

1. 域控清单是否完整，是否包含所有生产、灾备、分支站点域控。
2. FSMO、DNS、GC、站点、Subnet 信息是否准确。
3. 是否同意采用新增 Windows Server 2025 域控替换旧域控的方式。
4. 是否同意功能级别提升与域控替换分开评估。
5. 是否确认业务应用依赖由各应用负责人填写和验证。
6. 是否确认旧域控下线前必须完成依赖迁移。
7. 是否确认备份可恢复，而不仅是备份任务成功。
8. 是否确认生产变更中设置暂停点和回退点。
9. 是否确认高风险应用需要提前测试或单独安排验证窗口。
10. 是否确认本阶段仅为方案设计，不包含后续测试、部署和运维交接实施。

---

## 13. 总结

本阶段方案设计应围绕客户表格中明确提出的 8 项内容展开：AD 节点缩容、域控信息梳理、升级计划、AD 2025 升级解决方案、备份、恢复回滚、业务影响和依赖 checklist。

微软侧需要重点输出“怎么设计、为什么这样设计、客户需要提供什么、风险在哪里、哪些动作需要暂停或单独评估”。其中，最容易被质疑的点包括：旧域控是否仍被业务依赖、DNS/GC/FSMO 如何迁移、备份是否可恢复、功能级别是否应同步提升、业务应用是否完成验证。因此方案中必须明确这些检查点和客户确认项。
