# 5.10 题库导入与拖拽结构验证报告

日期：2026-05-10

## 验证目标

验证两个上游生态是否符合最初设计：

1. PDF/vision 真实题库链路：使用现有 SC-100 题库 Markdown。
2. AI 纯生成链路：生成 DP-300 测试题。
3. 覆盖题型：single_choice、multiple_choice、drag_drop，并观察 HOTSPOT 的兼容导入情况。
4. 每条链路按较大样本验证，本次各 20 题。

## 测试输入

### SC-100 真实 PDF/vision 整理题

使用现有文件：

- `output/vision-md/sc-100-pages-147-182/sc-100-questions-051-060.md`
- `output/vision-md/sc-100-pages-183-214/sc-100-questions-061-070.md`

导入到临时验证库：

- `output/import-validation/sc100-existing-20.sqlite`

### DP-300 AI 纯生成题

新建 20 题测试集：

- `output/exam-gen/DP-300-validation/questions-001-010.md`
- `output/exam-gen/DP-300-validation/questions-011-020.md`

导入到临时验证库：

- `output/import-validation/dp300-ai-20.sqlite`

## 重要修正

本次验证发现并修复了一个关键 schema 问题：

- 最初 `drag_options.id` / `drag_slots.id` 是全局主键。
- 但设计中 `opt-a`、`slot-1` 是题内稳定 ID，多道拖拽题会重复使用。
- 已修正为：
  - `PRIMARY KEY(question_id, id)` for `drag_options`
  - `PRIMARY KEY(question_id, id)` for `drag_slots`

这与 TauriExam 后端按 `question_id` 查询并把 `id` 作为题内 key 返回的使用方式一致。

同时新增校验 warning：

- 如果题型是 `drag_drop`，但缺少 `Drag Options` 或 `Drag Slots`，导入继续进行，但输出 warning。

## 导入验证结果

### SC-100 existing 20

导入输出：

- 第 51–60 题：成功导入 10 题。
- 第 61–70 题：成功导入 10 题。
- Warning：
  - `sc-100-q0051: drag_drop missing structured Drag Options or Drag Slots`
  - `sc-100-q0063: drag_drop missing structured Drag Options or Drag Slots`

SQLite 统计：

| 项目 | 结果 |
|---|---:|
| questions | 20 |
| markdown_batches | 2 |
| options | 60 |
| answer_areas | 2 |
| drag_options | 0 |
| drag_slots | 0 |

题型分布：

| question_type | 数量 |
|---|---:|
| drag_drop | 2 |
| hotspot | 3 |
| multiple_choice | 5 |
| single_choice | 10 |

状态分布：

| status | 数量 |
|---|---:|
| parsed | 11 |
| needs_review | 8 |
| version_sensitive | 1 |

结论：

- 旧 SC-100 vision-md 文件可被导入，单选、多选、HOTSPOT 都能兼容。
- 旧 DRAG DROP 题只有 `Answer Area`，没有新设计要求的 `Drag Options` / `Drag Slots`，因此不能形成程序可判分的拖拽结构。
- warning 行为符合设计：不阻断历史数据导入，但明确暴露需要回补结构化拖拽区的题目。

### DP-300 AI 20

导入输出：

- 成功导入 20 题。
- 无 warning。

SQLite 统计：

| 项目 | 结果 |
|---|---:|
| questions | 20 |
| markdown_batches | 2 |
| options | 68 |
| answer_areas | 11 |
| drag_options | 16 |
| drag_slots | 11 |

题型分布：

| question_type | 数量 |
|---|---:|
| drag_drop | 4 |
| multiple_choice | 4 |
| single_choice | 12 |

拖拽结构：

| question_id | drag_options | drag_slots |
|---|---:|---:|
| dp-300-q0004 | 4 | 2 |
| dp-300-q0009 | 4 | 3 |
| dp-300-q0014 | 4 | 3 |
| dp-300-q0019 | 4 | 3 |

引用完整性：

- `missing_slot_refs = []`
- 所有 `drag_slots.correct_option` 都能在同题的 `drag_options.id` 中找到。
- 多道拖拽题重复使用 `opt-a`、`opt-b`、`slot-1` 等题内 ID，没有主键冲突。

结论：

- AI 生成链路符合最初设计。
- 单选、多选、拖拽均可导入。
- 结构化拖拽题可以落库到 `drag_options` / `drag_slots`，并满足 ID 引用完整性。

## 最终判断

1. **skill 约束是正确的**：DP-300 按新格式生成后，可完整落库并支持后续 UI 程序判分。
2. **导入脚本整体可用**：两条链路都能导入 20 题样本。
3. **拖拽题 schema 已修正为题内 ID 作用域**：这对真实大题库非常重要，否则多道题都会重复 `opt-a` / `slot-1`。
4. **历史 SC-100 旧 Markdown 需要回补**：已有 DRAG DROP 题目前多数只有 `Answer Area`，没有 `Drag Options` / `Drag Slots`。导入脚本会 warning，但不会凭空推断候选项池。
5. **下一步建议**：对旧 SC-100 的 drag_drop 题按 warning 列表逐题回补结构化拖拽区；之后再进入 TauriExam UI 真拖拽控件实现。

## 本次验证命令摘要

已执行：

- 两个 skill-local importer 的 Python 语法检查。
- SC-100 第 51–70 题真实 Markdown 导入验证。
- DP-300 20 题 AI 生成 Markdown 导入验证。
- SQLite 统计查询与 slot → option 引用完整性检查。
- 确认无 `__pycache__` 残留。
